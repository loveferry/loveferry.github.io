<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Spring Security（一） 体系结构</title>
    <url>/Spring-Security-Architecture-Authentication.html</url>
    <content><![CDATA[<p>&emsp;&emsp;<code>Spring Security</code>是Spring的安全框架，它能够帮助我们完成用户的认证和访问控制，具体的来说呢就是帮助我们判断当前请求是否需要认证，如果需要认证那么提供的认证信息是否合法有效，提供的认证信息是否具有足够的权限访问当前请求需要的资源。 </p>
<a id="more"></a>
<h1 id="认证（Authentication）"><a href="#认证（Authentication）" class="headerlink" title="认证（Authentication）"></a>认证（Authentication）</h1><p>&emsp;&emsp;<strong>认证主要完成对提供的用户信息进行识别，判断其是否合法有效。</strong> 在Spring Security中，为认证设计的接口是<code>AuthenticationManager</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.security.authentication;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.Authentication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.AuthenticationException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">AuthenticationManager</span> </span>&#123;</span><br><span class="line">	<span class="function">Authentication <span class="title">authenticate</span><span class="params">(Authentication authentication)</span></span></span><br><span class="line"><span class="function">			<span class="keyword">throws</span> AuthenticationException</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这个接口只有一个方法，所有的的用户认证都得调用这个接口去完成信息认证，参数<code>authentication</code>其类型也是一个接口，这个接口是对认证信息的封装，Spring Security通过调用该接口的方法获取用户信息完成认证并且后期对用户信息鉴权也需要从此接口获取信息。在调用认证方法后，如果认证顺利，那么就会返回一个认证信息，此认证信息因为已经完成认证，此时调用<code>isAuthenticated()</code>方法应该返回<code>true</code>。注意，返回的这个认证信息一般情况下是创建了一个新的对象，而不是修改参数对象并返回，所以这是一个访问器方法。在实际开发中，我们可以自定义<code>Authentication</code>接口的实现类，在其中封装更多的项目需要的参数，比如说我们用户信息需要携带一个邮箱地址，电话号码等，Spring Security本身提供了一些其实现类供我们使用，例如<code>UsernamePasswordAuthenticationToken</code>用于用户名密码认证的场景，<code>AnonymousAuthenticationToken</code>用于匿名认证的场景。</p>
<p>&emsp;&emsp;如果认证的过程并不顺利，比如说用户不存在，密码错误，用户失效等常见情况，此时应当抛出认证异常<code>AuthenticationException</code>。认证异常是一个抽象类。其有诸多的子类，在不同的情况下应当抛出不同子类异常，例如当账户失效时应当抛出的<code>AccountExpiredException</code>，当认证的用户名密码错误时应当抛出的<code>BadCredentialsException</code>和用户信息被锁定时应当抛出的<code>DisabledException</code>等。当抛出AuthenticationException异常时，服务应当响应给客户端401状态码，告诉客户端认证失败了，此时客户端应当自行处理最终将其渲染并呈现给用户。</p>
<p>&emsp;&emsp;AuthenticationManager的一个重要实现就是<code>ProviderManager</code>，一般情况下都是通过ProviderManager完成认证，接下来看看这个类能做些什么吧。</p>
<p>&emsp;&emsp;通过阅读源码上的注释可以了解到，它是将认证信息Authentication依次尝试AuthenticationProviders列表，如果响应结果不为空表示当前提供者有权决定认证请求，并且不再尝试其他提供者。如果后续提供者成功验证了请求，则将忽略较早的验证异常，并将使用成功的验证。如果没有后续提供者提供非空响应或new AuthenticationException，AuthenticationException则将使用最后收到的提供者。如果没有提供者返回非null的响应，或者表明它甚至可以处理an Authentication，ProviderManager则将抛出 ProviderNotFoundException。还可以设置一个parent的AuthenticationManager，如果没有配置的认证程序可以执行身份验证，也可以尝试使用此方法。但是通常不应该使用此功能。</p>
<p>&emsp;&emsp;完整的看一下这个类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.security.authentication;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Collections;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.apache.commons.logging.Log;</span><br><span class="line"><span class="keyword">import</span> org.apache.commons.logging.LogFactory;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.InitializingBean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.MessageSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.MessageSourceAware;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.support.MessageSourceAccessor;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.Authentication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.AuthenticationException;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.CredentialsContainer;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.SpringSecurityMessageSource;</span><br><span class="line"><span class="keyword">import</span> org.springframework.util.Assert;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProviderManager</span> <span class="keyword">implements</span> <span class="title">AuthenticationManager</span>, <span class="title">MessageSourceAware</span>,</span></span><br><span class="line"><span class="class">		<span class="title">InitializingBean</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 日志对象</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Log logger = LogFactory.getLog(ProviderManager<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 认证事件发布器，认证成功或者失败的通知机制</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> AuthenticationEventPublisher eventPublisher = <span class="keyword">new</span> NullEventPublisher();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 认证提供者列表，实际完成认证工作的列表</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> List&lt;AuthenticationProvider&gt; providers = Collections.emptyList();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 消息国际化的实现类，这里不做扩展</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">protected</span> MessageSourceAccessor messages = SpringSecurityMessageSource.getAccessor();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 父级认证管理器，相当于一个默认的认证程序，如果上面的认证提供者列表中没有一个完成认证工作，那么就会尝试使用这个认证管理器进行认证</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> AuthenticationManager parent;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 认证成功后是否擦除凭证信息</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">boolean</span> eraseCredentialsAfterAuthentication = <span class="keyword">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ProviderManager</span><span class="params">(List&lt;AuthenticationProvider&gt; providers)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(providers, <span class="keyword">null</span>); </span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 初始化对象的时候必须提供一个认证提供者的列表，父级的认证管理器可以为空</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ProviderManager</span><span class="params">(List&lt;AuthenticationProvider&gt; providers,</span></span></span><br><span class="line"><span class="function"><span class="params">            AuthenticationManager parent)</span> </span>&#123;</span><br><span class="line">        Assert.notNull(providers, <span class="string">"providers list cannot be null"</span>);</span><br><span class="line">        <span class="keyword">this</span>.providers = providers;</span><br><span class="line">        <span class="keyword">this</span>.parent = parent;</span><br><span class="line">        checkState();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 属性设置完成后校验，父级认证管理器和认证提供者列表二者至少有一个不能为空，因为若都为空则无法通过此对象完成认证工作 </span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        checkState();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">checkState</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (parent == <span class="keyword">null</span> &amp;&amp; providers.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(</span><br><span class="line">                    <span class="string">"A parent AuthenticationManager or a list of AuthenticationProviders is required"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 认证过程</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Authentication <span class="title">authenticate</span><span class="params">(Authentication authentication)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> AuthenticationException </span>&#123;</span><br><span class="line">        Class&lt;? extends Authentication&gt; toTest = authentication.getClass();</span><br><span class="line">        AuthenticationException lastException = <span class="keyword">null</span>;</span><br><span class="line">        Authentication result = <span class="keyword">null</span>;</span><br><span class="line">        Authentication parentResult = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">boolean</span> debug = logger.isDebugEnabled();</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">for</span> (AuthenticationProvider provider : getProviders()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!provider.supports(toTest)) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (debug) &#123;</span><br><span class="line">                logger.debug(<span class="string">"Authentication attempt using "</span> + provider.getClass().getName());</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                result = provider.authenticate(authentication);</span><br><span class="line">                <span class="keyword">if</span> (result != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    copyDetails(authentication, result);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">catch</span> (AccountStatusException e) &#123;</span><br><span class="line">                prepareException(e, authentication);</span><br><span class="line">                <span class="comment">// SEC-546: Avoid polling additional providers if auth failure is due to</span></span><br><span class="line">                <span class="comment">// invalid account status</span></span><br><span class="line">                <span class="keyword">throw</span> e;</span><br><span class="line">            &#125;<span class="keyword">catch</span> (InternalAuthenticationServiceException e) &#123;</span><br><span class="line">                prepareException(e, authentication);</span><br><span class="line">                <span class="keyword">throw</span> e;</span><br><span class="line">            &#125;<span class="keyword">catch</span> (AuthenticationException e) &#123;</span><br><span class="line">                lastException = e;</span><br><span class="line">            &#125;</span><br><span class="line">		&#125;</span><br><span class="line">        <span class="keyword">if</span> (result == <span class="keyword">null</span> &amp;&amp; parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// Allow the parent to try.</span></span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                result = parentResult = parent.authenticate(authentication);</span><br><span class="line">            &#125;<span class="keyword">catch</span> (ProviderNotFoundException e) &#123;</span><br><span class="line">                <span class="comment">// ignore as we will throw below if no other exception occurred prior to</span></span><br><span class="line">                <span class="comment">// calling parent and the parent</span></span><br><span class="line">                <span class="comment">// may throw ProviderNotFound even though a provider in the child already</span></span><br><span class="line">                <span class="comment">// handled the request</span></span><br><span class="line">            &#125;<span class="keyword">catch</span> (AuthenticationException e) &#123;</span><br><span class="line">                lastException = e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (result != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (eraseCredentialsAfterAuthentication</span><br><span class="line">                    &amp;&amp; (result <span class="keyword">instanceof</span> CredentialsContainer)) &#123;</span><br><span class="line">                <span class="comment">// Authentication is complete. Remove credentials and other secret data</span></span><br><span class="line">                <span class="comment">// from authentication</span></span><br><span class="line">                ((CredentialsContainer) result).eraseCredentials();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// If the parent AuthenticationManager was attempted and successful than it will publish an AuthenticationSuccessEvent</span></span><br><span class="line">            <span class="comment">// This check prevents a duplicate AuthenticationSuccessEvent if the parent AuthenticationManager already published it</span></span><br><span class="line">            <span class="keyword">if</span> (parentResult == <span class="keyword">null</span>) &#123;</span><br><span class="line">                eventPublisher.publishAuthenticationSuccess(result);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// Parent was null, or didn't authenticate (or throw an exception).</span></span><br><span class="line">        <span class="keyword">if</span> (lastException == <span class="keyword">null</span>) &#123;</span><br><span class="line">            lastException = <span class="keyword">new</span> ProviderNotFoundException(messages.getMessage(</span><br><span class="line">                <span class="string">"ProviderManager.providerNotFound"</span>,</span><br><span class="line">                <span class="keyword">new</span> Object[] &#123; toTest.getName() &#125;,</span><br><span class="line">                <span class="string">"No AuthenticationProvider found for &#123;0&#125;"</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        prepareException(lastException, authentication);</span><br><span class="line">        <span class="keyword">throw</span> lastException;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 将异常通过认证事件发布器发布出去 </span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="meta">@SuppressWarnings</span>(<span class="string">"deprecation"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">prepareException</span><span class="params">(AuthenticationException ex, Authentication auth)</span> </span>&#123;</span><br><span class="line">        eventPublisher.publishAuthenticationFailure(ex, auth);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 将需要认证的认证信息赋值到新的认证信息中</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">copyDetails</span><span class="params">(Authentication source, Authentication dest)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> ((dest <span class="keyword">instanceof</span> AbstractAuthenticationToken) &amp;&amp; (dest.getDetails() == <span class="keyword">null</span>)) &#123;</span><br><span class="line">            AbstractAuthenticationToken token = (AbstractAuthenticationToken) dest;</span><br><span class="line">            </span><br><span class="line">            token.setDetails(source.getDetails());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 获取认证提供者</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;AuthenticationProvider&gt; <span class="title">getProviders</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> providers;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 设置消息国际化 </span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setMessageSource</span><span class="params">(MessageSource messageSource)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.messages = <span class="keyword">new</span> MessageSourceAccessor(messageSource);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 设置认证事件发布器</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAuthenticationEventPublisher</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">            AuthenticationEventPublisher eventPublisher)</span> </span>&#123;</span><br><span class="line">        Assert.notNull(eventPublisher, <span class="string">"AuthenticationEventPublisher cannot be null"</span>);</span><br><span class="line">        <span class="keyword">this</span>.eventPublisher = eventPublisher;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 设置是否在认证成功后将凭证信息擦除</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setEraseCredentialsAfterAuthentication</span><span class="params">(<span class="keyword">boolean</span> eraseSecretData)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.eraseCredentialsAfterAuthentication = eraseSecretData;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 是否在认证成功后擦除凭证信息</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEraseCredentialsAfterAuthentication</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> eraseCredentialsAfterAuthentication;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 空的认证事件发布器</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">NullEventPublisher</span> <span class="keyword">implements</span> <span class="title">AuthenticationEventPublisher</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">publishAuthenticationFailure</span><span class="params">(AuthenticationException exception,</span></span></span><br><span class="line"><span class="function"><span class="params">                Authentication authentication)</span> </span>&#123;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">publishAuthenticationSuccess</span><span class="params">(Authentication authentication)</span> </span>&#123;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;以上代码因个人有强迫症，所以对排版有所改动，但是代码还是那个代码。这个类呢有一个重要属性<code>private List&lt;AuthenticationProvider&gt; providers</code>，它是<code>AuthenticationProvider</code>的集合，下面来了解一个这个认证提供者的组成成分。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.security.authentication;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.Authentication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.AuthenticationException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">AuthenticationProvider</span> </span>&#123;</span><br><span class="line">    <span class="function">Authentication <span class="title">authenticate</span><span class="params">(Authentication authentication)</span> <span class="keyword">throws</span> AuthenticationException</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">supports</span><span class="params">(Class&lt;?&gt; authentication)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;首先这是一个接口，这就意味着我们可以基于此自定义其实现类完成认证操作，该接口提供两个方法，一个是认证方法，要求其调用者需要提供待认证的信息且其实现者实现认证的具体逻辑，认证失败应当返回null或者抛出AuthenticationException，认证成功应当返回调用<code>isAuthenticated()</code>为true的认证对象。另一个方法是测试当前的这个认证支持者是否支持解析该类型的认证对象。显然，<strong>这个接口的作用就是用来提供一个认证器</strong>。</p>
<p>&emsp;&emsp;上面了解了AuthenticationProvider，再结合authenticate方法看就很明了了，就是一个个的认证器去尝试认证，直到认证成功或者抛出异常为止。说的更细一点就是认证器依次尝试认证，首先判断当前认证器是否支持认证该Authentication。不支持就让下一个认证器尝试认证，支持就调用认证器的认证方法开始认证，具体的认证实现(AuthenticationProvider.authenticate())可以返回空，返回空的时候继续向后尝试使用其他的认证器进行认证，如果所有的认证器都返回的空，那么就是使用父级的认证管理器尝试认证，这是最后的手段，如果再次失败，就会抛出<code>ProviderNotFoundException</code>。如果有认证器认证返回结果不为空，那么就可以认为认证成功了，不再尝试其他的认证器。如果认证过程中抛出了<code>InternalAuthenticationServiceException</code>或者<code>AccountStatusException</code>就会将此异常直接抛出，如果是别的AuthenticationException，那么就会先记录下来，然后让别的认证器尝试认证，如果最后认证器都认证失败了并且父级的认证管理器也认证失败了，那么就会抛出最后记录的那个认证异常。如果认证成功了，会判断是否是父级认证管理器完成的认证，如果是，那么这个认证管理器可能已经发布过认证成功的事件了，这里就不发布了，如果是认证器列表中的某个认证器完成的认证，那么先发布一下认证成功事件，再返回一个崭新的认证信息。</p>
]]></content>
      <categories>
        <category>Spring Security</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Java</tag>
        <tag>Spring Security</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Security（二） 认证配置类</title>
    <url>/Spring-Security-Authentication-Configuration.html</url>
    <content><![CDATA[<p>&emsp;&emsp;继上一篇 <a href="Spring-Security-Architecture-Authentication.html">Spring Security（一） 体系结构之认证</a> 后，这里研究一下Spring Security一个重要配置–认证配置。<code>AuthenticationConfiguration</code>是Spring提供的认证配置的配置类，该配置类的主要作用是用于生成一个全局的<code>AuthenticationManagerBuilder</code>注入到Spring容器中。使用者注入该配置类然后获取AuthenticationManagerBuilder再生成<code>AuthenticationManager</code>。</p>
<a id="more"></a>
<p>&emsp;&emsp;AuthenticationConfiguration中最核心的方法就是<code>getAuthenticationManager()</code>，其他的方法都是为这个方法服务的，该方法主要目的就是生成一个全局的认证管理器，但在讲解这个方法之前，需要先介绍一下在这个方法中用到的几个类。</p>
<h1 id="AuthenticationManagerBuilder"><a href="#AuthenticationManagerBuilder" class="headerlink" title="AuthenticationManagerBuilder"></a>AuthenticationManagerBuilder</h1><p>&emsp;&emsp;<strong>这个类采取的是建造者模式，用于创建一个认证管理器(AuthenticationManager)，能够轻松的建造出一个基于内存的认证，LDAP认证或者是基于jdbc的认证，添加<code>UserDetailsService</code>以及添加<code>AuthenticationProvider</code></strong> 。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> AuthenticationManagerBuilder <span class="title">authenticationManagerBuilder</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">                ObjectPostProcessor&lt;Object&gt; objectPostProcessor, ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    LazyPasswordEncoder defaultPasswordEncoder = <span class="keyword">new</span> LazyPasswordEncoder(context);</span><br><span class="line">    AuthenticationEventPublisher authenticationEventPublisher = getBeanOrNull(context, AuthenticationEventPublisher<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line"></span><br><span class="line">    DefaultPasswordEncoderAuthenticationManagerBuilder result = <span class="keyword">new</span> DefaultPasswordEncoderAuthenticationManagerBuilder(objectPostProcessor, defaultPasswordEncoder);</span><br><span class="line">    <span class="keyword">if</span> (authenticationEventPublisher != <span class="keyword">null</span>) &#123;</span><br><span class="line">        result.authenticationEventPublisher(authenticationEventPublisher);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;以上代码片段是截取自<code>AuthenticationConfiguration</code>类。<code>ObjectPostProcessor&lt;Object&gt; objectPostProcessor</code>是后置处理器，他的一个重要实现是<code>AutowireBeanFactoryObjectPostProcessor</code>，作用是生成Spring bean。<code>ApplicationContext context</code>是应用上下文。</p>
<p>&emsp;&emsp;这里初始化了一个解码器，解码器的类型是该配置类中的内部类<code>LazyPasswordEncoder</code>,这个内部类采用了委托模式，它从Spring 上下文中寻找解码器，如果找到了就委托给这个解码器，如果没找到就调用<code>PasswordEncoderFactories.createDelegatingPasswordEncoder();</code>生成解码器。而这个方法生成的是<code>DelegatingPasswordEncoder</code>的实例，该类的设计采用的是也是委托模式，他实际委派的对象是聚合了多个不同的解码器。</p>
<p>&emsp;&emsp;接下来从上下文中获取认证事件发布器，如果找到了就将该发布器的引用赋值给稍后生成的<code>AuthenticationManagerBuilder</code>。</p>
<p>&emsp;&emsp;注入到Spring上下文中的AuthenticationManagerBuilder的实例类型是<code>DefaultPasswordEncoderAuthenticationManagerBuilder</code>，这是认证配置类中的一个内部类，其内部方法实现都是调用父类的方法的，只不过实现类添加了一个解码器的属性，通过构造器将传入一个解码器，在进行配置的时候将解码器传递给对应的认证配置对象。</p>
<p>&emsp;&emsp;需要注意的是，使用AuthenticationManagerBuilder.build()构造出来的AuthenticationManager是<code>ProviderManager</code>的实例。看一下它的继承关系。</p>
<p><img src="/Spring-Security-Authentication-Configuration/AuthenticationManagerBuilder-extends.png" alt="继承关系"></p>
<p>&emsp;&emsp;通过继承关系可以看出，AuthenticationManagerBuilder和它的父类AbstractConfiguredecurityBuilder都没有实现build方法，所以调用的是AbstractSecurityBuilder方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> O <span class="title">build</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.building.compareAndSet(<span class="keyword">false</span>, <span class="keyword">true</span>)) &#123;</span><br><span class="line">        <span class="keyword">this</span>.object = doBuild();</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.object;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> AlreadyBuiltException(<span class="string">"This object has already been built"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;调用了<code>doBuild()</code>方法并返回其结果，AuthenticationManagerBuilder没有实现此方法，而父类AbstractConfiguredecurityBuilder实现了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> O <span class="title">doBuild</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="keyword">synchronized</span> (configurers) &#123;</span><br><span class="line">        buildState = BuildState.INITIALIZING;</span><br><span class="line">        </span><br><span class="line">        beforeInit();</span><br><span class="line">        init();</span><br><span class="line">        </span><br><span class="line">        buildState = BuildState.CONFIGURING;</span><br><span class="line">        </span><br><span class="line">        beforeConfigure();</span><br><span class="line">        configure();</span><br><span class="line">        </span><br><span class="line">        buildState = BuildState.BUILDING;</span><br><span class="line">        </span><br><span class="line">        O result = performBuild();</span><br><span class="line">        </span><br><span class="line">        buildState = BuildState.BUILT;</span><br><span class="line">        </span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;可以看到，最终返回的是<code>performBuild()</code>方法的返回值，而AuthenticationManagerBuilder实现了此方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> ProviderManager <span class="title">performBuild</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!isConfigured()) &#123;</span><br><span class="line">        logger.debug(<span class="string">"No authenticationProviders and no parentAuthenticationManager defined. Returning null."</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    ProviderManager providerManager = <span class="keyword">new</span> ProviderManager(authenticationProviders, parentAuthenticationManager);</span><br><span class="line">    <span class="keyword">if</span> (eraseCredentials != <span class="keyword">null</span>) &#123;</span><br><span class="line">        providerManager.setEraseCredentialsAfterAuthentication(eraseCredentials);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (eventPublisher != <span class="keyword">null</span>) &#123;</span><br><span class="line">        providerManager.setAuthenticationEventPublisher(eventPublisher);</span><br><span class="line">    &#125;</span><br><span class="line">    providerManager = postProcess(providerManager);</span><br><span class="line">    <span class="keyword">return</span> providerManager;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;显然，使用AuthenticationManagerBuilder的build方法构造AuthenticationManager其实例的类型就是ProviderManager。</p>
<h1 id="AuthenticationManagerDelegator"><a href="#AuthenticationManagerDelegator" class="headerlink" title="AuthenticationManagerDelegator"></a>AuthenticationManagerDelegator</h1><p>&emsp;&emsp;这个类是配置类中的一个内部类，他的作用是在获取认证管理器的时候防止出现无限递归（具体在什么样的情况下会出现无限递归我还没搞明白，源码注释是这么说的），它是采取了委托模式，将实际的认证任务委托给了前面我们初始化出来的AuthenticationManagerBuilder生成的认证管理器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">AuthenticationManagerDelegator</span> <span class="keyword">implements</span> <span class="title">AuthenticationManager</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> AuthenticationManagerBuilder delegateBuilder;</span><br><span class="line">    <span class="keyword">private</span> AuthenticationManager delegate;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Object delegateMonitor = <span class="keyword">new</span> Object();</span><br><span class="line">    AuthenticationManagerDelegator(AuthenticationManagerBuilder delegateBuilder) &#123;</span><br><span class="line">        Assert.notNull(delegateBuilder, <span class="string">"delegateBuilder cannot be null"</span>);</span><br><span class="line">        <span class="keyword">this</span>.delegateBuilder = delegateBuilder;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Authentication <span class="title">authenticate</span><span class="params">(Authentication authentication)</span></span></span><br><span class="line"><span class="function">                <span class="keyword">throws</span> AuthenticationException </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>.delegate != <span class="keyword">null</span>) &#123; </span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.delegate.authenticate(authentication);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">synchronized</span> (<span class="keyword">this</span>.delegateMonitor) &#123;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">this</span>.delegate == <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">this</span>.delegate = <span class="keyword">this</span>.delegateBuilder.getObject();</span><br><span class="line">                <span class="keyword">this</span>.delegateBuilder = <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.delegate.authenticate(authentication);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"AuthenticationManagerDelegator [delegate="</span> + <span class="keyword">this</span>.delegate + <span class="string">"]"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过源码可以看到，在调用认证方法时，判断委托对象delegate是否为空，如果为空那么就构造一个认证管理器赋值给委托对象并且使用委托对象完成认证。</p>
<h1 id="GlobalAuthenticationConfigurerAdapter"><a href="#GlobalAuthenticationConfigurerAdapter" class="headerlink" title="GlobalAuthenticationConfigurerAdapter"></a>GlobalAuthenticationConfigurerAdapter</h1><p>&emsp;&emsp;这个一个安全配置抽象类，里面对实现的接口<code>SecurityConfigurer</code>中的方法进行了空实现。在安全配置类AuthenticationConfiguration中，有多个它的子类被注入到Spring上下文中，随后被应用在了上下文中的AuthenticationManagerBuilder中。接下来我们抽取AuthenticationConfiguration中的部分代码展示。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> List&lt;GlobalAuthenticationConfigurerAdapter&gt; globalAuthConfigurers = Collections.emptyList();</span><br><span class="line"></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> GlobalAuthenticationConfigurerAdapter <span class="title">enableGlobalAuthenticationAutowiredConfigurer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> EnableGlobalAuthenticationAutowiredConfigurer(context);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> InitializeUserDetailsBeanManagerConfigurer <span class="title">initializeUserDetailsBeanManagerConfigurer</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> InitializeUserDetailsBeanManagerConfigurer(context);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> InitializeAuthenticationProviderBeanManagerConfigurer <span class="title">initializeAuthenticationProviderBeanManagerConfigurer</span><span class="params">(ApplicationContext context)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> InitializeAuthenticationProviderBeanManagerConfigurer(context);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Autowired</span>(required = <span class="keyword">false</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setGlobalAuthenticationConfigurers</span><span class="params">(List&lt;GlobalAuthenticationConfigurerAdapter&gt; configurers)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    Collections.sort(configurers, AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">    <span class="keyword">this</span>.globalAuthConfigurers = configurers;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里面涉及三个子类：</p>
<blockquote>
<p><code>EnableGlobalAuthenticationAutowiredConfigurer</code>：看源码就是在init方法中获取使用了<code>EnableGlobalAuthentication</code>注解的bean，然后通过debug日志打印，再就没有其他操作了。<br><code>InitializeUserDetailsBeanManagerConfigurer</code>：初始化一个认证数据服务配置对象应用到全局的 AuthenticationManagerBuilder 中，其主要作用就是初始化一个认证实现添加到Spring上下文的 AuthenticationManagerBuilder bean中，其类型为<code>DaoAuthenticationProvider</code>，获取Spring上下文中的<code>UserDetailsService</code>，<code>PasswordEncoder</code>和<code>UserDetailsPasswordService</code>，将其放入认证对象中。默认的表单登陆认证就是在这里配置的。这里加载的<code>UserDetailsService</code>bean是在<code>UserDetailsServiceAutoConfiguration</code>配置类中初始化的，初始化了一个基于内存的数据服务对象，Spring Security的默认的UUID密码就是在这里设置进去的，是从<code>SecurityProperties.User</code>类中获取。<br><code>InitializeAuthenticationProviderBeanManagerConfigurer</code>：初始化一个认证管理配置对象，从Spring上下文中获取 AuthenticationProvider 并加载到 AuthenticationManagerBuilder 中。</p>
</blockquote>
<p>&emsp;&emsp;说到这里，认证的全局配置基本说完了，这里总结一下，在<code>AuthenticationConfiguration</code>中，初始化了三个认证配置对象加载到Spring上下文中并且注入到自身对象中，其次初始化了<code>AuthenticationManagerBuilder</code>并且将之前的三个配置类应用到此对象中，然后将对象加载到Spring上下文中，这是一个全局的构造器，用来生成一个全局的认证配置器。此外，该类还提供了一个获取全局认证器的方法。</p>
]]></content>
      <categories>
        <category>Spring Security</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Java</tag>
        <tag>Spring Security</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Security（三） 用户信息配置</title>
    <url>/Spring-Security-User-Details.html</url>
    <content><![CDATA[<p>&emsp;&emsp;继上一篇 <a href="Spring-Security-Authentication-Configuration.html">Spring Security（二） 认证配置类</a> 后，这里展开说明一下其中点到的一个接口<code>UserDetailsService</code>，这个接口的作用在于加载用户数据。这里加载的用户数据就是我们合法的用户信息，前端登录传递的用户信息必须是这里加载的用户信息中的一员才可以通过校验。我们可以通过实现这个接口来完成自定义加载用户信息的实现。</p>
<a id="more"></a>
<h1 id="UserDetailsService"><a href="#UserDetailsService" class="headerlink" title="UserDetailsService"></a>UserDetailsService</h1><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> org.springframework.security.core.userdetails;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">UserDetailsService</span> </span>&#123;</span><br><span class="line">    <span class="function">UserDetails <span class="title">loadUserByUsername</span><span class="params">(String username)</span> <span class="keyword">throws</span> UsernameNotFoundException</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;加载用户信息的核心接口。它作为用户DAO在整个框架中使用，在<code>DaoAuthenticationProvider</code>中就是使用的此接口获取用户数据。这个接口只有一个访问器方法，简化了新的数据访问策略的支持。</p>
<p>&emsp;&emsp;关注一下这个访问器方法，他是基于用户名加载用户数据的，当找不到用户时应当抛出<code>UsernameNotFoundException</code>异常，在我们项目开发中，可能不同的实现类实现方式不同，例如有的区分大小写，有的不区分大小写，在这种情况下我们返回的用户信息和前端传过来的就不一致了，此时我们应当在认证通过后将有凭证的用户信息存储在<code>SecurityContext</code>中。</p>
<h1 id="用户信息组织结构"><a href="#用户信息组织结构" class="headerlink" title="用户信息组织结构"></a>用户信息组织结构</h1><p><img src="/Spring-Security-User-Details/UserDetailsService.png" alt="组织结构图"></p>
<ul>
<li><code>UserDetailsManager</code>：对用户信息接口进行扩展，使之可以对用户信息进行更新；</li>
<li><code>JdbcDaoImpl</code>：封装了默认的查询语句，用于从指定的表获取用户信息；</li>
<li><code>CachingUserDetailsService</code>：用户数据缓存实现，将加载的数据缓存起来，在使用时先从缓存中获取，若为空再从委托的UserDetailsService接口加载用户并缓存。其中的缓存接口<code>UserCache</code>既可以使用其实现类<code>EhCacheBasedUserCache</code>也可以使用<code>SpringCacheBasedUserCache</code>传入一个<code>Cache</code>从而使用Spring的缓存。</li>
<li><code>UserDetailsServiceDelegator</code>：是一个内部类，他的作用是获取全局或者局部的<code>AuthenticationManagerBuilder</code>的默认UserDetailsService对象，从而委托给其完成加载数据的操作。</li>
</ul>
<p>&emsp;&emsp;总结一下，UserDetailsService接口是用来获取系统的用户信息认证客户端提供的认证信息，在实际项目使用中，更多的是选择将用户信息持久化到数据库中，但是我们用户信息的表结构未必像JdbcUserDetailsManager中的表结构那样标准，所以我们可以实现UserDetailsManager自定义用户数据更新。此外，如果用户基数过大，查询用户信息的逻辑复杂，那么避免服务器负荷过高，可以将加载的用户信息缓存起来，我们可以借鉴CachingUserDetailsService，将实际工作委托给UserDetailsService，在加载用户信息前后添加判断逻辑，若缓存中存在则返回缓存中的用户信息，若不存在则加载并缓存，缓存的实现也是可以定制化开发的，自定义缓存类实现UserCache接口既可。</p>
]]></content>
      <categories>
        <category>Spring Security</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Java</tag>
        <tag>Spring Security</tag>
      </tags>
  </entry>
  <entry>
    <title>String</title>
    <url>/String.html</url>
    <content><![CDATA[<p>&emsp;&emsp;在这里记录一下自己对java字符串的学习过程，记录的同时也加深印象。</p>
<a id="more"></a>
<h3 id="字符串创建"><a href="#字符串创建" class="headerlink" title="字符串创建"></a>字符串创建</h3><p>&emsp;&emsp;我们平时使用String str = “This is a string.”这种方式创建的字符串，JVM会将该字符串存储在常量池中，而使用String str = new String(“This is a string.”)创建的字符串和普通的对象一样，会在堆空间开辟一块空间存储该对象，然后在栈空间开辟一块空间存储该对象的地址的值。</p>
<ul>
<li>例子</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StringTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        String str = <span class="string">"This is a string."</span>;</span><br><span class="line">        String str1 = <span class="string">"This is a string."</span>;</span><br><span class="line">        String str2 = <span class="string">"This is "</span> + <span class="string">"a string."</span>;</span><br><span class="line">        String str3 = <span class="keyword">new</span> String(<span class="string">"This is a string."</span>);</span><br><span class="line">        System.out.println(str == str1);</span><br><span class="line">        System.out.println(str == str2);</span><br><span class="line">        System.out.println(str == str3);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-create-test.png" alt="测试结果"></p>
<p>&emsp;&emsp;JVM在创建这些对象的时候首先在常量池中查找这些字面量是否存在，如果存在，那么该对象直接引用这个字面量即可，如果不存在那么在常量池中创建这个字面量，然后该对象引用这个字面量。</p>
<p>&emsp;&emsp;本例中，在创建str对象时，JVM在常量池中没有找到”This is a string”这个字面量，那么先在常量池创建这个字面量，然后变量名为str的对象引用这个字面量。创建变量名为str2的对象时在常量池中找到了这个字面量，所以直接引用这个字面量即可，所以，str和str2的地址值一样。在创建变量名为str3的对象时，首先在常量池寻找”This is “这个字面量，没有找到，那么创建该字面量，再寻找”a string.”这个字面量，没有找到，再创建该字面量，然后将这两个字面量进行拼接之后形成新的字面量”This is a string.”，在常量池中寻找到该字面量，那么直接引用这个字面量，所以str2引用的地址值和str以及str1一样，但是在创建str2这个对象的时候又在常量池中创建了两个字面量，这就是str2和str以及str1创建过程的不同之处。在创建str3对象时，使用了<code>public String(String original)</code>这个构造器来创建对象，很显然，这个构造器就是将一个字符串赋值给另一个对象，但是地址值发生变化，毕竟new了一个对象。</p>
<h3 id="字符串常用方法"><a href="#字符串常用方法" class="headerlink" title="字符串常用方法"></a>字符串常用方法</h3><h4 id="字符串分割方法"><a href="#字符串分割方法" class="headerlink" title="字符串分割方法"></a>字符串分割方法</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String[] split(String regex)</span><br><span class="line">String[] split(String regex, <span class="keyword">int</span> limit)</span><br></pre></td></tr></table></figure>
<ul>
<li><p>String regex 正则表达式分隔符</p>
</li>
<li><p>int limit 分割的份数</p>
</li>
</ul>
<h5 id="用法"><a href="#用法" class="headerlink" title="用法"></a>用法</h5><p>&emsp;&emsp;在java中，使用<code>split()</code>方法可以将字符串按照指定的分割符进行分割，String类中重载了该方法。可以看一下一个参数的方法实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String[] split(String regex) &#123;</span><br><span class="line">    <span class="keyword">return</span> split(regex, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;很显然，传递一个参数调用了传递两个参数的方法，int型的值传递0，也就是说使用regex分割符将字符串分割，返回分割后的不限制分割份数的字符串数组。</p>
<ul>
<li>看一下传递一个参数的方法，即不考虑分隔份数的情况下方法的使用。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"I-love-you"</span>;</span><br><span class="line">    String[] strs = str.split(<span class="string">"-"</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"使用普通的字符进行字符串的分隔\t长度："</span>+strs.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String str1 = <span class="string">"ferry.org.cn"</span>;</span><br><span class="line">    String[] strs1 = str1.split(<span class="string">"\\."</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"使用转义字符进行字符串的分隔\t长度："</span>+strs1.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs1)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String sql = <span class="string">"1 = 1 and 2 = 2 or 3 = 3 or4 = 4"</span>;</span><br><span class="line">    String[] sqls = sql.split(<span class="string">"and |or "</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"使用多个字符进行字符串的分隔\t长度："</span>+sqls.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : sqls)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-split-test.png" alt="运行结果"></p>
<p>&emsp;&emsp;如果字符串是使用转义字符进行连接的，我们需要以这些转义字符进行字符串的分割，那么需要在转义字符的前面加上<code>\\</code>，不然该分割符是无效的。</p>
<p>&emsp;&emsp;上面最后一个例子需要注意一下，split方法中可以使用<code>|</code>字符将多个字符作为分隔符。而本例中我使用了两个分割符对该字符串进行分割，<code>and</code>和<code>or</code>，注意，and和or后面都是有空格的，所以，本例中最后一个or4 = 4并没有被分割，因为or和4中间并没有空格。</p>
<p>&emsp;&emsp;值得一提的是，如果你需要进行分割的字符串并不包含你给定的分割符，那么它会返回一个长度为1的字符串数组，该数组中的唯一一个元素就是待分割的字符串。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"I love you"</span>;</span><br><span class="line">    String[] strs = str.split(<span class="string">"-"</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"使用普通的字符进行字符串的分隔\t长度："</span>+strs.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-split-test1.png" alt="运行结果"></p>
<p>&emsp;&emsp;还有一种可能是需要注意一下的，当你使用多个分割符时，如果待处理的字符串中这几种分割符正好在一起，那么会发生什么呢，看一个例子。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"ferry.org.cn"</span>;</span><br><span class="line">    String[] strs = str.split(<span class="string">"\\.|org"</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"长度："</span>+strs.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-split-test3.png" alt="运行结果"></p>
<p>&emsp;&emsp;运行结果就是：分割成了四个字符串，其中两个为空字符串。那么这个结果怎么产生的呢？这是因为在分割这个字符串时遇到第一个分割符<code>&quot;.&quot;</code>，得到第一个字符串”ferry”和剩余字符串”org.cn”，接着执行遇到第二个分割符<code>&quot;org&quot;</code>,这个分割符位于字符串起始位置，那么就会分割出一个空字符串然后得到剩余字符串”.cn”，继续执行，遇到第三个分割符<code>&quot;.&quot;</code>，位于字符串起始位置，分割得到空字符串和剩余字符串，继续执行，未找到分割符，将剩余字符串添加到集合中并转换成数组返回。</p>
<ul>
<li>指定分割份数的分割方法</li>
</ul>
<p>&emsp;&emsp;指定分割份数我们考虑四种情况，第一种：limit为负值；第二种：limit为0；第三种：limit值在待分割的字符串能分割的份数的有效范围内；第四种：limit值大于待分割的字符串能分割的份数。当然，第二种情况不用考虑了，前面只传递一个参数分割字符串的正则表达式调用的就是limit=0的方法，该种情况下不考虑分割份数，按给定的正则表达式分割并返回分割后的字符串数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str1 = <span class="string">"I-love-you"</span>;</span><br><span class="line">    String[] strs1 = str1.split(<span class="string">"-"</span>, -<span class="number">1</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"分割份数为负值：\t长度："</span>+strs1.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs1)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String str2 = <span class="string">"I-love-you"</span>;</span><br><span class="line">    String[] strs2 = str2.split(<span class="string">"-"</span>, <span class="number">2</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"分割份数在待分割字符串能分割的份数的范围内：\t长度："</span>+strs2.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs2)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    String str3 = <span class="string">"I-love-you"</span>;</span><br><span class="line">    String[] strs3 = str3.split(<span class="string">"-"</span>, <span class="number">999</span>);</span><br><span class="line">    System.out.println(<span class="string">"/*---------------------分割符---------------------------*/"</span>);</span><br><span class="line">    System.out.println(<span class="string">"分割份数大于待分割字符串能分割的份数：\t长度："</span>+strs3.length);</span><br><span class="line">    <span class="keyword">for</span>(String s : strs3)&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-split-test2.png" alt="运行结果"></p>
<ul>
<li><p>当limit为负值时，和limit=0返回结果一样，都是能分割几份就分割几份。</p>
</li>
<li><p>当limit在分割份数有效范围内时，前面几份分割按指定分割符分割，最后一份则为剩余未分割的字符串。</p>
</li>
<li><p>当limit大于有效分割份数时，分割结果与limit=0结果一样。</p>
</li>
</ul>
<h4 id="字符串比较是否相等"><a href="#字符串比较是否相等" class="headerlink" title="字符串比较是否相等"></a>字符串比较是否相等</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object anObject)</span></span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>Object anObject: 待比较的Object对象</li>
</ul>
<h5 id="用法-1"><a href="#用法-1" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"ferry"</span>;</span><br><span class="line">    Object obj = <span class="string">"ferry"</span>;</span><br><span class="line">    System.out.println(str.equals(obj));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/string-equals.png" alt="运行结果"></p>
<h5 id="源码分析"><a href="#源码分析" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object anObject)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span> == anObject) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (anObject <span class="keyword">instanceof</span> String) &#123;</span><br><span class="line">        String anotherString = (String)anObject;</span><br><span class="line">        <span class="keyword">int</span> n = value.length;</span><br><span class="line">        <span class="keyword">if</span> (n == anotherString.value.length) &#123;</span><br><span class="line">            <span class="keyword">char</span> v1[] = value;</span><br><span class="line">            <span class="keyword">char</span> v2[] = anotherString.value;</span><br><span class="line">            <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> (n-- != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (v1[i] != v2[i])</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">                i++;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>判断两个对象地址值是否相等，相等返回true，否则判断2；</p>
</li>
<li><p>判断形参类型是否为字符串，不是字符串返回false，是判断3；</p>
</li>
<li><p>将形参强制类型转化为字符串，判断两个字符串长度是否相等，不相等返回false，相等判断4；</p>
</li>
<li><p>循环比较两个字符串中的每一个字符是否相等，遇到不相等的，方法返回false，全部相等返回true。</p>
</li>
</ol>
<h4 id="字符串忽略大小写比较是否相等"><a href="#字符串忽略大小写比较是否相等" class="headerlink" title="字符串忽略大小写比较是否相等"></a>字符串忽略大小写比较是否相等</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">equalsIgnoreCase</span><span class="params">(String anotherString)</span></span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>String anatherString: 待比较的字符串对象</li>
</ul>
<h5 id="用法-2"><a href="#用法-2" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"FERRY"</span>;</span><br><span class="line">    String msg = <span class="string">"ferry"</span>;</span><br><span class="line">    System.out.println(str.equalsIgnoreCase(msg));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/string-equalsIgnore.png" alt="运行结果"></p>
<h5 id="源码分析-1"><a href="#源码分析-1" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equalsIgnoreCase</span><span class="params">(String anotherString)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">this</span> == anotherString) ? <span class="keyword">true</span></span><br><span class="line">            : (anotherString != <span class="keyword">null</span>)</span><br><span class="line">            &amp;&amp; (anotherString.value.length == value.length)</span><br><span class="line">            &amp;&amp; regionMatches(<span class="keyword">true</span>, <span class="number">0</span>, anotherString, <span class="number">0</span>, value.length);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这个方法的实现很简单，分为几个步骤：</p>
<ol>
<li><p>判断两个字符串的引用是否相等，相等返回true；</p>
</li>
<li><p>判断形参是否为空，若为空返回false，否则判断3；</p>
</li>
<li><p>判断两个字符串长度是否相等，相等判断4，否则返回false；</p>
</li>
<li><p>调用<code>regionMatches()</code>方法判断每个字符是否相等。</p>
</li>
</ol>
<p>&emsp;&emsp;<code>regionMatches(boolean ignoreCase, int toffset, String other, int ooffset, int len)</code>方法的作用是判断一个区域内两个字符串是否相等，相等返回true，否则返回false。当形参ignoreCase为true时忽略大小写进行比较。</p>
<h4 id="字符串比较某个区域内是否相等"><a href="#字符串比较某个区域内是否相等" class="headerlink" title="字符串比较某个区域内是否相等"></a>字符串比较某个区域内是否相等</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">regionMatches</span><span class="params">(<span class="keyword">int</span> toffset, String other, <span class="keyword">int</span> ooffset, <span class="keyword">int</span> len)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">regionMatches</span><span class="params">(<span class="keyword">boolean</span> ignoreCase, <span class="keyword">int</span> toffset, String other, <span class="keyword">int</span> ooffset, <span class="keyword">int</span> len)</span></span>;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>boolean ignoreCase: 是否忽略大小写</p>
</li>
<li><p>int toffset: 调用方法的字符串的子区域起始偏移量</p>
</li>
<li><p>String other: 待比较字符串</p>
</li>
<li><p>int ooffset: 待比较字符串子区域起始偏移量</p>
</li>
<li><p>int len: 要比较的字符数量</p>
</li>
</ul>
<h5 id="用法-3"><a href="#用法-3" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"I love ferry"</span>;</span><br><span class="line">    String msg = <span class="string">"You love ferry"</span>;</span><br><span class="line">    System.out.println(str.regionMatches(<span class="number">2</span>, msg, <span class="number">4</span>, <span class="number">4</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-rem-1.png" alt="运行结果"></p>
<p>&emsp;&emsp;偏移量的理解，本例中调用该方法的字符串偏移量为2，也就是偏移两个字符，从第三个字符开始比较，同样，msg字符偏移量为4，就是偏移4个字符，从第五个字符开始比较。其实也就是比较<code>&quot;love&quot;</code>和<code>&quot;love&quot;</code>是否相等。</p>
<ul>
<li>忽略大小写比较两个字符串某一区域是否相等</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    String str = <span class="string">"I love ferry"</span>;</span><br><span class="line">    String msg = <span class="string">"You LOVE ferry"</span>;</span><br><span class="line">    System.out.println(str.regionMatches(<span class="keyword">true</span>, <span class="number">2</span>, msg, <span class="number">4</span>, <span class="number">4</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-rem-2.png" alt="运行结果"></p>
<h5 id="源码分析-2"><a href="#源码分析-2" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">regionMatches</span><span class="params">(<span class="keyword">int</span> toffset, String other, <span class="keyword">int</span> ooffset, <span class="keyword">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">char</span> ta[] = value;</span><br><span class="line">    <span class="keyword">int</span> to = toffset;</span><br><span class="line">    <span class="keyword">char</span> pa[] = other.value;</span><br><span class="line">    <span class="keyword">int</span> po = ooffset;</span><br><span class="line">    <span class="keyword">if</span> ((ooffset &lt; <span class="number">0</span>) || (toffset &lt; <span class="number">0</span>)</span><br><span class="line">            || (toffset &gt; (<span class="keyword">long</span>)value.length - len)</span><br><span class="line">            || (ooffset &gt; (<span class="keyword">long</span>)other.value.length - len)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (len-- &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (ta[to++] != pa[po++]) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>获取两个字符串的字符数组和对应的比较字符的起始位置，判断2；</p>
</li>
<li><p>如果两个起始位置不合法，返回false，否则判断3；</p>
</li>
<li><p>循环判断两个字符串对应的字符是否相等，不相等返回false，循环结束返回true，循环次数为比较长度。</p>
</li>
</ol>
<ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">regionMatches</span><span class="params">(<span class="keyword">boolean</span> ignoreCase, <span class="keyword">int</span> toffset, String other, <span class="keyword">int</span> ooffset, <span class="keyword">int</span> len)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">char</span> ta[] = value;</span><br><span class="line">    <span class="keyword">int</span> to = toffset;</span><br><span class="line">    <span class="keyword">char</span> pa[] = other.value;</span><br><span class="line">    <span class="keyword">int</span> po = ooffset;</span><br><span class="line">    <span class="keyword">if</span> ((ooffset &lt; <span class="number">0</span>) || (toffset &lt; <span class="number">0</span>)</span><br><span class="line">            || (toffset &gt; (<span class="keyword">long</span>)value.length - len)</span><br><span class="line">            || (ooffset &gt; (<span class="keyword">long</span>)other.value.length - len)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (len-- &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">char</span> c1 = ta[to++];</span><br><span class="line">        <span class="keyword">char</span> c2 = pa[po++];</span><br><span class="line">        <span class="keyword">if</span> (c1 == c2) &#123;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (ignoreCase) &#123;</span><br><span class="line">            <span class="keyword">char</span> u1 = Character.toUpperCase(c1);</span><br><span class="line">            <span class="keyword">char</span> u2 = Character.toUpperCase(c2);</span><br><span class="line">            <span class="keyword">if</span> (u1 == u2) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (Character.toLowerCase(u1) == Character.toLowerCase(u2)) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>获取两个字符串的字符数组和对应的比较字符的起始位置，判断2；</p>
</li>
<li><p>如果两个起始位置不合法，返回false，否则判断3；</p>
</li>
<li><p>循环判断每个字符是否相等，循环次数为比较长度，循环结束返回true：</p>
<ul>
<li><p>循环中获取待比较的两个字符；</p>
</li>
<li><p>判断两个字符是否相等，相等结束本次循环开始下次循环，不相等进行下一步判断；</p>
</li>
<li><p>判断是否忽略大小写进行比较，否返回false，是将两个字符都转化成大写进行比较，相等结束本次循环，开始下一此循环，否将两个字符转化为小写进行比较，相等结束本次循环进行下一次循环，否返回false。</p>
</li>
</ul>
</li>
</ol>
<h4 id="字符串截取"><a href="#字符串截取" class="headerlink" title="字符串截取"></a>字符串截取</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex)</span></span>;</span><br><span class="line"><span class="function">String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex, <span class="keyword">int</span> endIndex)</span></span></span><br></pre></td></tr></table></figure>
<ul>
<li><p>int beginIndex: 截取起始位置</p>
</li>
<li><p>int endIndex: 截取结束为止</p>
</li>
</ul>
<h5 id="用法-4"><a href="#用法-4" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    String str = <span class="string">"I love ferry"</span>;</span><br><span class="line">    System.out.println(str.substring(<span class="number">2</span>));</span><br><span class="line">    System.out.println(str.substring(<span class="number">2</span>,<span class="number">6</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-substring.png" alt="运行结果"></p>
<p>&emsp;&emsp;截取字符串的起始位置和结束为止的包含关系：[beginIndex,endIndex)。如果起始位置数值上等于字符串长度，那么截取的就是一个空字符串。</p>
<h5 id="源码分析-3"><a href="#源码分析-3" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (beginIndex &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(beginIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> subLen = value.length - beginIndex;</span><br><span class="line">    <span class="keyword">if</span> (subLen &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(subLen);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (beginIndex == <span class="number">0</span>) ? <span class="keyword">this</span> : <span class="keyword">new</span> String(value, beginIndex, subLen);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>如果起始位置小于0，抛出下标越界异常，否则判断2；</p>
</li>
<li><p>如果截取的字符串长度小于0，抛出下标越界异常，否则判断3；</p>
</li>
<li><p>如果起始位置为0，返回调用方法的字符串，否则调用构造器<code>new String(char value[], int offset, int count)</code>生成字符串。</p>
</li>
</ol>
<ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">substring</span><span class="params">(<span class="keyword">int</span> beginIndex, <span class="keyword">int</span> endIndex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (beginIndex &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(beginIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (endIndex &gt; value.length) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(endIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> subLen = endIndex - beginIndex;</span><br><span class="line">    <span class="keyword">if</span> (subLen &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> StringIndexOutOfBoundsException(subLen);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ((beginIndex == <span class="number">0</span>) &amp;&amp; (endIndex == value.length)) ? <span class="keyword">this</span></span><br><span class="line">            : <span class="keyword">new</span> String(value, beginIndex, subLen);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>如果起始位置小于0或者结束位置大于字符串长度，抛出下标越界异常，否则判断2；</p>
</li>
<li><p>如果截取的字符串长度小于0，抛出下标越界异常，否则判断3；</p>
</li>
<li><p>如果起始位置为0，结束位置数值上等于字符串长度，则返回调用方法的字符串，否则调用构造器<code>new String(char value[], int offset, int count)</code>生成字符串。</p>
</li>
</ol>
<h4 id="字符-字符串-替换"><a href="#字符-字符串-替换" class="headerlink" title="字符(字符串)替换"></a>字符(字符串)替换</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">String <span class="title">replace</span><span class="params">(<span class="keyword">char</span> oldChar, <span class="keyword">char</span> newChar)</span></span>;</span><br><span class="line"><span class="function">String <span class="title">replace</span><span class="params">(CharSequence target, CharSequence replacement)</span></span>;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>char oldChar: 待替换的字符</p>
</li>
<li><p>char newChar: 替换字符</p>
</li>
<li><p>CharSequence target: 待替换的字符序列</p>
</li>
<li><p>CharSequence replacement: 替换字符序列</p>
</li>
</ul>
<h5 id="用法-5"><a href="#用法-5" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">replaceTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">"I love you"</span>;</span><br><span class="line">    System.out.println(str.replace(<span class="string">' '</span>, <span class="string">'-'</span>));</span><br><span class="line">    System.out.println(str.replace(<span class="string">"love"</span>, <span class="string">"miss"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-replace-char.png" alt="运行结果"></p>
<p>&emsp;&emsp;字符串替换方法中,可以传递字符,字符串,该方法将所有值为oldChar/target的字符/字符串替换为newChar/replacement.</p>
<h5 id="源码分析-4"><a href="#源码分析-4" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">replace</span><span class="params">(<span class="keyword">char</span> oldChar, <span class="keyword">char</span> newChar)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (oldChar != newChar) &#123;</span><br><span class="line">        <span class="keyword">int</span> len = value.length;</span><br><span class="line">        <span class="keyword">int</span> i = -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">char</span>[] val = value; <span class="comment">/* avoid getfield opcode */</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (++i &lt; len) &#123;</span><br><span class="line">            <span class="keyword">if</span> (val[i] == oldChar) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (i &lt; len) &#123;</span><br><span class="line">            <span class="keyword">char</span> buf[] = <span class="keyword">new</span> <span class="keyword">char</span>[len];</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">                buf[j] = val[j];</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">while</span> (i &lt; len) &#123;</span><br><span class="line">                <span class="keyword">char</span> c = val[i];</span><br><span class="line">                buf[i] = (c == oldChar) ? newChar : c;</span><br><span class="line">                i++;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> String(buf, <span class="keyword">true</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>如果待替换字符和替换字符不相等返回调用方法的字符串,否则判断2;</p>
</li>
<li><p>对字符数组进行循环,如果待替换字符与字符串的字符数组中任意一个都不相等则返回调用方法的字符串,否则得到第一个相等的字符下标,判断3;</p>
</li>
<li><p>循环将第一个相等的字符前面的字符复制到新的字符数组中,判断4;</p>
</li>
<li><p>从第一个相等的字符位置开始循环将后面的字符数组复制到新的字符数组中若待复制字符等于oldChar则替换成newChar,使用构造器返回替换完成的字符串.</p>
</li>
</ol>
<p>&emsp;&emsp;实际上整个流程就是如果字符串中没有oldChar,那么返回本字符串,如果有,就先复制第一个字符等于oldChar之前的字符数组,然后剩下的字符数组在循环中替换字符,然后通过构造器将字符数组转换成字符串返回.</p>
<ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">replace</span><span class="params">(CharSequence target, CharSequence replacement)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Pattern.compile(target.toString(), Pattern.LITERAL).matcher(</span><br><span class="line">            <span class="keyword">this</span>).replaceAll(Matcher.quoteReplacement(replacement.toString()));</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;权衡利弊之后,不能解析了…时间紧张…</p>
<h4 id="字符串移除前后空格"><a href="#字符串移除前后空格" class="headerlink" title="字符串移除前后空格"></a>字符串移除前后空格</h4><ul>
<li>返回值类型+方法签名</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">String <span class="title">trim</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<h5 id="用法-6"><a href="#用法-6" class="headerlink" title="用法"></a>用法</h5><ul>
<li>使用案例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">trimTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    String str = <span class="string">" I love ferry "</span>;</span><br><span class="line">    System.out.println(<span class="string">"before length:"</span>+str.length());</span><br><span class="line">    System.out.println(str.trim());</span><br><span class="line">    System.out.println(<span class="string">"after length:"</span>+str.trim().length());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/String/String-trim.png" alt="运行结果"></p>
<p>&emsp;&emsp;<code>trim()</code>方法可以将字符串的前导空格、尾随空格和行终止符移除。</p>
<h5 id="源码分析-5"><a href="#源码分析-5" class="headerlink" title="源码分析"></a>源码分析</h5><ul>
<li>源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">trim</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = value.length;</span><br><span class="line">    <span class="keyword">int</span> st = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">char</span>[] val = value;    <span class="comment">/* avoid getfield opcode */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> ((st &lt; len) &amp;&amp; (val[st] &lt;= <span class="string">' '</span>)) &#123;</span><br><span class="line">        st++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> ((st &lt; len) &amp;&amp; (val[len - <span class="number">1</span>] &lt;= <span class="string">' '</span>)) &#123;</span><br><span class="line">        len--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ((st &gt; <span class="number">0</span>) || (len &lt; value.length)) ? substring(st, len) : <span class="keyword">this</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自上而下：</p>
<ol>
<li><p>循环判断字符串的前面的字符是不是空格,是<code>st</code>加一;</p>
</li>
<li><p>循环判断字符串的后面的字符是不是空格,是<code>len</code>减一;</p>
</li>
<li><p>如果字符串没有前导空格或尾随空格或者行终止符则返回调用方法的字符串,否则返回截取后的字符串;</p>
</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java字符串创建</tag>
        <tag>java字符串常用方法</tag>
      </tags>
  </entry>
  <entry>
    <title>注解</title>
    <url>/annotation.html</url>
    <content><![CDATA[<p>&emsp;&emsp;Java注解又称Java标注，是Java语言5.0版本开始支持加入源代码的特殊语法元数据。Java语言中的类、方法、变量、参数和包等都可以被标注。和Javadoc不同，Java标注可以通过反射获取标注内容。在编译器生成类文件时，标注可以被嵌入到字节码中。Java虚拟机可以保留标注内容，在运行时可以获取到标注内容。 当然它也支持自定义Java标注。</p>
<a id="more"></a>
<h3 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h3><ul>
<li>@Test注解定义</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用<code>@interface</code>来声明Test是一个注解,和声明类,接口的方法是一样的,请记住,注解是不支持继承的.</p>
<ul>
<li>使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Ferry</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DemoMain</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Ferry</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> String attribute;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Ferry</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> String <span class="title">annotation</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"haha"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(attribute);</span><br><span class="line">        System.out.println(DemoMain.annotation());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/annotation/annotation-ferry-1.png" alt="运行结果"></p>
<p>&emsp;&emsp;自定义的这个注解已经可以使用了,这个没有任何限的注解可以使用在类,私有属性以及方法上,当然,因为这个注解并没有实现任何功能,所以也毫无意义.</p>
<p>&emsp;&emsp;当我们想要对自定义注解做一些限制时,例如限制注解的使用范围,限制注解生命周期,可以使用java提供的元注解.</p>
<h3 id="元注解"><a href="#元注解" class="headerlink" title="元注解"></a>元注解</h3><p>&emsp;&emsp;元注解就是标记其他注解的注解;</p>
<ul>
<li><code>@Target</code>注解:限制注解的使用范围</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.METHOD)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>ElementType</code>枚举类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> ElementType &#123;</span><br><span class="line">    <span class="comment">/**标明该注解可以用于类、接口（包括注解类型）或enum声明*/</span></span><br><span class="line">    TYPE,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明该注解可以用于字段(域)声明，包括enum实例 */</span></span><br><span class="line">    FIELD,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明该注解可以用于方法声明 */</span></span><br><span class="line">    METHOD,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明该注解可以用于参数声明 */</span></span><br><span class="line">    PARAMETER,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明注解可以用于构造函数声明 */</span></span><br><span class="line">    CONSTRUCTOR,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明注解可以用于局部变量声明 */</span></span><br><span class="line">    LOCAL_VARIABLE,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明注解可以用于注解声明(应用于另一个注解上)*/</span></span><br><span class="line">    ANNOTATION_TYPE,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/** 标明注解可以用于包声明 */</span></span><br><span class="line">    PACKAGE,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 标明注解可以用于类型参数声明（1.8新加入）</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@since</span> 1.8</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    TYPE_PARAMETER,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 类型使用声明（1.8新加入)</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@since</span> 1.8</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    TYPE_USE</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;可以使用<code>@Target(value = {ElementType.METHOD,ElementType.FIELD})</code>方式来指定多个值.</p>
<ul>
<li><code>@Retention</code>注解:约束注解的生命周期</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.FIELD)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>RetentionPolicy</code>枚举类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> RetentionPolicy &#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 源码级别</span></span><br><span class="line"><span class="comment">     * 注解将被编译器丢弃（该类型的注解信息只会保留在源码里，源码经过编译后，注解信息会被丢弃，不会保留在编译好的class文件里）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    SOURCE,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 类文件级别</span></span><br><span class="line"><span class="comment">     * 注解在class文件中可用，但会被JVM丢弃（该类型的注解信息会保留在源码里和class文件里，在执行的时候，不会加载到虚拟机中），请注意，当注解未定义Retention值时，默认值是CLASS，如Java内置注解，<span class="doctag">@Override</span>、<span class="doctag">@Deprecated</span>、<span class="doctag">@SuppressWarnning</span>等</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    CLASS,</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 运行时级别</span></span><br><span class="line"><span class="comment">     * 注解信息将在运行期(JVM)也保留，因此可以通过反射机制读取注解的信息（源码、class文件和执行的时候都有注解的信息），如SpringMvc中的<span class="doctag">@Controller</span>、<span class="doctag">@Autowired</span>、<span class="doctag">@RequestMapping</span>等。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    RUNTIME</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>@Documented</code>注解:被修饰的注解会生成到javadoc中</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>@Inherited</code>注解:标注的注解可以被 使用这个注解的类的子类继承</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Inherited</span></span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@interface</span> X &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@interface</span> Y &#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@X</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Y</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">D</span> <span class="keyword">extends</span> <span class="title">C</span> </span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">skpcfsojv</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"使用了@Inherited注解: "</span>+B<span class="class">.<span class="keyword">class</span>.<span class="title">isAnnotationPresent</span>(<span class="title">X</span>.<span class="title">class</span>))</span>;</span><br><span class="line">    System.out.println(<span class="string">"未使用@Inherited注解: "</span>+D<span class="class">.<span class="keyword">class</span>.<span class="title">isAnnotationPresent</span>(<span class="title">Y</span>.<span class="title">class</span>))</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/annotation/annotation-Inherited.png" alt="运行结果"></p>
<p>&emsp;&emsp;换句话说,就是子类可以继承父类上的使用<code>@Inherited</code>标注的注解.通过上面的例子而言,就是注解X可以被使用了X注解的类A的子类B继承.</p>
<ul>
<li><code>@Repeatable</code>注解:使标注的注解可以在相同位置重复使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@interface</span> Paths&#123;</span><br><span class="line">    Path[] value();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Repeatable</span>(Paths<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">interface</span> <span class="title">Path</span></span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">value</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Path</span>(<span class="string">"ferry/downloads"</span>)</span><br><span class="line"><span class="meta">@Path</span>(<span class="string">"ferry/music"</span>)</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">AA</span></span>&#123;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dsbucn</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Path[] ps = AA<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotationsByType</span>(<span class="title">Path</span>.<span class="title">class</span>)</span>;</span><br><span class="line">    <span class="keyword">for</span> (Path p : ps) &#123;</span><br><span class="line">        System.out.println(p.value());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/annotation/annotation-repeatable.png" alt="运行结果"></p>
<p>&emsp;&emsp;在有<code>@Repeatable</code>注解之前,我们想实现这样的功能就只能在注解中定义数组元素来实现.<code>@Repeatable</code>注解的value元素赋值必须是一个注解的class类型,而这个注解中的value元素的数据类型必须是你定义的注解的数组类型,其实作用也就是和你直接定义一个数组来接受差不多.在获取注解中的元素的值时,针对这种被<code>@Repeatable</code>注解标注的注解而言,我们需要使用<code>getAnnotationsByType()</code>方法获取注解的数组类型然后循环获取值.</p>
<h3 id="注解元素"><a href="#注解元素" class="headerlink" title="注解元素"></a>注解元素</h3><p>&emsp;&emsp;当注解中不含有任何成员时,这个注解被称之为标记注解,如上的自定义注解就是一个标记注解,它的唯一目的就是标记声明.</p>
<ul>
<li>有成员的注解</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line">    <span class="function">String <span class="title">name</span><span class="params">()</span> <span class="keyword">default</span> "<span class="keyword">default</span>"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;注解中的元素的声明采用了方法的声明方式,后面使用default关键字来声明默认值,当然,默认值不是必须的,但是,没有设置默认值name在使用这个注解的时候这个成员就必须赋值,否则会报编译错误.</p>
<ul>
<li>使用注解并进行测试</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Ferry</span>(name = <span class="string">"love"</span>)</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span></span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Ferry</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Employee</span></span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">hjsvbj</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Ferry ferry = User<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">Ferry</span>.<span class="title">class</span>)</span>;</span><br><span class="line">    System.out.println(ferry.name());</span><br><span class="line">    Ferry f = Employee<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">Ferry</span>.<span class="title">class</span>)</span>;</span><br><span class="line">    System.out.println(f.name());</span><br><span class="line">    System.out.println(ferry.equals(f));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/annotation/annotation-ferry-2.png" alt="运行结果"></p>
<p>&emsp;&emsp;通过<code>Class</code>类的<code>getAnnotation()</code>方法获取到在这个类上使用的注解的实例,然后将注解的元素name输出.可以看到,一个输出的是指定的name值,一个输出的是默认值.显然,注解用在不同的地方会构建出不同的实例,另一个有力证据就是使用<code>equals()</code>方法判断输出false.</p>
<h3 id="注解元素的数据类型"><a href="#注解元素的数据类型" class="headerlink" title="注解元素的数据类型"></a>注解元素的数据类型</h3><ol>
<li><p>基本数据类型(byte,char,short,int,float,long,double)</p>
</li>
<li><p>String</p>
</li>
<li><p>Class</p>
</li>
<li><p>enum</p>
</li>
<li><p>Annotation</p>
</li>
<li><p>上述类型的数组</p>
</li>
</ol>
<p>&emsp;&emsp;需要注意的是,注解元素的数据类型不能使用包装类替代基本数据类型,凡是使用不合法的数据类型都会报编译错误.</p>
<ul>
<li>示例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.TYPE)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Ferry &#123;</span><br><span class="line">    <span class="function">String <span class="title">name</span><span class="params">()</span> <span class="keyword">default</span> ""</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">amount</span><span class="params">()</span> <span class="keyword">default</span> -1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">Class <span class="title">showClass</span><span class="params">()</span> <span class="keyword">default</span> Object.class</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">Sex <span class="title">showSex</span><span class="params">()</span> <span class="keyword">default</span> Sex.MALE</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function">Honey <span class="title">love</span><span class="params">()</span> <span class="keyword">default</span> @<span class="title">Honey</span><span class="params">(mySex = Sex.FEMALE)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span>[] amounts() <span class="keyword">default</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">enum</span> Sex &#123;</span><br><span class="line">        MALE,FEMALE;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@interface</span> Honey &#123;</span><br><span class="line">        <span class="function">Sex <span class="title">mySex</span><span class="params">()</span> <span class="keyword">default</span> Sex.FEMALE</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dscslkndv</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Ferry ferry = User<span class="class">.<span class="keyword">class</span>.<span class="title">getAnnotation</span>(<span class="title">Ferry</span>.<span class="title">class</span>)</span>;</span><br><span class="line">    System.out.println(ferry.toString());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/annotation/annotation-ferry-3.png" alt="运行结果"></p>
<ul>
<li>特殊的元素<code>value</code></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(ElementType.METHOD)</span><br><span class="line"><span class="meta">@interface</span> Look&#123;</span><br><span class="line">    <span class="function">String <span class="title">value</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Look</span>(<span class="string">"see"</span>)</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">cbsiflk</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Look look = <span class="keyword">this</span>.getClass().getMethod(<span class="string">"cbsiflk"</span>).getAnnotation(Look<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">        System.out.println(look);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (NoSuchMethodException e) &#123;</span><br><span class="line">        e.printStackTrace();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;当注解中有元素的名称为<code>value</code>时,在使用这个注解时为value元素赋值可以不使用key=value的形式而直接在括号中赋值,编译器会默认将该值赋予注解的value元素,当然,赋值的数据类型必须和注解中value元素的数据类型一致,否则会报编译错误.</p>
<h3 id="java内置注解"><a href="#java内置注解" class="headerlink" title="java内置注解"></a>java内置注解</h3><ul>
<li><code>@Override</code>:用以注明此方法重写了父类的方法.</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(ElementType.METHOD)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Override &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>@Deprecated</code>:用以注明此类或者方法已经过时,不推荐使用.</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Documented</span></span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.RUNTIME)</span><br><span class="line"><span class="meta">@Target</span>(value=&#123;CONSTRUCTOR, FIELD, LOCAL_VARIABLE, METHOD, PACKAGE, PARAMETER, TYPE&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> Deprecated &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><code>@SuppressWarnnings</code>:用以关闭编译器对类、方法、成员变量、变量初始化的警告.</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Target</span>(&#123;TYPE, FIELD, METHOD, PARAMETER, CONSTRUCTOR, LOCAL_VARIABLE&#125;)</span><br><span class="line"><span class="meta">@Retention</span>(RetentionPolicy.SOURCE)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> SuppressWarnings &#123;</span><br><span class="line">    String[] value();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;数组接收的值:</p>
<ol>
<li><p>deprecation：使用了不赞成使用的类或方法时的警告；</p>
</li>
<li><p>unchecked：执行了未检查的转换时的警告，例如当使用集合时没有用泛型 (Generics) 来指定集合保存的类型; </p>
</li>
<li><p>fallthrough：当 Switch 程序块直接通往下一种情况而没有 Break 时的警告;</p>
</li>
<li><p>path：在类路径、源文件路径等中有不存在的路径时的警告; </p>
</li>
<li><p>serial：当在可序列化的类上缺少 serialVersionUID 定义时的警告; </p>
</li>
<li><p>finally：任何 finally 子句不能正常完成时的警告; </p>
</li>
<li><p>all：关于以上所有情况的警告。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java注解</tag>
      </tags>
  </entry>
  <entry>
    <title>数组</title>
    <url>/array.html</url>
    <content><![CDATA[<p>&emsp;&emsp;数组是相同类型的、用一个标识符名称封装到一起的一个对象序列或基本类型数据序列。</p>
<a id="more"></a>
<h3 id="数组对象的定义"><a href="#数组对象的定义" class="headerlink" title="数组对象的定义"></a>数组对象的定义</h3><p>&emsp;&emsp;在java中,数组有两种声明方式</p>
<ul>
<li>声明数组</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] a; <span class="comment">// java推荐</span></span><br><span class="line"><span class="keyword">int</span> b[]; <span class="comment">// C语言形式,不推荐</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在java中,推荐使用第一种形式声明一个数组的引用.使用数据类型+中括号([])+空格+变量名来声明一种类型的数组形式.此时,java虚拟机会在栈空间开辟一块空间来存放我们声明的变量,也就是我们的引用的地址值.接下来,就需要初始化一个数组并使用我们声明的数组变量引用这个数组.</p>
<ul>
<li>初始化数组并引用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">a = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在java中,按照优先级,会先执行赋值符号右边的表达式,<code>new int[10]</code>是初始化一个长度为10的整型数组,这步操作将在堆空间开辟一块相应大小的内存,其中,数组的每一个元素都赋予默认值,int型的默认值为0,其他类型的默认值可以自行创建对应的数组类型并初始化然后debug查看.第二步执行赋值操作,将初始化的数组的第一个元素的地址的值赋值给栈空间的数组变量,所以,栈空间中存放的是堆空间开辟的内存的对应元素的地址的值.</p>
<p>&emsp;&emsp;初始化有三种方式,第一种方式如上,创建一个具有默认值的数组,使用这种方式要求必须在中括号中指定数组的大小.接下来介绍另外两种.</p>
<ul>
<li>数组初始化方式二</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">b = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种方式在初始化的时候就赋值,数组的长度即为初始化时大括号中元素的个数.注意!指定数组长度和赋默认值不能同时进行,也就是说使用方式二不能指定数组长度.</p>
<ul>
<li>数组初始化方式三</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span>[] c = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种方式是第二种方式的一种简写形式,数组的长度为初始化元素的个数.但是这种形式有一个问题,不能先声明再初始化,必须用一个表达式完成声明和初始化的工作.</p>
<ul>
<li>以上方式声明并初始化数组的debug效果</li>
</ul>
<p><img src="/array/array-init.png" alt="运行结果"></p>
<p>&emsp;&emsp;当然,我们平时定义数组为了安全考虑声明和初始化是同时进行的,例如<code>int[] a = new int[10];</code>,防止在声明数组变量但未初始化的时候就使用该变量从而引发空指针异常.</p>
<p>&emsp;&emsp;这里说几点:</p>
<ol>
<li><p><code>int[] a = new int[0];</code>和<code>int[] b = new int[]{};</code>和<code>int[] c = {};</code>都是声明一个长度为0的数组,也称之为空数组.注意:空数组不意味着它是null,空数组有点类似字符串的空字符串,在实际应用中,在方法中需要返回数组元素,但是经过操作并不能得到想要的数组元素,但是总不能返回null值吧,所以我们可以返回一个空数组.</p>
</li>
<li><p><code>new int[]{1,2,3};</code>是一个匿名数组,因为没有任何一个变量指向这个数组,它的性质和匿名对象一样,离开当前代码行,就没办法再使用这个匿名数组了,因为再也找不到它了.</p>
</li>
<li><p>数组是一个类,实例化一个数组就是创建了一个数组的对象!!!</p>
</li>
</ol>
<h3 id="数组使用"><a href="#数组使用" class="headerlink" title="数组使用"></a>数组使用</h3><p>&emsp;&emsp;通过<code>变量名[下标]</code>获取数组中对应位置的元素,下标从0开始.</p>
<p>&emsp;&emsp;使用数组时可以通过<code>a[4] = 10;</code>形式实现对数组中的元素的操作,当然,这样做是有很大的风险的,当下标不合法时会抛出数组下标越界异常.</p>
<ul>
<li>数组复制</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fdshfiue</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] a = &#123;<span class="number">43</span>,<span class="number">4454</span>,<span class="number">23</span>,<span class="number">1225</span>,<span class="number">3</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span>[] b = a;</span><br><span class="line">    System.out.println(Arrays.toString(a));</span><br><span class="line">    System.out.println(Arrays.toString(b));</span><br><span class="line">    b[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">    System.out.println(Arrays.toString(a));</span><br><span class="line">    System.out.println(Arrays.toString(b));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-copy-error.png" alt="迎新结果"></p>
<p>&emsp;&emsp;前面说过,数组是一个对象,所以将一个数组使用赋值符号直接赋值给另一个变量名,那么实际上也就是将该数组对象的地址值赋值给该变量,所以这两个变量引用的是同一个数组对象,无论哪个变量对数组对象中的元素进行了修改,本质上都是对同一个对象进行修改.所以如果我们想要对数组进行复制并再复制体上进行操作可以使用下面要说的复制方法.</p>
<h4 id="数组长度"><a href="#数组长度" class="headerlink" title="数组长度"></a>数组长度</h4><ul>
<li>获取数组的长度</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bcsjh</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] a = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> length = a.length;</span><br><span class="line">    System.out.println(length);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>length</code>是数组类的成员变量,所以可以直接使用变量名.length的方式获取数组的长度.</p>
<h4 id="数组的循环"><a href="#数组的循环" class="headerlink" title="数组的循环"></a>数组的循环</h4><p>&emsp;&emsp;当我们需要对数组中的每一个元素都进行统一操作的时候,就需要使用循环来完成了.</p>
<p>&emsp;&emsp;数组的循环有两种形式,一种是使用普通的循环方式,另一种是使用增强for循环.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bcsjh</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] a = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> length = a.length;</span><br><span class="line">    System.out.println(<span class="string">"数组的长度: "</span> + length);</span><br><span class="line">    <span class="comment">// 一般形式的循环</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; length; i++) &#123;</span><br><span class="line">        System.out.println(<span class="string">"数组中第 "</span> + i + <span class="string">" 个元素: "</span> + a[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 增强for循环</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> value: a)&#123;</span><br><span class="line">        ++value;</span><br><span class="line">        System.out.println(value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-for.png" alt="运行结果"></p>
<p>&emsp;&emsp;一般形式的循环有三种,<code>do{...}while(boolean)</code>, <code>while(boolean){...}</code> 和 <code>for(初始化;判断;操作){...}</code>,当我们需要在循环中使用数组的下标时那么我们用for循环肯定更加方便一些,而不需要使用下标则使用增强for循环更加简洁.另外两种循环在数组应用上稍显复杂,这里不推荐.</p>
<h4 id="数组的输出"><a href="#数组的输出" class="headerlink" title="数组的输出"></a>数组的输出</h4><ul>
<li>数组输出的几种形式</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dbscjdsn</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] a = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    System.out.println(a.toString());</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i : a) &#123;</span><br><span class="line">        System.out.println(i);</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(Arrays.toString(a));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-toString.png" alt="运行结果"></p>
<p>&emsp;&emsp;数组是一个类,而数组类并没有重写Object类的toString()方法,所以会直接输出类的信息,而数组类是JVM创建的所以类名才会如此奇怪.我们也可以选择使用循环进行每一个数组元素的输出,或者使用Arrays类的toString()方法进行数组的输出.</p>
<h4 id="数组的比较"><a href="#数组的比较" class="headerlink" title="数组的比较"></a>数组的比较</h4><ul>
<li>数组比较的几种方式</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">User</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fsjbdjvcb</span><span class="params">()</span></span>&#123;</span><br><span class="line">    User[] users = &#123;<span class="keyword">new</span> User(<span class="string">"ferry"</span>),<span class="keyword">new</span> User(<span class="string">"honey"</span>)&#125;;</span><br><span class="line">    User ferry = <span class="keyword">new</span> User(<span class="string">"ferry"</span>);</span><br><span class="line">    User honey = <span class="keyword">new</span> User(<span class="string">"honey"</span>);</span><br><span class="line">    User[] us = <span class="keyword">new</span> User[]&#123;ferry,honey&#125;;</span><br><span class="line">    System.out.println(users == us);</span><br><span class="line">    System.out.println(us.equals(users));</span><br><span class="line">    System.out.println(Arrays.equals(us, users));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-equals.png" alt="运行结果"></p>
<p>&emsp;&emsp;使用<code>==</code>进行比较就是比较两个对象的地址的值,显而易见,两个不同的对象地址值自然不相同;使用<code>对象.equals(对象)</code>的方式进行比较,由于枚举类没有重写Object对象的equals()方法,所以直接调用的Object类的equals()方法,实际上也就是使用<code>==</code>比较两者的地址的值.使用Arrays类的equals方法进行比较将进行数组对象中每一个元素的值进行比较,所以这种比较方式才是我们正常发开时所需要的方式.</p>
<p>&emsp;&emsp;到这里,已经使用了Arrays类的两个方法,toString()和equals()方法都非常的实用.那么,Arrays类中还有什么方法可以让我们更加高效的处理数组呢?</p>
<h3 id="Arrays类"><a href="#Arrays类" class="headerlink" title="Arrays类"></a>Arrays类</h3><p>&emsp;&emsp;Arrays类是数组的操作类，定义在java.util包中，主要的功能可以实现数组元素的查找，数组内容的填充、排序等。该类中的绝大多数方法都是静态的.通过<code>Arrays.方法名</code>形式调用.</p>
<h4 id="toString-方法"><a href="#toString-方法" class="headerlink" title="toString()方法"></a><code>toString()</code>方法</h4><ul>
<li>返回数组的字符串描述,返回字符串:此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">System.out.println(Arrays.toString(<span class="keyword">new</span> <span class="keyword">long</span>[]&#123;<span class="number">1L</span>,<span class="number">10L</span>&#125;));</span><br><span class="line"><span class="comment">// 运行结果: [1, 10]</span></span><br></pre></td></tr></table></figure>
<h4 id="equals-方法"><a href="#equals-方法" class="headerlink" title="equals()方法"></a><code>equals()</code>方法</h4><ul>
<li>比较两个数组对象是否相等,返回布尔值:此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">System.out.println(Arrays.equals(<span class="keyword">new</span> String[]&#123;<span class="string">"ferry"</span>,<span class="string">"honey"</span>&#125;,<span class="keyword">new</span> String[]&#123;<span class="string">"ferry"</span>,<span class="string">"honey"</span>&#125;));</span><br><span class="line"><span class="comment">// 运行结果: true</span></span><br></pre></td></tr></table></figure>
<h4 id="sort-方法和parallelSort-方法"><a href="#sort-方法和parallelSort-方法" class="headerlink" title="sort()方法和parallelSort()方法"></a><code>sort()</code>方法和<code>parallelSort()</code>方法</h4><ul>
<li>数组排序,无返回值: 此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">User</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">User</span>&gt;</span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">User</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(User user)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> StringUtils.compare(name,user.getName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">cbsj</span><span class="params">()</span></span>&#123;</span><br><span class="line">    User[] users = <span class="keyword">new</span> User[]&#123;<span class="keyword">new</span> User(<span class="string">"ferry"</span>),<span class="keyword">new</span> User(<span class="string">"joyce"</span>),<span class="keyword">new</span> User(<span class="string">"honey"</span>)&#125;;</span><br><span class="line">    Arrays.sort(users);</span><br><span class="line">    System.out.println(Arrays.toString(users));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-sort.png" alt="运行结果"></p>
<p>&emsp;&emsp;如果使用java类数组进行排序,那么要求这个类必须实现了<code>Comparable</code>接口实现其方法<code>compareTo()</code>,如果没有实现这个方法那么运行时会抛出异常.</p>
<p>&emsp;&emsp;sort()方法默认使用升序排序,即从小到大顺序,其中,可使用重载的方法<code>void sort(Object[] a, int fromIndex, int toIndex)</code>选择数组的一个区间进行排序.</p>
<p>&emsp;&emsp;java8以后如果处理的数据量过大,建议使用parallelSort()方法,该方法实现功能和sort方法一样,但是实现方式不同,处理起大数据parallelSort()方法更为高效</p>
<h4 id="fill-方法"><a href="#fill-方法" class="headerlink" title="fill()方法"></a><code>fill()</code>方法</h4><ul>
<li>数组填充,无返回值: 此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">asdia</span><span class="params">()</span></span>&#123;</span><br><span class="line">    User[] users  = <span class="keyword">new</span> User[<span class="number">3</span>];</span><br><span class="line">    User user = <span class="keyword">new</span> User(<span class="string">"ferry"</span>);</span><br><span class="line">    Arrays.fill(users,user);</span><br><span class="line">    System.out.println(Arrays.toString(users));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 运行结果: [ArrayTest.User(name=ferry), ArrayTest.User(name=ferry), ArrayTest.User(name=ferry)]</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;该方法将指定变量填充到数组的每个元素上.Arrays类重载了三参的方法,可以选择数组一个区间进行填充元素.</p>
<h4 id="copyOf-方法"><a href="#copyOf-方法" class="headerlink" title="copyOf()方法"></a><code>copyOf()</code>方法</h4><ul>
<li>复制数组，截取或用默认值填充(如有必要),以使副本具有指定的长度,返回复制后的数组: 此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sajhbc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    User[] users = &#123;<span class="keyword">new</span> User(<span class="string">"ferry"</span>),<span class="keyword">new</span> User(<span class="string">"joyce"</span>),<span class="keyword">new</span> User(<span class="string">"honey"</span>)&#125;;</span><br><span class="line">    User[] copyUsers = Arrays.copyOf(users,<span class="number">5</span>);</span><br><span class="line">    System.out.println(Arrays.toString(copyUsers));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 运行结果: [ArrayTest.User(name=ferry), ArrayTest.User(name=joyce), ArrayTest.User(name=honey), null, null]</span></span><br></pre></td></tr></table></figure>
<h4 id="copyOfRange-方法"><a href="#copyOfRange-方法" class="headerlink" title="copyOfRange()方法"></a><code>copyOfRange()</code>方法</h4><ul>
<li>将指定数组的指定范围复制到一个新数组,返回复制后的数组: 此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sajhbc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    User[] users = &#123;<span class="keyword">new</span> User(<span class="string">"ferry"</span>),<span class="keyword">new</span> User(<span class="string">"joyce"</span>),<span class="keyword">new</span> User(<span class="string">"honey"</span>)&#125;;</span><br><span class="line">    User[] copyUsers = Arrays.copyOfRange(users,<span class="number">1</span>,<span class="number">5</span>);</span><br><span class="line">    System.out.println(Arrays.toString(copyUsers));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 运行结果: [ArrayTest.User(name=joyce), ArrayTest.User(name=honey), null, null]</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>&lt;T&gt; T[] copyOfRange(T[] original, int from, int to)</code>,如果from小于0会抛出数组下标越界异常,如果to大于数组长度,则使用默认值进行新数组的填充,需要注意,to是不包含的.</p>
<h4 id="binarySearch-方法"><a href="#binarySearch-方法" class="headerlink" title="binarySearch()方法"></a><code>binarySearch()</code>方法</h4><ul>
<li>使用二分搜索法来搜索指定的数组,如果key在数组中，则返回搜索值的索引；否则返回-1或“-”（插入点）: 此方法被重载多次,支持多种数据类型</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">cskbc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] is = &#123;<span class="number">2</span>,<span class="number">6</span>,<span class="number">2</span>,<span class="number">6</span>,<span class="number">5</span>,<span class="number">21</span>,<span class="number">76</span>,<span class="number">4</span>&#125;;</span><br><span class="line">    Arrays.sort(is);</span><br><span class="line">    System.out.println(Arrays.toString(is));</span><br><span class="line">    System.out.println(Arrays.binarySearch(is, -<span class="number">4</span>));</span><br><span class="line">    System.out.println(Arrays.binarySearch(is, <span class="number">4</span>));</span><br><span class="line">    System.out.println(Arrays.binarySearch(is, <span class="number">8</span>));</span><br><span class="line">    System.out.println(Arrays.binarySearch(is, <span class="number">100</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-binarySearch.png" alt="运行结果"></p>
<p>&emsp;&emsp;使用<code>binarySearch()</code>方法的数组应该是已经排好序的数组.此方法有重载形式可以在数组指定范围进行所搜.</p>
<blockquote>
<p>当待搜索的值在数组中时返回搜索值出现的第一个的下标,<br>当待搜索的值不在数组中且值小于数组中最小的元素的值时返回-1,<br>当待搜索的值在不在数组中且值大于数组元素最小的值小于数组元素最大的值时返回<strong>按顺序待插入的下标-1 的相反数</strong>;<br>当待搜索的值在不在数组中且值大于数组元素最大的值时返回数组长度+1;</p>
</blockquote>
<h3 id="多维数组"><a href="#多维数组" class="headerlink" title="多维数组"></a>多维数组</h3><p>&emsp;&emsp;多维数组是数组的数组。其实java只有一维数组，但是由于数组可以存放任意类型的数据，当然也就可以存放数组了，这个时候，就可以模拟多维数组了。</p>
<ul>
<li>定义多维数组</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dsabcjh</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[][] a = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">4</span>][<span class="number">4</span>];</span><br><span class="line">    <span class="keyword">int</span> b[][] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">4</span>][];</span><br><span class="line">    <span class="keyword">int</span>[][][] c = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>][<span class="number">9</span>][<span class="number">5</span>];</span><br><span class="line">    <span class="keyword">int</span>[][][] d = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>][<span class="number">9</span>][];</span><br><span class="line">    <span class="keyword">int</span> e[][][] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">3</span>][][];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;多维数组的定义和一维数组的定义类似,我们推荐奖[]放在数据类型前面变量名后面的这种形式.在初始化的时候在指定后面纬度长度的前提是必须前面纬度的长度指定了,例如二维数组,不能指定二维长度而不指定一维长度.将数组举个例子.普通的数据类型就是一个点,一维数组就是一条线,二维数组就是一个以X轴和Y轴组成的面,三维数组就是以X轴,Y轴和Z轴组成的空间,至于更高维度,四维我们是不是可以想象成一个空间贯穿着一条时间轴构成空间随着时间的流逝产生的演变,五维就是一个空间比如地球随着时间流逝,发生在这片空间的历史…咳咳,扯远了…</p>
<ul>
<li>多维数组方法调用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dbajsbkc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[][] a = &#123;&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">7</span>&#125;,&#123;<span class="number">3</span>,<span class="number">6</span>,<span class="number">3</span>,<span class="number">1</span>,<span class="number">7</span>,<span class="number">9</span>,<span class="number">3</span>,<span class="number">43</span>&#125;&#125;;</span><br><span class="line">    System.out.println(Arrays.toString(a));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-more-tostring.png" alt="运行结果"></p>
<p>&emsp;&emsp;显然,Arrays类对于数组的支持只是基于一维的,所以多维数组想要实现像一维数组那样的操作就需要使用循环了.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dbajsbkcsd</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[][] a = &#123;&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">7</span>&#125;,&#123;<span class="number">3</span>,<span class="number">6</span>,<span class="number">3</span>,<span class="number">1</span>,<span class="number">7</span>,<span class="number">9</span>,<span class="number">3</span>,<span class="number">43</span>&#125;&#125;;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span>[] ints : a) &#123;</span><br><span class="line">        System.out.println(Arrays.toString(ints));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-more-tostring-2.png" alt="运行结果"></p>
<p>&emsp;&emsp;所以,Arrays类方法的支持想要对多维数组操作就需要使用循环降维.</p>
<h3 id="数组类探索"><a href="#数组类探索" class="headerlink" title="数组类探索"></a>数组类探索</h3><p>&emsp;&emsp;数组是一个类,数组类是通过数据类型和纬度来确定具体是哪一个类的,不同的数据类型和纬度的数组所属的类是不一样的.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sfjk</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span>[] arr = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br><span class="line">    <span class="keyword">double</span>[][] arr1 = <span class="keyword">new</span> <span class="keyword">double</span>[<span class="number">3</span>][<span class="number">4</span>];</span><br><span class="line">    String[][][] arr2 = <span class="keyword">new</span> String[<span class="number">7</span>][<span class="number">5</span>][<span class="number">6</span>];</span><br><span class="line">    System.out.println(arr.toString());</span><br><span class="line">    System.out.println(arr1.toString());</span><br><span class="line">    System.out.println(arr2.toString());</span><br><span class="line">    System.out.println(arr.getClass().getSuperclass().getName());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Object类toString实现</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> getClass().getName() + <span class="string">"@"</span> + Integer.toHexString(hashCode());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-class.png" alt="运行结果"></p>
<p>&emsp;&emsp;从结果可以推断出以下几点.</p>
<blockquote>
<p>数组类是Object类的直接子类;<br>不同纬度的不同数据类型的数组对象属于不同的数组类;<br>数组类应该是有JVM生成的.</p>
</blockquote>
<p>&emsp;&emsp;第一点好推断,此处不说,第二点:数组对象调用Object类的toString()方法拼接的是对象所属类的类名加上处理过后的哈希值,而上述例子使用了不同纬度的不同数据类型的数组对象打印出来的类名并不一致.再稍加分析推断,可以看到,’[‘的数量代表着数组的纬度,后面的’I,’D’等代表着数据类型.而这样的奇怪的非法的类名正常情况下是不存在的,能做到这一点的只有JVM了,所以得到第三点,数组类是由JVM在运行时生成的.</p>
<h4 id="协变的数组"><a href="#协变的数组" class="headerlink" title="协变的数组"></a>协变的数组</h4><p>&emsp;&emsp;数组的协变性是指：父类的数组引用可以指向子类的数组对象.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Base</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Sub</span> <span class="keyword">extends</span> <span class="title">Base</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Oth</span> <span class="keyword">extends</span> <span class="title">Base</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String hobby;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ssss</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Base[] arr = <span class="keyword">new</span> Sub[<span class="number">5</span>];</span><br><span class="line">    arr[<span class="number">0</span>] = <span class="keyword">new</span> Oth();</span><br><span class="line">    System.out.println(arr[<span class="number">0</span>].toString());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;Sub类Base类的子类,所以,Base类的数组引用可以指向Sub类的数组对象,但是,当你将引用中的某个元素指向该父类的其它子类时,编译器并不会报错,但是运行时会报错<code>java.lang.ArrayStoreException</code>,其实这一点就没有泛型好,泛型可以严格的检测类型是否一致,而且现在泛型的通配符的使用也实现了协变的功能.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sssss</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Base[] arr = <span class="keyword">new</span> Sub[<span class="number">5</span>];</span><br><span class="line">    System.out.println(arr.getClass());</span><br><span class="line">    System.out.println(arr.getClass().getSuperclass());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/array/array-extend.png" alt="运行结果"></p>
<p>&emsp;&emsp;说明虽然引用是父类数组的引用,但是实际的类型还是子类对象的数组类.第二行输出更是说明所有的数组类的直接父类是Object类.</p>
<p>&emsp;&emsp;在本文的最后,友情提示一下,数组是不支持泛型的,究其原因,是因为数组在创建的时候就要知道它的数据类型,并且在对数组元素进行操作的时候都会检查类型,而泛型是用擦除实现的,在运行时泛型的类型会被擦除.你看,一个要求我必须知道元素类型,一个会把类型干掉,这不是打架了么,所以这两者是不能共存滴!</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>java数组</tag>
        <tag>Arrays类</tag>
      </tags>
  </entry>
  <entry>
    <title>java集合</title>
    <url>/collections.html</url>
    <content><![CDATA[<p>&emsp;&emsp;java集合的学习记录…</p>
<a id="more"></a>
<h3 id="ArrayList"><a href="#ArrayList" class="headerlink" title="ArrayList"></a>ArrayList</h3><p>&emsp;&emsp;ArrayList集合是List接口的可变长数组的实现。他的底层是用数组实现的。</p>
<h4 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h4><ul>
<li><p>集合中允许null值存在；</p>
</li>
<li><p>集合容量可变，自增长，约1.5倍；</p>
</li>
<li><p>非线程安全。</p>
</li>
</ul>
<h4 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h4><ul>
<li>属性列表</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_CAPACITY = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object[] EMPTY_ELEMENTDATA = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">transient</span> Object[] elementData;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> size;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里面有一个很重要的属性<code>elementData</code>,该集合对象的所有的值都是存储在这个数组对象中的,只是将存值和取值的实现进行了一定的封装.可以看到,这个属性是反序列化的,这说明ArrayList这个类一定实现了<code>java.io.Serializable</code>接口.</p>
<ul>
<li>构造器源码</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (initialCapacity &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">this</span>.elementData = <span class="keyword">new</span> Object[initialCapacity];</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (initialCapacity == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">this</span>.elementData = EMPTY_ELEMENTDATA;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal Capacity: "</span>+</span><br><span class="line">                                           initialCapacity);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">    elementData = c.toArray();</span><br><span class="line">    <span class="keyword">if</span> ((size = elementData.length) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// c.toArray might (incorrectly) not return Object[] (see 6260652)</span></span><br><span class="line">        <span class="keyword">if</span> (elementData.getClass() != Object[]<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">            <span class="title">elementData</span> </span>= Arrays.copyOf(elementData, size, Object[]<span class="class">.<span class="keyword">class</span>)</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// replace with empty array.</span></span><br><span class="line">        <span class="keyword">this</span>.elementData = EMPTY_ELEMENTDATA;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;当使用空构造器时，会将类的静态常量所引用的空数组的地址值赋给<code>elementData</code>属性,此时,该对象被初始化为一个空集合.当使用传递一个int型参数的构造器时,若传参不合法抛出异常,传参为0初始化为一个空集合,传参大于0,则将<code>elementData</code>数组初始化,长度为传递的参数大小.第三种形式就是传递一个集合,将这个集合初始化.</p>
<h4 id="数据存储"><a href="#数据存储" class="headerlink" title="数据存储"></a>数据存储</h4><p>&emsp;&emsp;ArrayList添加元素有<code>set</code>和<code>add</code>方法</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>ArrayList</tag>
        <tag>HashMap</tag>
      </tags>
  </entry>
  <entry>
    <title>枚举类</title>
    <url>/enum.html</url>
    <content><![CDATA[<p>&emsp;&emsp;枚举类型（Enumerated Type） 很早就出现在编程语言中，它被用来将一组类似的值包含到一种类型当中。而这种枚举类型的名称则会被定义成独一无二的类型描述符，在这一点上和常量的定义相似。不过相比较常量类型，枚举类型可以为申明的变量提供更大的取值范围。</p>
<a id="more"></a>
<h3 id="定义枚举类型"><a href="#定义枚举类型" class="headerlink" title="定义枚举类型"></a>定义枚举类型</h3><ul>
<li>定义性别</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Sex &#123;</span><br><span class="line">    MALE,FEMALE;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;枚举的定义是通过关键字<code>enum</code>定义的,它表明这个类是一个枚举类,其中,有几种类型,就直接定义就好了,中间用逗号分隔,用封号结尾,枚举值一般采用大写形式.</p>
<p>&emsp;&emsp;我们所有的枚举类实际上都是继承自Enum类,那么根据java的单继承特性,枚举类是不能在继承其他的类了,当然,这并不妨碍它实现接口.</p>
<ul>
<li>使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sdmkl</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"Sex.FEMALE -&gt; "</span>+Sex.FEMALE);</span><br><span class="line">    System.out.println(<span class="string">"Sex.FEMALE.name() -&gt; "</span>+Sex.FEMALE.name());</span><br><span class="line">    System.out.println(<span class="string">"Sex.FEMALE.toString() -&gt; "</span>+Sex.FEMALE.toString());</span><br><span class="line">    System.out.println(<span class="string">"Sex.FEMALE.ordinal() -&gt; "</span>+Sex.FEMALE.ordinal());</span><br><span class="line">    System.out.println(<span class="string">"Sex.valueOf(\"FEMALE\") -&gt; "</span>+Sex.valueOf(<span class="string">"FEMALE"</span>));</span><br><span class="line">    System.out.println("Enum.valueOf(Sex.class,\"FEMALE\") -&gt; "+Enum.valueOf(Sex.class,"FEMALE"));</span><br><span class="line">    System.out.println(<span class="string">"Arrays.toString(Sex.values()) -&gt; "</span>+Arrays.toString(Sex.values()));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-test-1.png" alt="运行结果"></p>
<p>&emsp;&emsp;前面三种方式输出的都是变量的字符串形式;<code>ordinal()</code>方法输出该枚举变量声明的序列,序列从0开始;<code>valueOf()</code>方法重载了两种实现方式,第一种传递枚举变量的名称,第二种传递枚举类的Class对象和名称,两者都返回对应的枚举变量名称;<code>values()</code>方法返回该枚举类的所有枚举变量数组.</p>
<h3 id="自定义构造器"><a href="#自定义构造器" class="headerlink" title="自定义构造器"></a>自定义构造器</h3><p>&emsp;&emsp;有时候,我们需要给自定义的枚举类型添加一些中文描述或者添加一些标志之类的操作,那么我们可以自定义构造器以满足我们的需求.</p>
<ul>
<li>自定义构造器</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Sex2&#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>),</span><br><span class="line">    FEMALE(<span class="string">"女"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String description;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Sex2</span><span class="params">(String description)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.description = description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> description;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sfsnkjc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE -&gt; "</span>+Sex2.FEMALE);</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.name() -&gt; "</span>+Sex2.FEMALE.name());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.toString() -&gt; "</span>+Sex2.FEMALE.toString());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.ordinal() -&gt; "</span>+Sex2.FEMALE.ordinal());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.valueOf(\"FEMALE\") -&gt; "</span>+Sex2.valueOf(<span class="string">"FEMALE"</span>));</span><br><span class="line">    System.out.println("Enum.valueOf(Sex2.class,\"FEMALE\") -&gt; "+Enum.valueOf(Sex2.class,"FEMALE"));</span><br><span class="line">    System.out.println(<span class="string">"Arrays.toString(Sex2.values()) -&gt; "</span>+Arrays.toString(Sex2.values()));</span><br><span class="line"></span><br><span class="line">    System.out.println(Sex2.MALE.getDescription());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-test-2.png" alt="运行结果"></p>
<p>&emsp;&emsp;首先在枚举类中定义一个变量,安全起见,这个变量最好是私有的,通过构造器传递形参,然后将这个形参赋值给我们定义的私有属性,在我们的枚举变量后面使用括号来定义中文描述,再实现这个私有属性的getter方法以便外界获取.这样,我们就可以获取到我们枚举变量的中文描述啦.</p>
<h3 id="重写父类方法"><a href="#重写父类方法" class="headerlink" title="重写父类方法"></a>重写父类方法</h3><p>&emsp;&emsp;所有的枚举类都是继承自<code>Enum</code>类的,而<code>Enum</code>类中只有<code>toString()</code>方法不是用final修饰的,也就是说,我们自定义枚举类,只能重写<code>toString()</code>方法.</p>
<ul>
<li>重写<code>toString()</code>方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> Sex2&#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>),</span><br><span class="line">    FEMALE(<span class="string">"女"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String description;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Sex2</span><span class="params">(String description)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.description = description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Sex2&#123;"</span> +</span><br><span class="line">                <span class="string">"description='"</span> + description + <span class="string">'\''</span> +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sfsnkjc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE -&gt; "</span>+Sex2.FEMALE);</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.name() -&gt; "</span>+Sex2.FEMALE.name());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.toString() -&gt; "</span>+Sex2.FEMALE.toString());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.FEMALE.ordinal() -&gt; "</span>+Sex2.FEMALE.ordinal());</span><br><span class="line">    System.out.println(<span class="string">"Sex2.valueOf(\"FEMALE\") -&gt; "</span>+Sex2.valueOf(<span class="string">"FEMALE"</span>));</span><br><span class="line">    System.out.println("Enum.valueOf(Sex2.class,\"FEMALE\") -&gt; "+Enum.valueOf(Sex2.class,"FEMALE"));</span><br><span class="line">    System.out.println(<span class="string">"Arrays.toString(Sex2.values()) -&gt; "</span>+Arrays.toString(Sex2.values()));</span><br><span class="line"></span><br><span class="line">    System.out.println(Sex2.MALE.getDescription());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-test-3.png" alt="运行结果"></p>
<p>&emsp;&emsp;根据结果我们知道,直接电泳枚举类名点枚举变量的方式实际上就是调用<code>toString()</code>方法,<code>valueOf()</code>方法也是调用的<code>toString()</code>方法.</p>
<h3 id="枚举中的抽象方法"><a href="#枚举中的抽象方法" class="headerlink" title="枚举中的抽象方法"></a>枚举中的抽象方法</h3><p>&emsp;&emsp;在枚举中,有时候我们每一个枚举变量可能需要进行不同的操作,这时,我们可以在枚举类中定义一个抽象方法,然后每一个枚举变量都实现此抽象方法,在实现抽象方法的时候进行不同的以满足我们的需求.</p>
<ul>
<li>定义抽象方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Sex &#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is male"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    FEMALE(<span class="string">"女"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is female"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String description;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Sex</span><span class="params">(String description)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.description = description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Sex&#123;description='"</span> + description + <span class="string">"'&#125;"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sbkcj</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Sex male = Sex.MALE;</span><br><span class="line">    Sex female = Sex.FEMALE;</span><br><span class="line">    System.out.println(male.info());</span><br><span class="line">    System.out.println(female.info());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-abstract.png" alt="运行结果"></p>
<p>&emsp;&emsp;需要注意的是,此抽象方法的访问限制符大多数情况还是需要给public的,如果你省略访问修饰符不写那么此方法只能包下可见,包外的类中就访问不到这个方法了.</p>
<p>&emsp;&emsp;既然枚举变量可以重写枚举类中的抽象方法,那么其他的方法呢?</p>
<ul>
<li>枚举变量重写枚举类的方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Sex &#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is male"</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">super</span>.getDescription();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"haha"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    FEMALE(<span class="string">"女"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is female"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String description;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Sex</span><span class="params">(String description)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.description = description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Sex&#123;description='"</span> + description + <span class="string">"'&#125;"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;具体调用和上例中调用抽象方法的方式是一样的,这里就不展示运行结果了.</p>
<p>&emsp;&emsp;到这里,给我的感觉就是枚举变量很像是这个枚举类的一个子类,枚举变量可以实现枚举类的抽象方法,可以重写枚举类的方法,而且用IDE重写默认实现是调用<code>super.方法签名</code>的方式,像极了子类重写父类方法的形式,那么问题来了,可以在枚举变量中定义属性么?</p>
<ul>
<li>枚举变量中定义属性及方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">enum</span> Sex &#123;</span><br><span class="line">    MALE(<span class="string">"男"</span>)&#123;</span><br><span class="line">        <span class="keyword">private</span> String characteristic;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">long</span> number = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//        public static String HOBBY = "female";</span></span><br><span class="line"></span><br><span class="line">        &#123;</span><br><span class="line">            number++;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/*static&#123;</span></span><br><span class="line"><span class="comment">            </span></span><br><span class="line"><span class="comment">        &#125;*/</span></span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">getCharacteristic</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> characteristic;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCharacteristic</span><span class="params">(String characteristic)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">this</span>.characteristic = characteristic;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">getNumber</span><span class="params">()</span></span>&#123;</span><br><span class="line">            <span class="keyword">return</span> number;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is male"</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">super</span>.getDescription();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"haha"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    FEMALE(<span class="string">"女"</span>)&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"This is female"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String description;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">Sex</span><span class="params">(String description)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.description = description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getDescription</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> description;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Sex&#123;description='"</span> + description + <span class="string">"'&#125;"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;惊奇的发现,真的可以定义属性和方法以及初始化域,但是!静态的属性,方法,域是不可以的,会报编译错误.这样看来,反而觉得上面的关于枚举变量类似枚举类的子类的臆想不准确,其实枚举变量更像是枚举类的一个内部类.</p>
<p><img src="/enum/enum-inner.png" alt="运行结果"></p>
<p>&amp;emsp&emsp;在枚举变量中定义私有属性,初始化域,方法没有报错,编译也能正常的通过,但是这里我遇到了一个问题,除了实现的抽象方法以及重写的枚举类的方法外我并不能获取我定义的方法.</p>
<p><img src="/enum/enum-can-not-get.png" alt="运行结果"></p>
<p>&emsp;&emsp;既然常规方法不能,那么这里我换一种方式,通过反射获取方法然后调用.</p>
<ul>
<li>反射获取方法</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ancsjaknc</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Sex male = Sex.MALE;</span><br><span class="line">    Class clazz = male.getClass();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        Method method = clazz.getMethod(<span class="string">"getNumber"</span>);</span><br><span class="line">        System.out.println(method);</span><br><span class="line">        System.out.println(method.invoke(male));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (NoSuchMethodException e) &#123;</span><br><span class="line">        System.out.println(<span class="string">"未找到该方法"</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (IllegalAccessException e) &#123;</span><br><span class="line">        System.out.println(<span class="string">"方法调用出错"</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (InvocationTargetException e) &#123;</span><br><span class="line">        System.out.println(<span class="string">"方法调用出错"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-reflect.png" alt="运行结果"></p>
<p>&emsp;&emsp;想法是美好的,现实是残酷的.确实,我们可以通过反射获取该方法,但是在使用<code>invoke()</code>方法调用方法时却出问题了.对于这样的情况我心里有个大概的猜想,但是为了不误导,此处就不多提及.到此,我们知道枚举变量中可以重写枚举类的方法,不管是抽象的还是非抽象的都可以实现,调用也是没有问题的,至于定义私有属性和公有方法暂且当不可能吧,毕竟虽然可以编译通过,但是并不能调用.</p>
<h3 id="枚举和接口"><a href="#枚举和接口" class="headerlink" title="枚举和接口"></a>枚举和接口</h3><p>&emsp;&emsp;之前有提到过,所有的枚举类都是继承自<code>Enum</code>类,而java都是单继承的,所以枚举类不能继承,但是可以实现多接口.</p>
<ul>
<li>实现接口</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">SexDescription</span></span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">enum</span> Sex3 implements SexDescription&#123;</span><br><span class="line">    MALE,FEMALE;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(StringUtils.equals(<span class="keyword">this</span>.name(),MALE.name()))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"男"</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(StringUtils.equals(<span class="keyword">this</span>.name(),FEMALE.name()))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"女"</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">"未知"</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dsbjj</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Sex3 sex3 = Sex3.MALE;</span><br><span class="line">    System.out.println(sex3.info());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/enum/enum-interface-1.png" alt="运行结果"></p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>枚举类型</tag>
      </tags>
  </entry>
  <entry>
    <title>泛型</title>
    <url>/genericity.html</url>
    <content><![CDATA[<p>&emsp;&emsp;Java 泛型（generics）是 JDK 5 中引入的一个新特性, 泛型提供了编译时类型安全检测机制，该机制允许程序员在编译时检测到非法的类型。泛型的本质是参数化类型，也就是说所操作的数据类型被指定为一个参数。</p>
<a id="more"></a>
<h3 id="泛型类"><a href="#泛型类" class="headerlink" title="泛型类"></a>泛型类</h3><h4 id="泛型初涉"><a href="#泛型初涉" class="headerlink" title="泛型初涉"></a>泛型初涉</h4><ul>
<li>定义泛型类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Point</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T x;</span><br><span class="line">    <span class="keyword">private</span> T y;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Point</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Point</span><span class="params">(T x, T y)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">        <span class="keyword">this</span>.y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getX</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setX</span><span class="params">(T x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getY</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setY</span><span class="params">(T y)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Point&#123;"</span> +</span><br><span class="line">                <span class="string">"x="</span> + x +</span><br><span class="line">                <span class="string">", y="</span> + y +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在类名<code>Point</code>后面加上一对尖括号,尖括号中一个英文字母,这样就表示这个类是一个泛型类.这个英文字母在实际应用中可以自己替换成任意一个英文字母,但是在该类中使用这个泛型的时候字母要和这个尖括号中的字母保持一致,不然会报编译错误.在这个泛型类中使用这个T,可以将T当做一个类来进行使用,例如当做Object类,那么T可以当做私有属性的类型,可以作为构造器的形参类型,可以作为方法的形参类型或者是返回值类型来使用.</p>
<ul>
<li>实例化泛型类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">genericityClassTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Point&lt;Integer&gt; point = <span class="keyword">new</span> Point&lt;Integer&gt;(<span class="number">10</span>,<span class="number">10</span>);</span><br><span class="line">    Point&lt;Float&gt; p = <span class="keyword">new</span> Point&lt;Float&gt;(<span class="number">10f</span>,<span class="number">10f</span>);</span><br><span class="line">    System.out.println(point);</span><br><span class="line">    System.out.println(p);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/genericity/genericity_class.png" alt="运行结果"></p>
<p>&emsp;&emsp;在实例化泛型类的时候,需要在尖括号中传递一个类名,这个类名就是你希望限定这个对象类型的类名,例如你希望这个对象都是以Integer类型来进行数据处理,那么在尖括号中传递Integer,那么这个对象所实例化的那个类中的T将全部代表Integer类,此时你使用这个泛型的方法,构造器时,如果有T作为参数类型的那么就要求你全部使用Integer类型的参数,否则会报编译时错误.</p>
<h4 id="多泛型变量定义"><a href="#多泛型变量定义" class="headerlink" title="多泛型变量定义"></a>多泛型变量定义</h4><p>&emsp;&emsp;多泛型定义的java类形如Map集合.</p>
<ul>
<li>定义</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 坐标类</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@author</span> ferry ferry_sy@163.com</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &lt;X&gt; x轴坐标的类型</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@param</span> &lt;Y&gt; y轴坐标的类型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Point</span>&lt;<span class="title">X</span>,<span class="title">Y</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> X x;</span><br><span class="line">    <span class="keyword">private</span> Y y;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Point</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Point</span><span class="params">(X x, Y y)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">        <span class="keyword">this</span>.y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> X <span class="title">getX</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setX</span><span class="params">(X x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Y <span class="title">getY</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setY</span><span class="params">(Y y)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.y = y;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Point&#123;"</span> +</span><br><span class="line">                <span class="string">"x="</span> + x +</span><br><span class="line">                <span class="string">", y="</span> + y +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里重构了坐标类,使用两个泛型变量进行类的定义,其中X代表x轴的数据类型,Y代表y轴数据类型.当然,在定义泛型类时你可以使用任意多的泛型变量来满足你的需求.</p>
<ul>
<li>实例化</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">genericityClassTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Point&lt;Integer, Double&gt; point = <span class="keyword">new</span> Point&lt;Integer, Double&gt;(<span class="number">10</span>,<span class="number">10</span>D);</span><br><span class="line">    System.out.println(point);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/genericity/genericity-class-more.png" alt="运行结果"></p>
<p>&emsp;&emsp;在创建实例的时候,这里选择将x轴使用Integer类型,y轴使用Double类型来实例化坐标.</p>
<h4 id="字母命名规范"><a href="#字母命名规范" class="headerlink" title="字母命名规范"></a>字母命名规范</h4><p>&emsp;&emsp;在定义泛型时可以使用任意字母,他们所表示的意义是一样的,但是在实际应用中,我们还是应该使用有意义的字母来进行泛型的定义</p>
<ol>
<li><p>E — Element，常用在java Collection里，如：List<e>,Iterator<e>,Set<e></e></e></e></p>
</li>
<li><p>K,V — Key，Value，代表Map的键值对</p>
</li>
<li><p>N — Number，数字</p>
</li>
<li><p>T — Type，类型，如String，Integer等等</p>
</li>
</ol>
<h3 id="泛型接口"><a href="#泛型接口" class="headerlink" title="泛型接口"></a>泛型接口</h3><h4 id="接口定义"><a href="#接口定义" class="headerlink" title="接口定义"></a>接口定义</h4><ul>
<li>定义</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Fruits</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">toString</span><span class="params">(T <span class="keyword">var</span>)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="实现类方式一"><a href="#实现类方式一" class="headerlink" title="实现类方式一"></a>实现类方式一</h5><ul>
<li>定义</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Banana</span> <span class="keyword">implements</span> <span class="title">Fruits</span>&lt;<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Banana</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Banana</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">(String <span class="keyword">var</span>)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name.concat(<span class="string">" "</span>).concat(<span class="keyword">var</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用非泛型类实现泛型接口,此时需要将泛型变量赋予具体的类名,此时实现该接口方法时编译器会检测所实现的方法的泛型变量所代表的类型与实现泛型接口的数据类型是否一致,不一致时编译器会认为你并没有实现这个接口的方法.</p>
<p>&emsp;&emsp;<code>@Data</code>是<code>lombok</code>的注解,它的作用是用来自动生成getter和setter等方法.</p>
<ul>
<li>使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">genericityInterfaceTest1</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Banana banana = <span class="keyword">new</span> Banana();</span><br><span class="line">    banana.setName(<span class="string">"banana"</span>);</span><br><span class="line">    System.out.println(banana.toString(<span class="string">"is delicious!"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/genericity/genericity-interface-1.png" alt="运行结果"></p>
<h4 id="实现类方式二"><a href="#实现类方式二" class="headerlink" title="实现类方式二"></a>实现类方式二</h4><ul>
<li>定义</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Fruits</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T weight;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Apple</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">(T weight)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.weight = weight;</span><br><span class="line">        <span class="keyword">return</span> toString();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Apple&#123;"</span> +</span><br><span class="line">                <span class="string">"weight="</span> + weight +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用泛型类实现泛型接口,这个泛型类的泛型变量可以有多个,但是必须有一个变量与泛型接口的泛型变量保持一致.</p>
<ul>
<li>使用</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">genericityInterfaceTest2</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Apple&lt;Double&gt; apple = <span class="keyword">new</span> Apple&lt;Double&gt;();</span><br><span class="line">    System.out.println(apple.toString(<span class="number">0.5</span>D));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/genericity/genericity-interface-2.png" alt="运行结果"></p>
<h3 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h3><ul>
<li>在一个普通java类中定义一个泛型类.</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> &lt;T&gt; <span class="function">T <span class="title">genericity</span><span class="params">(T <span class="keyword">var</span>)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">var</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 泛型方法测试</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">genericityMethodTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    Banana banana = <span class="keyword">new</span> Banana(<span class="string">"banana"</span>);</span><br><span class="line">    System.out.println(genericity(banana).getClass().getName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/genericity/genericity-method.png" alt="运行结果"></p>
<p>&emsp;&emsp;定义一个方法,在返回值类型前面加上泛型变量即可声明这是一个泛型方法,可以通过形参传递泛型,也可以返回泛型所指定的对象.在本例中实例化<code>Banana</code>类,然后传递到这个泛型方法中,最后返回这个<code>Banana</code>对象.</p>
<p>&emsp;&emsp;静态方法也可以定义为泛型方法,形式与上述方式类似.</p>
<h3 id="泛型的通配符"><a href="#泛型的通配符" class="headerlink" title="泛型的通配符"></a>泛型的通配符</h3><h4 id="无边界通配符"><a href="#无边界通配符" class="headerlink" title="无边界通配符"></a>无边界通配符</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">List&lt;?&gt;</span><br></pre></td></tr></table></figure>
<h4 id="上边界通配符"><a href="#上边界通配符" class="headerlink" title="上边界通配符"></a>上边界通配符</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="keyword">protected</span> String name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String id;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> <span class="keyword">extends</span> <span class="title">A</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String sex;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span> List&lt;? extends A&gt; sbadjh(List&lt;? extends A&gt; list)&#123;</span><br><span class="line">    list.forEach(a -&gt; a.name = <span class="string">"this is a."</span>);</span><br><span class="line">    <span class="keyword">return</span> list;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sabdkjk</span><span class="params">()</span></span>&#123;</span><br><span class="line">    List&lt;A&gt; aList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    B b = <span class="keyword">new</span> B();</span><br><span class="line">    C c = <span class="keyword">new</span> C();</span><br><span class="line">    aList.add(b);</span><br><span class="line">    aList.add(c);</span><br><span class="line">    sbadjh(aList).forEach(a -&gt; System.out.println(a.name));</span><br><span class="line"></span><br><span class="line">    List&lt;B&gt; bList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    B b1 = <span class="keyword">new</span> B();</span><br><span class="line">    B b2 = <span class="keyword">new</span> B();</span><br><span class="line">    bList.add(b1);</span><br><span class="line">    bList.add(b2);</span><br><span class="line">    sbadjh(bList).forEach(a -&gt; System.out.println(a.name));</span><br><span class="line"></span><br><span class="line">    List&lt;C&gt; CList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    C c1 = <span class="keyword">new</span> C();</span><br><span class="line">    C c2 = <span class="keyword">new</span> C();</span><br><span class="line">    CList.add(c1);</span><br><span class="line">    CList.add(c2);</span><br><span class="line">    sbadjh(CList).forEach(a -&gt; System.out.println(a.name));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="下边界通配符"><a href="#下边界通配符" class="headerlink" title="下边界通配符"></a>下边界通配符</h4><p>&emsp;&emsp;下边界通配符使用和上边界通配符正好反过来,将<code>extends</code>关键字换成<code>super</code>.</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>泛型</tag>
      </tags>
  </entry>
  <entry>
    <title>git</title>
    <url>/git.html</url>
    <content><![CDATA[<p>&emsp;&emsp;git是非常先进的分布式版本控制系统，使用git可以将本地文件托管到github之类的服务器上并进行高效的版本控制。</p>
<a id="more"></a>
<h2 id="Git简单配置"><a href="#Git简单配置" class="headerlink" title="Git简单配置"></a>Git简单配置</h2><h3 id="查看配置信息"><a href="#查看配置信息" class="headerlink" title="查看配置信息"></a>查看配置信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --system</span><br><span class="line">git config --global user.name</span><br><span class="line">git config --<span class="built_in">local</span> --list</span><br></pre></td></tr></table></figure>
<h3 id="删除配置"><a href="#删除配置" class="headerlink" title="删除配置"></a>删除配置</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --<span class="built_in">local</span> --<span class="built_in">unset</span> user.name</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;作用域越小，优先级越高，即<code>local</code>的配置会覆盖<code>global</code>的配置；<code>global</code>的配置会覆盖<code>system</code>的配置。</p>
<h3 id="用户信息"><a href="#用户信息" class="headerlink" title="用户信息"></a>用户信息</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global user.name ferry</span><br><span class="line">git config --global user.email ferry_sy@163.com</span><br></pre></td></tr></table></figure>
<h3 id="记住密码"><a href="#记住密码" class="headerlink" title="记住密码"></a>记住密码</h3><p>&emsp;&emsp;有时候我们使用https方式clone项目，这时我们每次<code>git pull</code>和<code>git push</code>的时候都要求输入用户名密码，这里不提供切换ssh的方式，采用另一个方式，让本地记住你的用户名密码。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global credential.helper store</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global credential.helper cache</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这条指令的效果是在.gitconfig文件最后添加以下配置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[credential]</span><br><span class="line">helper = store/cache</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这时，你在git pull时会要求你输入用户名密码，输入正确后该用户名密码会被记录在本地，下次就不用输入了。store永久存储密码，cache临时记忆密码，可以通过<code>git config --global credential.helper ‘cache –timeout=3600’</code>来指定临时记忆密码的时间，单位秒</p>
<h3 id="本文编辑器"><a href="#本文编辑器" class="headerlink" title="本文编辑器"></a>本文编辑器</h3><p>&amp;&emsp;&emsp;设置文本编辑器以便git在调用文本编辑器时调用我们指定的编辑器工作。</p>
<ul>
<li>方式一：命令行</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --global core.editor <span class="string">"subl -w"</span></span><br></pre></td></tr></table></figure>
<ul>
<li>方式二：修改配置文件(～/.gitconfig)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[core]</span><br><span class="line">	editor = subl -w</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里使用<code>-w</code>参数是为了让git等待编辑器完成编辑操作并且保存退出后才可以进行下一步操作。在window是使用sublime可能还需要将sublime配置环境变量，且如果exe文件中有空格是需要使用单引号将括起来（<code>git config --global core.editor “‘sublime text.exe’ -w”</code>）。使用其他的文本编辑器作为git的默认编辑器方法大致相同。</p>
<h3 id="设置别名"><a href="#设置别名" class="headerlink" title="设置别名"></a>设置别名</h3><p>&emsp;&emsp;对于一些常用的命令我们可以设置别名来使用，协助我们高效进行版本控制。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --<span class="built_in">local</span> alias.s status</span><br><span class="line">git config --<span class="built_in">local</span> alias.last <span class="string">'log -n1'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;此时，在这个项目中就可以使用<code>git s</code>代替<code>git status</code>查看git状态了，可以使用<code>git last</code>代替<code>git log -n1</code>查看最后一次提交的内容了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --<span class="built_in">local</span> alias.k <span class="string">'!gitk'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;没有<code>!</code>的别名是作为git命令使用的，使用<code>!</code>开头的命令则调用的是外部命令。如上述，可以使用<code>git k</code>代替<code>gitk</code>命令。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git config --<span class="built_in">local</span> alias.lastoneline <span class="string">'!sh -c "git log --oneline --graph -n$1" -'</span></span><br><span class="line">git config --<span class="built_in">local</span> alias.last <span class="string">'!f() &#123; git log --graph -n$1; &#125;; f'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;设置别名传递参数可以通过shell调用git或者通过函数调用git。使用：<code>git last 5</code>，<code>git lastoneline 3</code>。</p>
<h2 id="git应用"><a href="#git应用" class="headerlink" title="git应用"></a>git应用</h2><h3 id="git-init"><a href="#git-init" class="headerlink" title="git init"></a>git init</h3><ul>
<li>方式一：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir git_study</span><br><span class="line"><span class="built_in">cd</span> git_study/</span><br><span class="line">git init</span><br><span class="line">ls -al</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_init_1.png" alt="初始化git项目方式1"></p>
<ul>
<li>方式二：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git init git_study</span><br><span class="line"><span class="built_in">cd</span> git_study/</span><br><span class="line">ls -al</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_init_2.png" alt="初始化git项目方式2"></p>
<p>&emsp;&emsp;方式一是先创建项目目录，然后进入项目根目录创建git的管理目录<code>.git</code>；方式二是在当前目录创建项目目录的同时在项目根目录中生成<code>.git</code>目录。</p>
<h3 id="git-add"><a href="#git-add" class="headerlink" title="git add"></a>git add</h3><ul>
<li>添加单个或者多个指定文件到暂存区，可以使用通配符进行批量添加</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add <span class="built_in">test</span> [test2]</span><br></pre></td></tr></table></figure>
<ul>
<li>批量添加已经被git管理的文件到暂存区</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add -u</span><br></pre></td></tr></table></figure>
<ul>
<li>提交所有变化到暂存区</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add .</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git add -A</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在 Git2.<em> 版本中 <code>git add .</code> 和 <code>git add -A</code> 没有区别，都是提交所有变化到暂存区；但是在1.</em>版本中 <code>git add .</code> 只处理修改的和新增的文件，删除的文件不做处理，<code>git add -A</code> 处理所有变化。</p>
<h3 id="git-commit"><a href="#git-commit" class="headerlink" title="git commit"></a>git commit</h3><ul>
<li>无参数提交</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -m <span class="string">'提交的注释'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用<code>git commit</code>不加任何参数则提交暂存区的变更到本地仓库,<code>-m</code>写注释信息，如果使用<code>git commit</code>指令的使用没有加<code>-m</code>那么会弹出编辑界面，在此界面输入提交信息。需要注意，其他参数需要在<code>-m</code>参数之前，<code>-m</code>参数后面的内容会被当作注释提交上去。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -a</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用<code>git commit -a</code>将已经被git跟踪的文件的变更提交到暂存区，然后将暂存区的变更提交到本地仓库，此时没有加<code>-m</code>参数，所以会弹出编辑界面，在此界面输入提交信息。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit -a -m<span class="string">'提交信息'</span></span><br><span class="line">git commit -am<span class="string">'提交信息'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;将<code>-a</code>和<code>-m</code>指令结合起来的形式有如上两种。</p>
<ul>
<li><code>--amend</code>参数</li>
</ul>
<p>&emsp;&emsp;使用<code>--amend</code>参数表示本次提交是一次增补提交，git会将当前提交清除，在父节点上再此生成一次提交记录。</p>
<p><img src="/git/amend.png" alt="增补提交"></p>
<ul>
<li>指定文件(目录)提交</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git commit <span class="built_in">test</span> -m<span class="string">'submit test'</span></span><br></pre></td></tr></table></figure>
<h3 id="git-log"><a href="#git-log" class="headerlink" title="git log"></a>git log</h3><ul>
<li>无参数查看</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span></span><br></pre></td></tr></table></figure>
<p><img src="/git/git_log_none_param.png" alt="无参数查看历史"></p>
<p>&emsp;&emsp;按 q 键退出</p>
<ul>
<li>查看指定数量的历史记录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> -n5</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> -5</span><br></pre></td></tr></table></figure>
<ul>
<li>以单行显示历史记录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline</span><br></pre></td></tr></table></figure>
<ul>
<li>查看指定分支历史</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> master</span><br></pre></td></tr></table></figure>
<ul>
<li>查看分支演变历史</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --graph</span><br></pre></td></tr></table></figure>
<ul>
<li>查看每次提交的文件列表</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --<span class="built_in">stat</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;更多参数可以参考<code>git log --help</code></p>
<ul>
<li>多个参数结合</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline -n10 --all --graph --no-merges</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_log_params.png" alt="定制化查看历史"></p>
<h3 id="git-show"><a href="#git-show" class="headerlink" title="git show"></a>git show</h3><ul>
<li>git show hash值</li>
</ul>
<p>&emsp;&emsp;hash值对应的类型有三种：</p>
<blockquote>
<p>blob: 查看这个文件的具体内容<br>tree: 查看这颗树的目录结构<br>commit: 查看本次提交的变更内容</p>
</blockquote>
<p>&emsp;&emsp;<code>git show</code>后面也可以跟branch/tag，但究其本质还是使用的branch/tag所在的commit_id,所以此时查看的也就是这个branch/tag最新的提交的变更内容。</p>
<h3 id="gitk"><a href="#gitk" class="headerlink" title="gitk"></a>gitk</h3><ul>
<li>git图形化工具</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gitk</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在gitk中，你可以通过这个图形化工具查看log，diff等信息。</p>
<h3 id="git-reset"><a href="#git-reset" class="headerlink" title="git reset"></a>git reset</h3><p>&emsp;&emsp;reset总共会有三个步骤，通过不同的指令可以是reset在某一步骤停住脚步从而达到我们要的效果。</p>
<ol>
<li><p>更新HEAD，通过参数<code>--soft</code>使reset在此停留；</p>
</li>
<li><p>更新暂存区，通过参数<code>--mixed</code>使reset在此停留，缺省参数则默认为<code>--mixed</code>;</p>
</li>
<li><p>更新工作目录，通过参数<code>--hard</code>使reset在此停留，此操作会将工作目录下修改但为提交的内容移除，无法再通过git找回，慎用！！！</p>
</li>
</ol>
<p>&emsp;&emsp;新建一个项目并创建三次提交来演示reset。</p>
<p><img src="/git/create_test_project.png" alt="创建项目演示reset"></p>
<ul>
<li><code>git reset --soft commit_id</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --soft de08188aa</span><br></pre></td></tr></table></figure>
<p><img src="/git/reset--soft.png" alt="--soft"></p>
<p>&emsp;&emsp;此时git将 HEAD 指向 de08188aa 提交上，但是工作区和暂存区不变。</p>
<ul>
<li><code>git reset --mixed commit_id</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset HEAD</span><br></pre></td></tr></table></figure>
<p><img src="/git/reset--mixed.png" alt="--mixed"></p>
<p>&emsp;&emsp;基于刚才的重置HEAD，此时再重置暂存区，此步操作将暂存区重置，与HEAD保持一致。</p>
<ul>
<li><code>git reset --hard commit_id</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reset --hard HEAD</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;此时将工作目录重置，保持与暂存区和HEAD的一致。</p>
<h3 id="git-checkout"><a href="#git-checkout" class="headerlink" title="git checkout"></a>git checkout</h3><p>&emsp;&emsp;<code>git checkout</code>和<code>git reset</code>功能有一定的相似性，在这里我简单的了解了一下它们的区别。<code>git checkout</code>后面跟一个commit_id，缺省的情况下默认为HEAD，在这里有必要说一点，<strong>branch,tag,HEAD本质上也是指向一个具体的提交</strong>，所以我们可以用branch,tag,HEAD替换对应的commit_id。checkout具体的提交那么git会处于分离头指针的状态，chekcout分支/标签会检出到对应的分支/标签的最新的提交上。</p>
<blockquote>
<p>不带路径: <code>git checkout commit_id</code>是检出到指定的提交，将HEAD移动到指定提交上，git将工作区和暂存区未做修改的内容先更新为当前检出的提交版本，然后将修改的文件进行合并更新，如果发生冲突，那么会要求你进行merge操作。checkout并不会修改git提交日志。但是<code>git reset</code>不一样，当你加上<code>--hard</code>时，它会将工作区，暂存区和HEAD重置到指定的提交，它是会修改git提交日志的。例如，当你执行<code>git reset HEAD^ --hard</code>后，工作区，暂存区，HEAD都重置为原HEAD的父节点，此时你查看git提交日志会发现，原提交记录不见了，HEAD指向最新的提交也就是原HEAD的父节点。</p>
</blockquote>
<blockquote>
<p>带路径: <code>git checkout commit_id</code>是检出指定文件的指定的提交版本。</p>
</blockquote>
<p><img src="/git/git_checkout.png" alt="检出"></p>
<h3 id="git-branch"><a href="#git-branch" class="headerlink" title="git branch"></a>git branch</h3><ul>
<li>新建分支</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch tmp HEAD</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch_new.png" alt="新建分支"></p>
<p>&emsp;&emsp;语法为<code>git branch 分支名 commit_id</code>，缺省commit_id默认为HEAD。</p>
<ul>
<li>查看本地所有分支</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch.png" alt="查看分支"></p>
<p>&emsp;&emsp;分支名前有<code>*</code>号的就是当前HEAD所在分支。</p>
<ul>
<li>查看本地和远端的所有分支</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -a</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch-a.png" alt="查看分支"></p>
<ul>
<li>查看分支并显示分支最新的提交信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -v</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch-v.png" alt="查看分支"></p>
<ul>
<li>显示其他与当前分支合并关系</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch --merged</span><br><span class="line">git branch --no-merged</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch-merged.png" alt="查看分支"></p>
<ul>
<li>删除分支</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git branch -d tmp</span><br><span class="line">git branch -D tmp</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_branch-d.png" alt="删除分支"></p>
<p>&emsp;&emsp;在删除分支的时候，如果git检测出待删除的分支上还有提交没有合并到其他分支，那么就会弹出提示，如果一定要删除，那么使用-D强制删除该分支。</p>
<h3 id="git-reflog"><a href="#git-reflog" class="headerlink" title="git reflog"></a>git reflog</h3><p>&emps;&emsp;<code>git reflog</code>记录了HEAD的所有变化，而<code>git log</code>显示的是但不包括删除的提交，所以如果误操作删除了某次提交记录，那么可以使用<code>git reflog</code>找到这次的提交，然后使用<code>git reset</code>或者<code>git cherry-pick</code>恢复这次提交。<code>git reflog</code>参数查询和<code>git log</code>差不多，基本可以通用，这里简单记录一下怎么恢复误删除的提交。</p>
<p>&emsp;&emsp;在这里先创建三次提交，然后使用reset回退到第二次提交，然后恢复。</p>
<p><img src="/git/git-reflog-bg.png" alt="演示背景"></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git reflog</span><br><span class="line">git checkout HEAD@&#123;0&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/git/git-reflog-checkout.png" alt="演示"></p>
<p>&emsp;&emsp;<code>git reflog</code>查看HEAD的变动记录，<strong>最前面的一段哈希值是本次操作返回的HEAD所指向的commit_id；括号中的是返回的提交所处的分支或者标签位置；HEAD@{n}表示HEAD更改历史记录,可以通过HEAD@{n}引用存在reflog中的提交；后面的内容则是展示HEAD变更的行为</strong>。使用<code>git checkout</code>检出到指定的提交上，此时处于分离头指针状态。</p>
<p>&emsp;&emsp;使用reflog命令查看到HEAD的所有变化后，可以使用reset命令直接将HEAD恢复到指定的提交上，如果所在分支后续又进行了开发，然后想将之前的某次提交内容还原，那么就可以使用cherry-pick命令将提交挑拣到当前分支。</p>
<h3 id="git-merge"><a href="#git-merge" class="headerlink" title="git merge"></a>git merge</h3><p>&emsp;&emsp;<code>git merge</code>用于将两个或多个提交合并在一起。</p>
<p>&emsp;&emsp;<strong>fast-forward</strong>：现有分支master，上面有提交记录A，现基于A创建一个分支develop，在develop上创建提交B，master未做任何修改。此时，将develop分支合并到master分支时会将master分支的文件指针直接指向提交B。这一过程成为<code>fast-forward</code>，触发<code>fast-forward</code>默认不会创建一个合并提交。</p>
<ul>
<li>简单合并</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;不使用任何参数时，将会弹出vi编辑器，可编辑本次合并的提交信息，保存后会自动提交本次合并的结果。</p>
<ul>
<li>取消合并</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge --abort</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在发生冲突的时候需要手动解决冲突然后再提交。这里可以使用<code>--abort</code>参数取消本次合并，将HEAD努力的还原到合并前的状态。但是，如果在合并前暂存区还有未提交的内容，那么冲突可能会修改未提交的内容，那么在还原的时候可能就无法重现合并前的状态。</p>
<ul>
<li>合并不自动提交</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --no-commit</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--no-commit</code>参数将是合并的脚步停止在提交前，即本次合并的结果将在HEAD中；缺省时默认为<code>--commit</code>，而且<code>--commit</code>会覆盖<code>--no-commit</code>。</p>
<ul>
<li>编辑合并提交信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id -m<span class="string">'merge branch dev'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>-m&#39;merge infomation&#39;</code>参数编辑合并成功提交时的提交信息</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --no-edit</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--no-edit</code>参数指定合并成功后提交时不需要编辑提交信息，使用默认的提交信息。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --edit</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--edit</code>或者<code>-e</code>参数表明合并成功后提交将进入vi编辑器，编辑提交信息，保存后提交成功，缺省时默认为此参数</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id -m<span class="string">'merge infomation'</span> --edit</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>-m&#39;merge infomation&#39; --edit</code>参数编辑合并成功提交时的提交信息，并在提交时进入vi编辑器进一步编辑<code>-m</code>所含提交信息。</p>
<ul>
<li>快进合并</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --ff</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--ff</code>采用<code>fast-forward</code>模式合并，缺省时默认为此值。<code>--no-ff</code>表明即使能够使用<code>fast-forward</code>模式合并也会创建一个新的合并提交节点，<code>git merge</code>合并tag是默认为此行为。<code>--ff-only</code>除非当前HEAD节点已经up-to-date（更新指向到最新节点）或者能够使用<code>fast-forward</code>模式进行合并，否则的话将拒绝合并，并返回一个失败状态。</p>
<ul>
<li>合并提交时合并提交信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --<span class="built_in">log</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--log</code>合并分支时创建的合并提交节点的提交信息将包含合并的分支名，以及合并分支的每个不在当前分支的提交的提交信息。<code>--no-log</code>不合并提交信息，缺省时的默认选项。</p>
<ul>
<li>合并显示文件差异</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --<span class="built_in">stat</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--stat</code>在合并完成后在shell上显示本次合并的文件差异状态，缺省时的默认选项。<code>--no-stat</code>或<code>-n</code>不显示合并的文件差异状态。</p>
<ul>
<li>压缩合并</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git merge commit_id --squash</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>--squash</code>将另一分支上 从两个分支共同祖先节点的下一个节点开始，至最新的节点 压缩成一个节点然后提交，和<code>--no-ff</code>参数冲突。<code>--no-squash</code>作用相反，缺省时的默认选项。</p>
<p>&emsp;&emsp;<a href="https://www.jianshu.com/p/58a166f24c81" target="_blank" rel="noopener">merge合并详解</a></p>
<h3 id="git-stash"><a href="#git-stash" class="headerlink" title="git stash"></a>git stash</h3><p>&emsp;&emsp;<code>stash</code>可以获取你工作目录的中间状态——也就是你修改过的被追踪的文件和暂存的变更——并将它保存到一个未完结变更的堆栈中，随时可以重新应用。在实际工作中，我们可能有各种各样的原因不得不先切换分支去完成别的任务，但是此时又不想将完成部分的修改提交，此时就可以使用<code>git stash</code>将修改暂存起来，等完成了别的任务再切换回来然后还原我们暂存的修改内容继续工作。</p>
<p>&emsp;&emsp;为了演示，这里创建一个文件，修改一个文件，再<code>git add</code>一个修改内容。</p>
<p><img src="/git/git_stash_init.png" alt="修改内容"></p>
<p>&emsp;&emsp;此时，我们要去修改另一个分支的bug，但是又不想提交修改或者放弃本次修改，那就暂存起来，等改完bug回来可以继续工作。</p>
<ul>
<li>暂存</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git stash</span><br></pre></td></tr></table></figure>
<p><img src="/git/git_stash_status.png" alt="暂存结果"></p>
<p>&emsp;&emsp;shell会输出一句内容，告知工作目录和index已经保存了，此时再<code>git status</code>查看会发现工作目录和index是干净的，但是我们新建的文件并没有暂存起来，这是因为新建的文件是untracked的，也就是未被git跟踪的，所以它不会被stash。</p>
<h3 id="git-rebase"><a href="#git-rebase" class="headerlink" title="git rebase"></a>git rebase</h3><h3 id="git-cherry-pick"><a href="#git-cherry-pick" class="headerlink" title="git cherry-pick"></a>git cherry-pick</h3>]]></content>
      <categories>
        <category>git</category>
      </categories>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>kubernetes集群部署</title>
    <url>/kubernetes.html</url>
    <content><![CDATA[<p>&emsp;&emsp;<code>Kubernetes</code>(简称k8s) 协调一个高可用计算机集群，每个计算机作为独立单元互相连接工作。 <code>Kubernetes</code> 中的抽象允许您将容器化的应用部署到集群，而无需将它们绑定到某个特定的独立计算机。为了使用这种新的部署模型，应用需要以将应用与单个主机分离的方式打包：它们需要被容器化。与过去的那种应用直接以包的方式深度与主机集成的部署模型相比，容器化应用更灵活、更可用。 <code>Kubernetes</code> 以更高效的方式跨集群自动分发和调度应用容器。 <code>Kubernetes</code> 是一个开源平台，并且可应用于生产环境。</p>
<a id="more"></a>
<p>&emsp;&emsp;一个 <code>Kubernetes</code> 集群包含两种类型的资源:</p>
<ul>
<li>Master 控制平面，调度整个集群</li>
<li>Nodes 工作节点，负责运行应用</li>
</ul>
<p>&emsp;&emsp;Master 负责管理整个集群。 Master 协调集群中的所有活动，例如调度应用、维护应用的所需状态、应用扩容以及推出新的更新。Node 是一个虚拟机或者物理机，它在 <code>Kubernetes</code> 集群中充当工作机器的角色 每个Node都有 <code>Kubelet</code> , 它管理 Node 而且是 Node 与 Master 通信的代理。 Node 还应该具有用于​​处理容器操作的工具，例如 <code>Docker</code> 或 <code>rkt</code> 。处理生产级流量的 <code>Kubernetes</code> 集群至少应具有三个 Node 。</p>
<p>&emsp;&emsp;Master 管理集群，Node 用于托管正在运行的应用。在 Kubernetes 上部署应用时，您告诉 Master 启动应用容器。 Master 就编排容器在集群的 Node 上运行。 Node 使用 Master 暴露的 <code>Kubernetes API</code> 与 Master 通信。终端用户也可以使用 <code>Kubernetes API</code> 与集群交互。</p>
<p>&emsp;&emsp;<code>Kubernetes</code> 既可以部署在物理机上也可以部署在虚拟机上。您可以使用 <code>Minikube</code> 开始部署 <code>Kubernetes</code> 集群。 <code>Minikube</code> 是一种轻量级的 <code>Kubernetes</code> 实现，可在本地计算机上创建 VM 并部署仅包含一个节点的简单集群。 <code>Minikube</code> 可用于 <code>Linux</code> ， <code>macOS</code> 和 <code>Windows</code> 系统。<code>Minikube CLI</code> 提供了用于引导集群工作的多种操作，包括启动、停止、查看状态和删除。</p>
<p>&emsp;&emsp;<code>kubernetes</code> 集群搭建有两种方式：</p>
<ul>
<li>使用部署工具快速搭建 <code>kubernetes</code> 集群，例如 <code>minikube</code>（快速搭建一个运行在本地的单节点的 <code>kubernetes</code>，一般用于本地学习），<code>kubeadm</code> 等</li>
<li>从官网下载二进制文件手动部署</li>
</ul>
<p>&emsp;&emsp;这里记录两种方式安装 <code>kubernetes</code> 集群，一种是使用 <code>kubeadm</code> 自动部署集群，一种是下载二进制文件手动部署。</p>
<h3 id="集群机器检查"><a href="#集群机器检查" class="headerlink" title="集群机器检查"></a>集群机器检查</h3><p>&emsp;&emsp;集群内所有节点均需检查是否符合要求。</p>
<p>&emsp;&emsp;安装要求：</p>
<blockquote>
<p>安装 k8s 的节点必须是大于 1 核心的 CPU<br>建议主机内存都是2G及以上<br>使用的物理机操作系统：Ubuntu 16.04+、Debian 9+、CentOS 7、Red Hat Enterprise Linux (RHEL) 7、Fedora 25+、HypriotOS v1.0.1+、Flatcar Container Linux<br>集群中的机器网络能够互联<br>节点之中不可以有重复的主机名、MAC 地址或 product_uuid<br>禁用交换分区。为了保证 kubelet 正常工作，你 必须 禁用交换分区</p>
</blockquote>
<p>&emsp;&emsp;集群安装要求查看并修改：</p>
<ul>
<li>查看逻辑内核数：</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /proc/cpuinfo| grep <span class="string">"processor"</span>| wc -l</span><br></pre></td></tr></table></figure>
<ul>
<li>查看主机名，mac地址，product_uuid</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hostname</span><br><span class="line">ip addr</span><br><span class="line">cat /sys/class/dmi/id/product_uuid</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果主机名重复，请修改</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hostnamectl <span class="built_in">set</span>-hostname k8s_master</span><br></pre></td></tr></table></figure>
<ul>
<li>永久禁用交换分区(/etc/fstab)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># /dev/mapper/centos-swap swap                    swap    defaults        0 0</span></span><br></pre></td></tr></table></figure>
<ul>
<li>查看内存大小</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">free -m</span><br></pre></td></tr></table></figure>
<ul>
<li>查看linux内核版本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /proc/version</span><br></pre></td></tr></table></figure>
<ul>
<li>集群机器网络互联</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ping 172.16.98.2</span><br></pre></td></tr></table></figure>
<ul>
<li>设置主机映射(/etc/hosts)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">172.16.98.2 k8s-master</span><br></pre></td></tr></table></figure>
<ul>
<li>加载模块(<code>/etc/modules-load.d/containerd.conf</code>)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">overlay</span><br><span class="line">br_netfilter</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">modprobe overlay</span><br><span class="line">modprobe br_netfilter</span><br></pre></td></tr></table></figure>
<ul>
<li>设置sysctl参数(<code>/etc/sysctl.d/99-kubernetes-cri.conf</code>)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">net.bridge.bridge-nf-call-iptables  = 1</span><br><span class="line">net.ipv4.ip_forward                 = 1</span><br><span class="line">net.bridge.bridge-nf-call-ip6tables = 1</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl --system</span><br></pre></td></tr></table></figure>
<ul>
<li>端口设置</li>
</ul>
<table>
<thead>
<tr>
<th>协议</th>
<th>方向</th>
<th>端口范围</th>
<th>作用</th>
<th>使用者</th>
</tr>
</thead>
<tbody>
<tr>
<td>TCP</td>
<td>入站</td>
<td>6443</td>
<td>Kubernetes API 服务器</td>
<td>所有组件</td>
</tr>
<tr>
<td>TCP</td>
<td>入站</td>
<td>2379-2380</td>
<td>etcd 服务器客户端 API</td>
<td>kube-apiserver, etcd</td>
</tr>
<tr>
<td>TCP</td>
<td>入站</td>
<td>10250</td>
<td>Kubelet API</td>
<td>kubelet 自身、控制平面组件</td>
</tr>
<tr>
<td>TCP</td>
<td>入站</td>
<td>10251</td>
<td>kube-scheduler</td>
<td>kube-scheduler 自身</td>
</tr>
<tr>
<td>TCP</td>
<td>入站</td>
<td>10252</td>
<td>kube-controller-manager</td>
<td>kube-controller-manager 自身</td>
</tr>
</tbody>
</table>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-port=6443/tcp --permanent</span><br><span class="line">firewall-cmd --zone=public --add-port=2379/tcp --permanent</span><br><span class="line">firewall-cmd --zone=public --add-port=2380/tcp --permanent</span><br><span class="line">firewall-cmd --zone=public --add-port=10250/tcp --permanent</span><br><span class="line">firewall-cmd --zone=public --add-port=10251/tcp --permanent</span><br><span class="line">firewall-cmd --zone=public --add-port=10252/tcp --permanent</span><br><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>
<h3 id="使用kubeadm引导集群"><a href="#使用kubeadm引导集群" class="headerlink" title="使用kubeadm引导集群"></a>使用kubeadm引导集群</h3><h4 id="安装容器"><a href="#安装容器" class="headerlink" title="安装容器"></a>安装容器</h4><p>&emsp;&emsp;这一步需要在集群所有节点执行。</p>
<p>&emsp;&emsp;在linux上结合 <code>Kubernetes</code> 使用的几种通用容器为：<code>containerd</code>、<code>CRI-O</code>、<code>Docker</code>。这里我们选择<code>Docker</code>。</p>
<p>&emsp;&emsp;这里提供传送门<a href="https://docs.docker.com/engine/install/#server" target="_blank" rel="noopener">docker官网的安装说明</a>,可以查看对应的操作系统安装 <code>Docker</code> 的详细步骤。下面的安装是基于centos7安装 <code>Docker</code> 的</p>
<ul>
<li>docker安装</li>
</ul>
<p>&emsp;&emsp;我们可以先去<a href="https://github.com/kubernetes/kubernetes/blob/v1.21.1/build/dependencies.yaml" target="_blank" rel="noopener">kubernetes的github仓库依赖关系</a>中看到我们想要安装的k8s集群版本对应的docker版本。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum remove docker docker-client docker-client-latest docker-common docker-latest docker-latest-logrotate docker-logrotate docker-engine</span><br><span class="line">yum install -y yum-utils</span><br><span class="line">yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo</span><br><span class="line">yum install -y docker-ce-20.10.6 docker-ce-cli-20.10.6 containerd.io</span><br><span class="line">systemctl start docker</span><br></pre></td></tr></table></figure>
<ul>
<li>配置(<code>/etc/docker/daemon.json</code>)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"exec-opts"</span>: [<span class="string">"native.cgroupdriver=systemd"</span>],</span><br><span class="line">  <span class="string">"log-driver"</span>: <span class="string">"json-file"</span>,</span><br><span class="line">  <span class="string">"log-opts"</span>: &#123;</span><br><span class="line">    <span class="string">"max-size"</span>: <span class="string">"100m"</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">"storage-driver"</span>: <span class="string">"overlay2"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> docker</span><br><span class="line">systemctl daemon-reload</span><br><span class="line">systemctl restart docker</span><br></pre></td></tr></table></figure>
<h4 id="安装工具"><a href="#安装工具" class="headerlink" title="安装工具"></a>安装工具</h4><ul>
<li><code>kubeadm</code>：用来初始化集群的指令。</li>
<li><code>kubelet</code>：在集群中的每个节点上用来启动 Pod 和容器等。</li>
<li><code>kubectl</code>：用来与集群通信的命令行工具。</li>
</ul>
<p>&emsp;&emsp;在实际安装之前我们还需要了解一下 <code>Kubernetes</code> 版本与版本间的偏差策略</p>
<h5 id="版本差异"><a href="#版本差异" class="headerlink" title="版本差异"></a>版本差异</h5><p>&emsp;&emsp;<code>Kubernetes</code> 版本号格式为 x.y.z，其中 x 为大版本号，y 为小版本号，z 为补丁版本号。</p>
<ul>
<li><code>kube-apiserver</code></li>
</ul>
<p>&emsp;&emsp;在 高可用（HA）集群 中， 多个 <code>kube-apiserver</code> 实例小版本号最多差1。也就是说，集群的每一个节点版本可以有差异，但是小版本差异最多为1。举例，k8s_master机器上版本号为1.19,那么集群的其他机器上安装的版本只能在1.18,1.19,1.20之间选择</p>
<ul>
<li><code>kubelet</code></li>
</ul>
<p>&emsp;&emsp;<code>kubelet</code> 版本号不能高于 <code>kube-apiserver</code>，最多可以比 <code>kube-apiserver</code> 低两个小版本。在集群中，则 <code>kubelet</code> 版本号不能高于集群中 <code>kube-apiserver</code> 最低的版本号，最多可以比集群中 <code>kube-apiserver</code> 最高的版本号低两个小版本。举例，存在 <code>kube-apiserver</code> 版本1.20，那么 <code>kubelet</code> 版本可以选择1.18,1.19,1.20。但如果集群中的 <code>kube-apiserver</code> 版本不一致，存在两个版本1.20,1.19，那么可选的 <code>kubelet</code> 版本为 1.19,1.18。</p>
<ul>
<li><code>kube-controller-manager</code>、 <code>kube-scheduler</code> 和 <code>cloud-controller-manager</code></li>
</ul>
<p>&emsp;&emsp;<code>kube-controller-manager</code>、<code>kube-scheduler</code> 和 <code>cloud-controller-manager</code> 版本不能高于 <code>kube-apiserver</code> 版本号。 最好它们的版本号与 <code>kube-apiserver</code> 保持一致，但允许比 <code>kube-apiserver</code> 低一个小版本（为了支持在线升级）。如果在 HA 集群中，多个 <code>kube-apiserver</code> 实例版本号不一致，他们也可以跟任意一个 <code>kube-apiserver</code> 实例通信（例如，通过 load balancer）， 但 <code>kube-controller-manager</code>、<code>kube-scheduler</code> 和 <code>cloud-controller-manager</code> 版本可用范围会相应的减小。</p>
<ul>
<li><code>kubectl</code></li>
</ul>
<p>&emsp;&emsp;<code>kubectl</code> 可以比 <code>kube-apiserver</code> 高一个小版本，也可以低一个小版本。在集群中，<code>kubectl</code> 可以比集群中最高的 <code>kube-apiserver</code> 版本号低一个小版本，可以比最低的 <code>kube-apiserver</code> 版本号高一个小版本。</p>
<ul>
<li><code>kube-proxy</code></li>
</ul>
<p>&emsp;&emsp;<code>kube-proxy</code> 必须与节点上的 <code>kubelet</code> 的小版本相同;<code>kube-proxy</code> 一定不能比 <code>kube-apiserver</code> 小版本高;<code>kube-proxy</code> 最多只能比 <code>kube-apiserver</code> 低两个小版本</p>
<h5 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h5><p>&emsp;&emsp;需要在集群所有机器完成这一步。</p>
<ul>
<li>k8s的yum源配置(创建文件<code>/etc/yum.repos.d/kubernetes.repo</code>)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[kubernetes]</span><br><span class="line">name=Kubernetes</span><br><span class="line">baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64</span><br><span class="line">enabled=1</span><br><span class="line">gpgcheck=1</span><br><span class="line">repo_gpgcheck=1</span><br><span class="line">gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg</span><br></pre></td></tr></table></figure>
<ul>
<li>查看可安装的版本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list kubeadm kubectl kubelet</span><br></pre></td></tr></table></figure>
<ul>
<li>安装指定版本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y kubelet-1.21.1-0 kubeadm-1.21.1-0 kubectl-1.21.1-0</span><br></pre></td></tr></table></figure>
<ul>
<li>设置服务开机自启（这里不设置在初始化集群的时候会报错）</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> kubelet</span><br></pre></td></tr></table></figure>
<h4 id="初始化集群"><a href="#初始化集群" class="headerlink" title="初始化集群"></a>初始化集群</h4><p>&emsp;&emsp;这一步只需要在控制平面执行即可。</p>
<p>&emsp;&emsp;在初始化控制平面之前，我们需要验证一下与 gcr.io 容器镜像仓库的连通性。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm config images pull</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果这一步报错，说没有响应，那么就是无法连接到该仓库，此时，我们需要从国内的镜像仓库获取对应的镜像了。这一步网上有很多的方法，例如从 <code>docker hub</code> 上搜索对应的镜像，下载并重新打标签，这种方式依赖于 <code>docker hub</code>上现存的镜像版本，如果你需要的版本没找到，那就有点尴尬了。下面提供的这种方法虽然繁琐了些，但是他能保证你需要获取的版本一定能获取到。</p>
<ul>
<li>查看需要的镜像</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm config images list</span><br></pre></td></tr></table></figure>
<p><img src="/kubernetes/k8s_need_image_list.png" alt="需要的镜像"></p>
<p>&emsp;&emsp;代码仓操作：</p>
<p>&emsp;&emsp;这里我使用github创建一个仓库专门用来创建dockerfile。</p>
<p><img src="/kubernetes/k8s_create_github_repository.png" alt="创建仓库"></p>
<ul>
<li>在仓库中创建各个组件的dockerfile文件。</li>
</ul>
<p><img src="/kubernetes/k8s_create_github_dockerfile.png" alt="创建dockerfile"></p>
<ul>
<li>登录<a href="https://cr.console.aliyun.com/cn-hangzhou/instances/repositories" target="_blank" rel="noopener">阿里云容器镜像服务</a>,创建镜像仓库。容器镜像服务-&gt;默认实例-&gt;镜像仓库-&gt;创建镜像仓库</li>
</ul>
<p><img src="/kubernetes/k8s_create_aliyun_docker_repository.png" alt="创建dockerfile"></p>
<p><img src="/kubernetes/k8s_create_aliyun_docker_repository2.png" alt="创建dockerfile"></p>
<ul>
<li>创建规则执行构建,若失败则查看日志，大概率是构建规则选的分支，文件，路径与代码仓中的不匹配</li>
</ul>
<p><img src="/kubernetes/k8s_click_aliyun_create_rule.png" alt="查看构建"></p>
<p><img src="/kubernetes/k8s_aliyun_create_docker_build_rule.png" alt="创建构建规则"></p>
<p><img src="/kubernetes/k8s_aliyun_docker_build.png" alt="执行构建"></p>
<ul>
<li>设置访问凭证</li>
</ul>
<p><img src="/kubernetes/k8s_aliyun_set_password.png" alt="执行构建"></p>
<ul>
<li>在控制平面登录阿里云的镜像仓库，用户名为阿里云的账户名，拉取镜像，这里登录的地址一定要看好，镜像仓库所在的区域不同，登录的仓库地址也是有所区别的。具体的登录地址可以点击仓库进入仓库详情页面，基本信息-操作指南中查看</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker login --username=ferry_sy registry.cn-shanghai.aliyuncs.com</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-controller-manager_v1.21.1</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-apiserver_v1.21.1</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:coredns_v1.8.0</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-proxy_v1.21.1</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:pause_3.4.1</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-scheduler_v1.21.1</span><br><span class="line">docker pull registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:etcd_3.4.13-0</span><br></pre></td></tr></table></figure>
<ul>
<li>镜像重命名</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-apiserver_v1.21.1 k8s.gcr.io/kube-apiserver:v1.21.1</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-controller-manager_v1.21.1 k8s.gcr.io/kube-controller-manager:v1.21.1</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-scheduler_v1.21.1 k8s.gcr.io/kube-scheduler:v1.21.1</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-proxy_v1.21.1 k8s.gcr.io/kube-proxy:v1.21.1</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:pause_3.4.1 k8s.gcr.io/pause:3.4.1</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:etcd_3.4.13-0 k8s.gcr.io/etcd:3.4.13-0</span><br><span class="line">docker tag registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:coredns_v1.8.0 k8s.gcr.io/coredns/coredns:v1.8.0</span><br></pre></td></tr></table></figure>
<ul>
<li>删除阿里云镜像</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-controller-manager_v1.21.1</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-apiserver_v1.21.1</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:coredns_v1.8.0</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-proxy_v1.21.1</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:pause_3.4.1</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:kube-scheduler_v1.21.1</span><br><span class="line">docker image rm registry.cn-shanghai.aliyuncs.com/ferry/docker-repository:etcd_3.4.13-0</span><br></pre></td></tr></table></figure>
<ul>
<li>初始化集群，<code>--pod-network-cidr</code> 参数用于配置CIDR，参数值可以填写 “内网ip/16”。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm init --pod-network-cidr=172.10.0.0/16 --service-cidr=10.96.0.0/12</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[init] Using Kubernetes version: v1.20.2</span><br><span class="line">[preflight] Running pre-flight checks</span><br><span class="line">	[WARNING Firewalld]: firewalld is active, please ensure ports [6443 10250] are open or your cluster may not <span class="keyword">function</span> correctly</span><br><span class="line">[preflight] Pulling images required <span class="keyword">for</span> setting up a Kubernetes cluster</span><br><span class="line">[preflight] This might take a minute or two, depending on the speed of your internet connection</span><br><span class="line">[preflight] You can also perform this action <span class="keyword">in</span> beforehand using <span class="string">'kubeadm config images pull'</span></span><br><span class="line">[certs] Using certificateDir folder <span class="string">"/etc/kubernetes/pki"</span></span><br><span class="line">[certs] Generating <span class="string">"ca"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"apiserver"</span> certificate and key</span><br><span class="line">[certs] apiserver serving cert is signed <span class="keyword">for</span> DNS names [k8s-master kubernetes kubernetes.default kubernetes.default.svc kubernetes.default.svc.cluster.local] and IPs [10.96.0.1 172.16.98.2]</span><br><span class="line">[certs] Generating <span class="string">"apiserver-kubelet-client"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"front-proxy-ca"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"front-proxy-client"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"etcd/ca"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"etcd/server"</span> certificate and key</span><br><span class="line">[certs] etcd/server serving cert is signed <span class="keyword">for</span> DNS names [k8s-master localhost] and IPs [172.16.98.2 127.0.0.1 ::1]</span><br><span class="line">[certs] Generating <span class="string">"etcd/peer"</span> certificate and key</span><br><span class="line">[certs] etcd/peer serving cert is signed <span class="keyword">for</span> DNS names [k8s-master localhost] and IPs [172.16.98.2 127.0.0.1 ::1]</span><br><span class="line">[certs] Generating <span class="string">"etcd/healthcheck-client"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"apiserver-etcd-client"</span> certificate and key</span><br><span class="line">[certs] Generating <span class="string">"sa"</span> key and public key</span><br><span class="line">[kubeconfig] Using kubeconfig folder <span class="string">"/etc/kubernetes"</span></span><br><span class="line">[kubeconfig] Writing <span class="string">"admin.conf"</span> kubeconfig file</span><br><span class="line">[kubeconfig] Writing <span class="string">"kubelet.conf"</span> kubeconfig file</span><br><span class="line">[kubeconfig] Writing <span class="string">"controller-manager.conf"</span> kubeconfig file</span><br><span class="line">[kubeconfig] Writing <span class="string">"scheduler.conf"</span> kubeconfig file</span><br><span class="line">[kubelet-start] Writing kubelet environment file with flags to file <span class="string">"/var/lib/kubelet/kubeadm-flags.env"</span></span><br><span class="line">[kubelet-start] Writing kubelet configuration to file <span class="string">"/var/lib/kubelet/config.yaml"</span></span><br><span class="line">[kubelet-start] Starting the kubelet</span><br><span class="line">[control-plane] Using manifest folder <span class="string">"/etc/kubernetes/manifests"</span></span><br><span class="line">[control-plane] Creating static Pod manifest <span class="keyword">for</span> <span class="string">"kube-apiserver"</span></span><br><span class="line">[control-plane] Creating static Pod manifest <span class="keyword">for</span> <span class="string">"kube-controller-manager"</span></span><br><span class="line">[control-plane] Creating static Pod manifest <span class="keyword">for</span> <span class="string">"kube-scheduler"</span></span><br><span class="line">[etcd] Creating static Pod manifest <span class="keyword">for</span> <span class="built_in">local</span> etcd <span class="keyword">in</span> <span class="string">"/etc/kubernetes/manifests"</span></span><br><span class="line">[<span class="built_in">wait</span>-control-plane] Waiting <span class="keyword">for</span> the kubelet to boot up the control plane as static Pods from directory <span class="string">"/etc/kubernetes/manifests"</span>. This can take up to 4m0s</span><br><span class="line">[apiclient] All control plane components are healthy after 11.503773 seconds</span><br><span class="line">[upload-config] Storing the configuration used <span class="keyword">in</span> ConfigMap <span class="string">"kubeadm-config"</span> <span class="keyword">in</span> the <span class="string">"kube-system"</span> Namespace</span><br><span class="line">[kubelet] Creating a ConfigMap <span class="string">"kubelet-config-1.20"</span> <span class="keyword">in</span> namespace kube-system with the configuration <span class="keyword">for</span> the kubelets <span class="keyword">in</span> the cluster</span><br><span class="line">[upload-certs] Skipping phase. Please see --upload-certs</span><br><span class="line">[mark-control-plane] Marking the node k8s-master as control-plane by adding the labels <span class="string">"node-role.kubernetes.io/master=''"</span> and <span class="string">"node-role.kubernetes.io/control-plane='' (deprecated)"</span></span><br><span class="line">[mark-control-plane] Marking the node k8s-master as control-plane by adding the taints [node-role.kubernetes.io/master:NoSchedule]</span><br><span class="line">[bootstrap-token] Using token: ayqhud.254isto3e88zn9ba</span><br><span class="line">[bootstrap-token] Configuring bootstrap tokens, cluster-info ConfigMap, RBAC Roles</span><br><span class="line">[bootstrap-token] configured RBAC rules to allow Node Bootstrap tokens to get nodes</span><br><span class="line">[bootstrap-token] configured RBAC rules to allow Node Bootstrap tokens to post CSRs <span class="keyword">in</span> order <span class="keyword">for</span> nodes to get long term certificate credentials</span><br><span class="line">[bootstrap-token] configured RBAC rules to allow the csrapprover controller automatically approve CSRs from a Node Bootstrap Token</span><br><span class="line">[bootstrap-token] configured RBAC rules to allow certificate rotation <span class="keyword">for</span> all node client certificates <span class="keyword">in</span> the cluster</span><br><span class="line">[bootstrap-token] Creating the <span class="string">"cluster-info"</span> ConfigMap <span class="keyword">in</span> the <span class="string">"kube-public"</span> namespace</span><br><span class="line">[kubelet-finalize] Updating <span class="string">"/etc/kubernetes/kubelet.conf"</span> to point to a rotatable kubelet client certificate and key</span><br><span class="line">[addons] Applied essential addon: CoreDNS</span><br><span class="line">[addons] Applied essential addon: kube-proxy</span><br><span class="line"></span><br><span class="line">Your Kubernetes control-plane has initialized successfully!</span><br><span class="line"></span><br><span class="line">To start using your cluster, you need to run the following as a regular user:</span><br><span class="line"></span><br><span class="line">  mkdir -p <span class="variable">$HOME</span>/.kube</span><br><span class="line">  sudo cp -i /etc/kubernetes/admin.conf <span class="variable">$HOME</span>/.kube/config</span><br><span class="line">  sudo chown $(id -u):$(id -g) <span class="variable">$HOME</span>/.kube/config</span><br><span class="line"></span><br><span class="line">Alternatively, <span class="keyword">if</span> you are the root user, you can run:</span><br><span class="line"></span><br><span class="line">  <span class="built_in">export</span> KUBECONFIG=/etc/kubernetes/admin.conf</span><br><span class="line"></span><br><span class="line">You should now deploy a pod network to the cluster.</span><br><span class="line">Run <span class="string">"kubectl apply -f [podnetwork].yaml"</span> with one of the options listed at:</span><br><span class="line">  https://kubernetes.io/docs/concepts/cluster-administration/addons/</span><br><span class="line"></span><br><span class="line">Then you can join any number of worker nodes by running the following on each as root:</span><br><span class="line"></span><br><span class="line">kubeadm join 172.16.98.2:6443 --token ayqhud.254isto3e88zn9ba \</span><br><span class="line">    --discovery-token-ca-cert-hash sha256:9393b7ecc8c6f81d81eb362e4b74e2635eedda15e749872a74cb6ac77bc8c1e5</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;我们可以从中捕获关键信息，在日志末尾它提示我们，如果想要非root用户使用集群，需要做那些操作，如果是root用户需要怎么做，才能够使用集群，将工作节点加入集群执行怎样的命令。</p>
<ul>
<li>root用户下添加环境变量(~/.bash_profile) </li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> KUBECONFIG=/etc/kubernetes/admin.conf</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bash_profile</span><br></pre></td></tr></table></figure>
<h4 id="CNI-容器网络接口"><a href="#CNI-容器网络接口" class="headerlink" title="CNI(容器网络接口)"></a>CNI(容器网络接口)</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get pods --all-namespaces</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;执行上述命令会发现此时集群的 <code>coredns</code> 的状态一直是 <code>Pending</code>，这是因为系统就是这么设计的。 <code>kubeadm</code> 的网络供应商是中立的，因此管理员应该选择 安装 <code>pod</code> 的网络解决方案。 你必须完成 <code>Pod</code> 的网络配置，然后才能完全部署 <code>CoreDNS</code>。 在网络被配置好之前，<code>DNS</code> 组件会一直处于 <code>Pending</code> 状态。</p>
<h5 id="kube-router"><a href="#kube-router" class="headerlink" title="kube-router"></a>kube-router</h5><p>&emsp;&emsp;CNI的选择有很多，<code>Kube-router</code> 是 <code>Kubernetes</code> 的专用网络解决方案， 旨在提供高性能和易操作性。 <code>Kube-router</code> 提供了一个基于 <code>Linux LVS/IPVS</code> 的服务代理、一个基于 <code>Linux</code> 内核转发的无覆盖 <code>Pod-to-Pod</code> 网络解决方案和基于 <code>iptables/ipset</code> 的网络策略执行器。</p>
<p>&emsp;&emsp;这里选择采用 <code>Kube-router</code> 代替 <code>Kube-proxy</code> 并且提供网络解决方案。</p>
<p>&emsp;&emsp;首先需要下载 <code>Kube-router</code> 的yaml文件，我们需要访问 <code>raw.githubusercontent.com</code> 去获取，但是很有可能我们无法访问这个网站，那么我们先<a href="https://site.ip138.com/raw.Githubusercontent.com" target="_blank" rel="noopener">获取该域名对应的ip地址</a>然后在 <code>/etc/hosts</code> 文件中配置域名映射,这样我们就可以访问了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://raw.githubusercontent.com/cloudnativelabs/kube-router/master/daemonset/kubeadm-kuberouter.yaml</span><br><span class="line">wget https://raw.githubusercontent.com/cloudnativelabs/kube-router/master/daemonset/kubeadm-kuberouter-all-features.yaml</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面两个yaml，第一个是仅提供网络解决方案的，第二个还可以代替 <code>Kube-proxy</code>。</p>
<ul>
<li>应用下载的yaml</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f kubeadm-kuberouter-all-features.yaml</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;应用之后可以看一下状态，不出意外应该是启动不了。</p>
<p><img src="/kubernetes/k8s_kube-router_apply.png" alt="错误状态"></p>
<p>&emsp;&emsp;遇到这种情况我们可以和先前下载 <code>kube-proxy</code> 等镜像一样，通过阿里云的镜像仓库服务来下载。</p>
<p>&emsp;&emsp;下载好之后查看pod状态如果发现状态是 <code>CrashLoopBackOff</code>，此时我们可以查看日志</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl logs kube-router-bxl96 -n kube-system</span><br></pre></td></tr></table></figure>
<p><img src="/kubernetes/k8s_kube-router_pod_crashloopbackoff.png" alt="错误日志"></p>
<p>&emsp;&emsp;这说明我们初始化集群 <code>kubeadm init</code> 的时候没有使用参数 <code>--pod-network-cidr</code>，这里我们只能添加一下这个参数配置。</p>
<ul>
<li><code>kubeadm-config</code> 增加 <code>podSubnet</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl edit cm kubeadm-config -n kube-system</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">data:</span><br><span class="line">  ClusterConfiguration: |</span><br><span class="line">    networking:</span><br><span class="line">      podSubnet: 172.16.0.0/16</span><br></pre></td></tr></table></figure>
<ul>
<li><code>kube-controller-manager</code> 修改,增加 <code>allocate-node-cidrs</code> 和 <code>cluster-cidr</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/kubernetes/manifests/kube-controller-manager.yaml</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">spec:</span><br><span class="line">  containers:</span><br><span class="line">  - <span class="built_in">command</span>:</span><br><span class="line">    - --allocate-node-cidrs=<span class="literal">true</span></span><br><span class="line">    - --cluster-cidr=172.16.0.0/16</span><br></pre></td></tr></table></figure>
<ul>
<li>删除 <code>kube-router</code> pod</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl delete pod kube-router-bxl96 -n kube-system</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;删除pod后集群会立刻再创建一个pod，稍等一会可以观察pod的状态，理论上此时就没有什么问题了。<code>kube-router</code> 创建好了那么之前一直在 <code>ContainerCreating</code> 的 <code>coredns</code> 的pod随后状态也会变成 <code>running</code>。</p>
<p>&emsp;&emsp;由于这里使用的是 <code>kube-router</code> 的全特性，可以代替 <code>kube-proxy</code>, 所以我们删除 <code>kube-proxy</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl delete ds kube-proxy -n kube-system</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;至此，我们工作平面已经初步安装完成，现在需要加入工作节点壮大我们的集群。</p>
<p>&emsp;&emsp;使用weave插件</p>
<h5 id="weave"><a href="#weave" class="headerlink" title="weave"></a>weave</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f <span class="string">"https://cloud.weave.works/k8s/net?k8s-version=<span class="variable">$(kubectl version | base64 | tr -d '\n')</span>"</span></span><br></pre></td></tr></table></figure>
<h5 id="flannel"><a href="#flannel" class="headerlink" title="flannel"></a>flannel</h5><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">policy/v1beta1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">PodSecurityPolicy</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">psp.flannel.unprivileged</span></span><br><span class="line">  <span class="attr">annotations:</span></span><br><span class="line">    <span class="attr">seccomp.security.alpha.kubernetes.io/allowedProfileNames:</span> <span class="string">docker/default</span></span><br><span class="line">    <span class="attr">seccomp.security.alpha.kubernetes.io/defaultProfileName:</span> <span class="string">docker/default</span></span><br><span class="line">    <span class="attr">apparmor.security.beta.kubernetes.io/allowedProfileNames:</span> <span class="string">runtime/default</span></span><br><span class="line">    <span class="attr">apparmor.security.beta.kubernetes.io/defaultProfileName:</span> <span class="string">runtime/default</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">privileged:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">volumes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">configMap</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">secret</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">emptyDir</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">hostPath</span></span><br><span class="line">  <span class="attr">allowedHostPaths:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">pathPrefix:</span> <span class="string">"/etc/cni/net.d"</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">pathPrefix:</span> <span class="string">"/etc/kube-flannel"</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">pathPrefix:</span> <span class="string">"/run/flannel"</span></span><br><span class="line">  <span class="attr">readOnlyRootFilesystem:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment"># Users and groups</span></span><br><span class="line">  <span class="attr">runAsUser:</span></span><br><span class="line">    <span class="attr">rule:</span> <span class="string">RunAsAny</span></span><br><span class="line">  <span class="attr">supplementalGroups:</span></span><br><span class="line">    <span class="attr">rule:</span> <span class="string">RunAsAny</span></span><br><span class="line">  <span class="attr">fsGroup:</span></span><br><span class="line">    <span class="attr">rule:</span> <span class="string">RunAsAny</span></span><br><span class="line">  <span class="comment"># Privilege Escalation</span></span><br><span class="line">  <span class="attr">allowPrivilegeEscalation:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">defaultAllowPrivilegeEscalation:</span> <span class="literal">false</span></span><br><span class="line">  <span class="comment"># Capabilities</span></span><br><span class="line">  <span class="attr">allowedCapabilities:</span> <span class="string">['NET_ADMIN',</span> <span class="string">'NET_RAW'</span><span class="string">]</span></span><br><span class="line">  <span class="attr">defaultAddCapabilities:</span> <span class="string">[]</span></span><br><span class="line">  <span class="attr">requiredDropCapabilities:</span> <span class="string">[]</span></span><br><span class="line">  <span class="comment"># Host namespaces</span></span><br><span class="line">  <span class="attr">hostPID:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">hostIPC:</span> <span class="literal">false</span></span><br><span class="line">  <span class="attr">hostNetwork:</span> <span class="literal">true</span></span><br><span class="line">  <span class="attr">hostPorts:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">min:</span> <span class="number">0</span></span><br><span class="line">    <span class="attr">max:</span> <span class="number">65535</span></span><br><span class="line">  <span class="comment"># SELinux</span></span><br><span class="line">  <span class="attr">seLinux:</span></span><br><span class="line">    <span class="comment"># SELinux is unused in CaaSP</span></span><br><span class="line">    <span class="attr">rule:</span> <span class="string">'RunAsAny'</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ClusterRole</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">rbac.authorization.k8s.io/v1</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">flannel</span></span><br><span class="line"><span class="attr">rules:</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">apiGroups:</span> <span class="string">['extensions']</span></span><br><span class="line">  <span class="attr">resources:</span> <span class="string">['podsecuritypolicies']</span></span><br><span class="line">  <span class="attr">verbs:</span> <span class="string">['use']</span></span><br><span class="line">  <span class="attr">resourceNames:</span> <span class="string">['psp.flannel.unprivileged']</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">apiGroups:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">""</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">pods</span></span><br><span class="line">  <span class="attr">verbs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">get</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">apiGroups:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">""</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">nodes</span></span><br><span class="line">  <span class="attr">verbs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">list</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">watch</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">apiGroups:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">""</span></span><br><span class="line">  <span class="attr">resources:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">nodes/status</span></span><br><span class="line">  <span class="attr">verbs:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="string">patch</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ClusterRoleBinding</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">rbac.authorization.k8s.io/v1</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">flannel</span></span><br><span class="line"><span class="attr">roleRef:</span></span><br><span class="line">  <span class="attr">apiGroup:</span> <span class="string">rbac.authorization.k8s.io</span></span><br><span class="line">  <span class="attr">kind:</span> <span class="string">ClusterRole</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">flannel</span></span><br><span class="line"><span class="attr">subjects:</span></span><br><span class="line"><span class="bullet">-</span> <span class="attr">kind:</span> <span class="string">ServiceAccount</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">flannel</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">kube-system</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ServiceAccount</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">flannel</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">kube-system</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">ConfigMap</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">kube-flannel-cfg</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">kube-system</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">tier:</span> <span class="string">node</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">flannel</span></span><br><span class="line"><span class="attr">data:</span></span><br><span class="line">  <span class="attr">cni-conf.json:</span> <span class="string">|</span></span><br><span class="line">    <span class="string">&#123;</span></span><br><span class="line">      <span class="attr">"name":</span> <span class="string">"cbr0"</span><span class="string">,</span></span><br><span class="line">      <span class="attr">"cniVersion":</span> <span class="string">"0.3.1"</span><span class="string">,</span></span><br><span class="line">      <span class="attr">"plugins":</span> <span class="string">[</span></span><br><span class="line">        <span class="string">&#123;</span></span><br><span class="line">          <span class="attr">"type":</span> <span class="string">"flannel"</span><span class="string">,</span></span><br><span class="line">          <span class="attr">"delegate":</span> <span class="string">&#123;</span></span><br><span class="line">            <span class="attr">"hairpinMode":</span> <span class="literal">true</span><span class="string">,</span></span><br><span class="line">            <span class="attr">"isDefaultGateway":</span> <span class="literal">true</span></span><br><span class="line">          <span class="string">&#125;</span></span><br><span class="line">        <span class="string">&#125;,</span></span><br><span class="line">        <span class="string">&#123;</span></span><br><span class="line">          <span class="attr">"type":</span> <span class="string">"portmap"</span><span class="string">,</span></span><br><span class="line">          <span class="attr">"capabilities":</span> <span class="string">&#123;</span></span><br><span class="line">            <span class="attr">"portMappings":</span> <span class="literal">true</span></span><br><span class="line">          <span class="string">&#125;</span></span><br><span class="line">        <span class="string">&#125;</span></span><br><span class="line">      <span class="string">]</span></span><br><span class="line">    <span class="string">&#125;</span></span><br><span class="line">  <span class="attr">net-conf.json:</span> <span class="string">|</span></span><br><span class="line">    <span class="string">&#123;</span></span><br><span class="line">      <span class="attr">"Network":</span> <span class="string">"10.244.0.0/16"</span><span class="string">,</span></span><br><span class="line">      <span class="attr">"Backend":</span> <span class="string">&#123;</span></span><br><span class="line">        <span class="attr">"Type":</span> <span class="string">"vxlan"</span></span><br><span class="line">      <span class="string">&#125;</span></span><br><span class="line">    <span class="string">&#125;</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">apiVersion:</span> <span class="string">apps/v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">DaemonSet</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">kube-flannel-ds</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">kube-system</span></span><br><span class="line">  <span class="attr">labels:</span></span><br><span class="line">    <span class="attr">tier:</span> <span class="string">node</span></span><br><span class="line">    <span class="attr">app:</span> <span class="string">flannel</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">selector:</span></span><br><span class="line">    <span class="attr">matchLabels:</span></span><br><span class="line">      <span class="attr">app:</span> <span class="string">flannel</span></span><br><span class="line">  <span class="attr">template:</span></span><br><span class="line">    <span class="attr">metadata:</span></span><br><span class="line">      <span class="attr">labels:</span></span><br><span class="line">        <span class="attr">tier:</span> <span class="string">node</span></span><br><span class="line">        <span class="attr">app:</span> <span class="string">flannel</span></span><br><span class="line">    <span class="attr">spec:</span></span><br><span class="line">      <span class="attr">affinity:</span></span><br><span class="line">        <span class="attr">nodeAffinity:</span></span><br><span class="line">          <span class="attr">requiredDuringSchedulingIgnoredDuringExecution:</span></span><br><span class="line">            <span class="attr">nodeSelectorTerms:</span></span><br><span class="line">            <span class="bullet">-</span> <span class="attr">matchExpressions:</span></span><br><span class="line">              <span class="bullet">-</span> <span class="attr">key:</span> <span class="string">kubernetes.io/os</span></span><br><span class="line">                <span class="attr">operator:</span> <span class="string">In</span></span><br><span class="line">                <span class="attr">values:</span></span><br><span class="line">                <span class="bullet">-</span> <span class="string">linux</span></span><br><span class="line">      <span class="attr">hostNetwork:</span> <span class="literal">true</span></span><br><span class="line">      <span class="attr">priorityClassName:</span> <span class="string">system-node-critical</span></span><br><span class="line">      <span class="attr">tolerations:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">operator:</span> <span class="string">Exists</span></span><br><span class="line">        <span class="attr">effect:</span> <span class="string">NoSchedule</span></span><br><span class="line">      <span class="attr">serviceAccountName:</span> <span class="string">flannel</span></span><br><span class="line">      <span class="attr">initContainers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">install-cni</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">quay.io/coreos/flannel:v0.14.0-rc1</span></span><br><span class="line">        <span class="attr">command:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">cp</span></span><br><span class="line">        <span class="attr">args:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">-f</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">/etc/kube-flannel/cni-conf.json</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">/etc/cni/net.d/10-flannel.conflist</span></span><br><span class="line">        <span class="attr">volumeMounts:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">cni</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/etc/cni/net.d</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">flannel-cfg</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/etc/kube-flannel/</span></span><br><span class="line">      <span class="attr">containers:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">kube-flannel</span></span><br><span class="line">        <span class="attr">image:</span> <span class="string">quay.io/coreos/flannel:v0.14.0-rc1</span></span><br><span class="line">        <span class="attr">command:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">/opt/bin/flanneld</span></span><br><span class="line">        <span class="attr">args:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">--ip-masq</span></span><br><span class="line">        <span class="bullet">-</span> <span class="string">--kube-subnet-mgr</span></span><br><span class="line">        <span class="attr">resources:</span></span><br><span class="line">          <span class="attr">requests:</span></span><br><span class="line">            <span class="attr">cpu:</span> <span class="string">"100m"</span></span><br><span class="line">            <span class="attr">memory:</span> <span class="string">"50Mi"</span></span><br><span class="line">          <span class="attr">limits:</span></span><br><span class="line">            <span class="attr">cpu:</span> <span class="string">"100m"</span></span><br><span class="line">            <span class="attr">memory:</span> <span class="string">"50Mi"</span></span><br><span class="line">        <span class="attr">securityContext:</span></span><br><span class="line">          <span class="attr">privileged:</span> <span class="literal">false</span></span><br><span class="line">          <span class="attr">capabilities:</span></span><br><span class="line">            <span class="attr">add:</span> <span class="string">["NET_ADMIN",</span> <span class="string">"NET_RAW"</span><span class="string">]</span></span><br><span class="line">        <span class="attr">env:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">POD_NAME</span></span><br><span class="line">          <span class="attr">valueFrom:</span></span><br><span class="line">            <span class="attr">fieldRef:</span></span><br><span class="line">              <span class="attr">fieldPath:</span> <span class="string">metadata.name</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">POD_NAMESPACE</span></span><br><span class="line">          <span class="attr">valueFrom:</span></span><br><span class="line">            <span class="attr">fieldRef:</span></span><br><span class="line">              <span class="attr">fieldPath:</span> <span class="string">metadata.namespace</span></span><br><span class="line">        <span class="attr">volumeMounts:</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">run</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/run/flannel</span></span><br><span class="line">        <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">flannel-cfg</span></span><br><span class="line">          <span class="attr">mountPath:</span> <span class="string">/etc/kube-flannel/</span></span><br><span class="line">      <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">run</span></span><br><span class="line">        <span class="attr">hostPath:</span></span><br><span class="line">          <span class="attr">path:</span> <span class="string">/run/flannel</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">cni</span></span><br><span class="line">        <span class="attr">hostPath:</span></span><br><span class="line">          <span class="attr">path:</span> <span class="string">/etc/cni/net.d</span></span><br><span class="line">      <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">flannel-cfg</span></span><br><span class="line">        <span class="attr">configMap:</span></span><br><span class="line">          <span class="attr">name:</span> <span class="string">kube-flannel-cfg</span></span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl apply -f flannel.yaml</span><br></pre></td></tr></table></figure>
<h4 id="加入工作节点"><a href="#加入工作节点" class="headerlink" title="加入工作节点"></a>加入工作节点</h4><p>&emsp;&emsp;这一步需要控制平面和工作节点配合完成。</p>
<blockquote>
<p>控制平面：用于管理集群的机器<br>工作节点：运行应用服务的机器</p>
</blockquote>
<p>&emsp;&emsp;将工作节点加入集群需要令牌认证，令牌默认24小时失效。我们先查询当前未失效的令牌</p>
<ul>
<li>控制平面</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm token list</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果shell中输出了令牌信息，那么我们就用这个令牌就可以让工作节点加入集群，若没有输出令牌信息，说明已有的令牌都失效了，需要重新生成令牌。</p>
<ul>
<li>控制平面</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm token create</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;此外，我们还需要获取ca证书sha256编码hash值</p>
<ul>
<li>控制平面</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">openssl x509 -pubkey -<span class="keyword">in</span> /etc/kubernetes/pki/ca.crt | openssl rsa -pubin -outform der 2&gt;/dev/null | openssl dgst -sha256 -hex | sed <span class="string">'s/^.* //'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;将工作节点加入集群，命令中的ip地址是控制平面的内网ip，端口是 Kubernetes API 服务器的端口</p>
<ul>
<li>工作节点</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm join 172.16.98.2:6443 --token udl1vd.ln1kzbtertnzbfmi --discovery-token-ca-cert-hash sha256:9393b7ecc8c6f81d81eb362e4b74e2635eedda15e749872a74cb6ac77bc8c1e5</span><br></pre></td></tr></table></figure>
<p><img src="/kubernetes/k8s_join_node_to_master.png" alt="加入节点错误"></p>
<p>&emsp;&emsp;出错了，说明我们工作节点的环境准备不是很充分，此时我们按照提示一步步解决。</p>
<ul>
<li>未识别的的主机名,编辑工作节点的 <code>/etc/hosts</code> 文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">172.16.98.6 k8s-node-1</span><br></pre></td></tr></table></figure>
<ul>
<li>编辑工作节点文件<code>/etc/sysctl.conf</code>，添加设置然后重启系统</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">net.bridge.bridge-nf-call-iptables = 1</span><br></pre></td></tr></table></figure>
<ul>
<li>工作节点</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubeadm join 172.16.98.2:6443 --token udl1vd.ln1kzbtertnzbfmi --discovery-token-ca-cert-hash sha256:9393b7ecc8c6f81d81eb362e4b74e2635eedda15e749872a74cb6ac77bc8c1e5</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;依次将所有工作节点加入集群中，接下来在查看集群中的节点信息。</p>
<ul>
<li>控制平面</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl get node</span><br></pre></td></tr></table></figure>
<h3 id="集群参数设置"><a href="#集群参数设置" class="headerlink" title="集群参数设置"></a>集群参数设置</h3><h4 id="kube-proxy模式切换"><a href="#kube-proxy模式切换" class="headerlink" title="kube-proxy模式切换"></a>kube-proxy模式切换</h4><p>&emsp;&emsp;<code>ipvs</code>相比较<code>iptables</code>来说性能更好，我们将<code>kube-proxy</code>的模式切换成<code>ipvs</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kubectl edit cm -n kube-system</span><br></pre></td></tr></table></figure>
<p>将<code>data.config.conf.mode</code>设置成<code>ipvs</code>保存退出即可。</p>
]]></content>
      <categories>
        <category>kubernetes</category>
      </categories>
      <tags>
        <tag>k8s</tag>
      </tags>
  </entry>
  <entry>
    <title>lambda</title>
    <url>/lambda.html</url>
    <content><![CDATA[<p>&emsp;&emsp;<code>lambda表达式</code>是一个可传递的代码块。</p>
<a id="more"></a>
<h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><ul>
<li>基本语法：参数，箭头（-&gt;）以及一个表达式或者代码块。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(String msg, String str) -&gt; <span class="keyword">return</span> msg.length()&gt;str.length()</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(String msg, String str) -&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span>(msg.length()&gt;str.length() &gt; <span class="number">0</span>) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(msg.length()&gt;str.length() == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<em>如果代码块在有些分支有返回值，有些分支没有返回值是不合法的。返回值的类型无需指定，lambda表达式的返回值类型总是有上下文推导而出。lambda表达式可以捕获外围作用域中变量的值，但是所捕获的变量必须实际上是最终变量</em></p>
<ul>
<li>无参表达式：提供空括号</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">() -&gt; System.out.println(<span class="string">"no arguments lambda"</span>)</span><br></pre></td></tr></table></figure>
<ul>
<li>忽略参数类型：若参数类型可推导，可忽略参数类型。若只有一个参数且参数类型可推导，可省略括号。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">(msg, str) -&gt; <span class="keyword">return</span> msg.length() &gt; str.length()</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">msg -&gt; System.out.println(msg)</span><br></pre></td></tr></table></figure>
<h3 id="函数式接口"><a href="#函数式接口" class="headerlink" title="函数式接口"></a>函数式接口</h3><ul>
<li><p>定义：只有一个抽象方法的接口。自己设计接口时，如果接口只有一个抽象方法，那么可以使用注解<code>@FunctionalInterface</code>标记这个接口。这样，当你向这个接口再次添加一个抽象方法时，编译器会产生一个错误。另外javadoc页会指出此接口为函数式接口。</p>
</li>
<li><p>demo：返回指定字符串10倍的长度</p>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 函数式接口</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">LengthInterface</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">renderLength</span><span class="params">(String msg)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* 匿名内部类方式实现</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">innerClass</span><span class="params">()</span></span>&#123;</span><br><span class="line">    System.out.println(<span class="keyword">new</span> LengthInterface() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">renderLength</span><span class="params">(String msg)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> msg.length()*<span class="number">10</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;.renderLength(<span class="string">"ferry"</span>));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* lambda表达式方式实现</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">lambda</span><span class="params">()</span></span>&#123;</span><br><span class="line">    LengthInterface lengthInterface = msg -&gt; msg.length()*<span class="number">10</span>;</span><br><span class="line">    System.out.println(lengthInterface.renderLength(<span class="string">"ferry"</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/lambda/lambda_functionalInterface.png" alt="初始化git项目方式2"></p>
<p>&emsp;&emsp;定义一个函数式接口，这个接口只有一个抽象方法，这个抽象方法的用途是希望获取到指定的字符串进过渲染后的长度。在lambda表达式出现之前，我们想要实现这样的功能，最好的做法是通过匿名内部类来实现，但是在这里，我们可以让函数式接口的引用指向一个lambda表达式，通过lambda表达式定义渲染长度规则，然后在通过引用调用函数式接口的抽象方法。很显然，虽然内部类和lambda都可以实现功能，但是不管是从代码的简洁度，可读性还是从性能对比上（内部类的类加载，解析，初始化等高于lambda表达式的性能开销）lambda表达式都是我们的首选方案。</p>
<ul>
<li>常用函数式接口</li>
</ul>
<table>
<thead>
<tr>
<th>函数式接口</th>
<th>参数类型</th>
<th>返回类型</th>
<th>抽象方法</th>
<th>描述</th>
<th>其他方法</th>
</tr>
</thead>
<tbody>
<tr>
<td>Runnable</td>
<td>无</td>
<td>void</td>
<td>run</td>
<td>作为无参数或返回值的动作运行</td>
<td></td>
</tr>
<tr>
<td>Supplier<t></t></td>
<td>无</td>
<td>T</td>
<td>get</td>
<td>提供一个T类型的值</td>
<td></td>
</tr>
<tr>
<td>Consumer<t></t></td>
<td>T</td>
<td>void</td>
<td>accept</td>
<td>处理一个T类铟的值</td>
<td>andThen</td>
</tr>
<tr>
<td>BiConsumer&lt;T,U&gt;</td>
<td>T,U</td>
<td>void</td>
<td>accept</td>
<td>处理T和U类型的值</td>
<td>andThen</td>
</tr>
<tr>
<td>Function&lt;T,R&gt;</td>
<td>T</td>
<td>R</td>
<td>apply</td>
<td>有一个T类型参数的函数</td>
<td>compose, andThen, identity</td>
</tr>
<tr>
<td>BiFunction&lt;T,U,R&gt;</td>
<td>T,U</td>
<td>R</td>
<td>apply</td>
<td>有T和U类型参数的函数</td>
<td>andThen</td>
</tr>
<tr>
<td>UnaryOperator<t></t></td>
<td>T</td>
<td>T</td>
<td>apply</td>
<td>类型T上的一元操作符</td>
<td>compose, andThen, identity</td>
</tr>
<tr>
<td>BinaryOperator<t></t></td>
<td>T,T</td>
<td>T</td>
<td>apply</td>
<td>类型T上的二元操作符</td>
<td>andThen, maxBy, minBy</td>
</tr>
<tr>
<td>Predicate<t></t></td>
<td>T</td>
<td>boolean</td>
<td>test</td>
<td>布尔值函数</td>
<td>and, or, negate, isEqual</td>
</tr>
<tr>
<td>BiPredicate&lt;T,U&gt;</td>
<td>T,U</td>
<td>boolean</td>
<td>test</td>
<td>有两个参数的布尔值函数</td>
<td>and, or, negate</td>
</tr>
</tbody>
</table>
<ul>
<li>基本类型的函数式接口</li>
</ul>
<table>
<thead>
<tr>
<th>函数式接口</th>
<th>参数类型</th>
<th>返回类型</th>
<th>抽象方法 </th>
</tr>
</thead>
<tbody>
<tr>
<td>BooleanSupplier</td>
<td>none</td>
<td>boolean</td>
<td>getAsBoolean</td>
</tr>
<tr>
<td>PSupplier</td>
<td>none</td>
<td>p</td>
<td>getAsP</td>
</tr>
<tr>
<td>PConsumer</td>
<td>p</td>
<td>void</td>
<td>accept</td>
</tr>
<tr>
<td>ObjPConsumer&lt; T&gt;</td>
<td>T,p</td>
<td>void</td>
<td>accept</td>
</tr>
<tr>
<td>PFunction<t></t></td>
<td>p</td>
<td>T</td>
<td>apply</td>
</tr>
<tr>
<td>PToQFunction</td>
<td>p</td>
<td>q</td>
<td>applyAsQ</td>
</tr>
<tr>
<td>ToPFunction<t></t></td>
<td>T</td>
<td>p</td>
<td>applyAsP</td>
</tr>
<tr>
<td>ToPBiFunction&lt;T,U&gt;</td>
<td>T,U</td>
<td>p</td>
<td>applyAsP</td>
</tr>
<tr>
<td>PUnaryOperator</td>
<td>p</td>
<td>p</td>
<td>testAsP</td>
</tr>
<tr>
<td>PBinaryOperator</td>
<td>p,p</td>
<td>p</td>
<td>testAsP</td>
</tr>
<tr>
<td>PPredicate</td>
<td>p</td>
<td>boolean</td>
<td>test</td>
</tr>
</tbody>
</table>
<p><em>注：p,q为int, long, double;P, Q为Integer, Long, Double</em></p>
<h3 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h3><p>&emsp;&emsp;将现有的方法传递给函数式接口，等价于lambda表达式</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String::length  &lt;==&gt; msg -&gt; msg.length()</span><br><span class="line">(String msg) -&gt; System.out.println(msg)  &lt;==&gt; System.out::println</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;语法：用操作符<code>::</code>分割对象名或类名与方法名，有三种用法，第一：对象::非静态方法名；第二：类名::静态方法名；第三：类名::非静态方法名。前两种方式，方法引用等价于提供方法参数的lambda表达式。第三种情况，第一个参数将成为方法的目标，例如，(msg, str) -&gt; msg.equals(str) 等同于 String::equals。</p>
<h3 id="构造器引用"><a href="#构造器引用" class="headerlink" title="构造器引用"></a>构造器引用</h3><p>&emsp;&emsp;和方法引用很类似，只不过方法名为new，具体方式为：类名::new，例如：String::new，具体使用类的哪一个构造器取决于上下文。数组的构造器引用：String[]::new，它有一个参数，即数组的长度，等价于length -&gt; new String[length]。</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>lambda</tag>
      </tags>
  </entry>
  <entry>
    <title>linux常用指令</title>
    <url>/linux-order.html</url>
    <content><![CDATA[<p>&emsp;&emsp;linux常用的指令，用到一个记一个！！！</p>
<a id="more"></a>
<h3 id="yum"><a href="#yum" class="headerlink" title="yum"></a>yum</h3><p>&emsp;&emsp;Yum是一个用来安装软件的软件，使用Yum指令可以安装以<code>.rpm</code>结尾的软件包，实际上很多软件包又依赖于其他软件包，众多的软件包之间的依赖关系复杂，使用rpm指令安装略显复杂，所以有了Yum的诞生，使用Yum可以很方便的安装你所需要的软件包，他会自动下载安装目标软件需要的依赖。</p>
<h4 id="查询软件包"><a href="#查询软件包" class="headerlink" title="查询软件包"></a>查询软件包</h4><ul>
<li>根据关键字查询可安装的软件包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum search mysql</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux-yum-search.png" alt="运行结果"></p>
<ul>
<li>根据关键字检索系统中已安装的软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list installed mysql-community-client</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux-yum-list.png" alt="运行结果"></p>
<ul>
<li>结合通配符检索系统中已安装的软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list installed mysql-community*</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux-yum-list-i.png" alt="运行结果"></p>
<p>&emsp;&emsp;很奇怪，我使用<code>yum list installed mysql*</code>进行检索就没有找到，多打几个字可以检索到软件，不懂。。</p>
<ul>
<li>显示所有可更新的软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list updates</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux-yum-list-updates.png" alt="运行结果"></p>
<ul>
<li>显示详细信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum info mysql-community-client*</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux-yum-info.png" alt="运行结果"></p>
<h4 id="安装软件包"><a href="#安装软件包" class="headerlink" title="安装软件包"></a>安装软件包</h4><p>&emsp;&emsp;使用yum指令进行软件的安装，可以使用<code>-y</code>（安装过程中提示全部选择’y’），<code>-p</code>（不显示安装过程）或者<code>-h</code>（帮助）。</p>
<ul>
<li>安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install mysql-community-server</span><br></pre></td></tr></table></figure>
<h4 id="更新软件"><a href="#更新软件" class="headerlink" title="更新软件"></a>更新软件</h4><ul>
<li>更新全部软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum update</span><br></pre></td></tr></table></figure>
<ul>
<li>更新指定软件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum update mysql-community-client</span><br></pre></td></tr></table></figure>
<h4 id="移除软件包"><a href="#移除软件包" class="headerlink" title="移除软件包"></a>移除软件包</h4><ul>
<li>移除</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum remove mysql-community-client</span><br></pre></td></tr></table></figure>
<h4 id="清除缓存（-var-cache-yum）"><a href="#清除缓存（-var-cache-yum）" class="headerlink" title="清除缓存（/var/cache/yum）"></a>清除缓存（/var/cache/yum）</h4><ul>
<li>清除软件包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum clean packages</span><br></pre></td></tr></table></figure>
<ul>
<li>清除headers</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum clean headers</span><br></pre></td></tr></table></figure>
<ul>
<li>一步到位直接清除所有</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum clean all</span><br></pre></td></tr></table></figure>
<h3 id="查看网卡信息"><a href="#查看网卡信息" class="headerlink" title="查看网卡信息"></a>查看网卡信息</h3><h4 id="linux"><a href="#linux" class="headerlink" title="linux"></a>linux</h4><p>&emsp;&emsp;<a href="https://blog.51cto.com/13150617/1963833" target="_blank" rel="noopener">查看网卡信息</a></p>
<h3 id="locate"><a href="#locate" class="headerlink" title="locate"></a>locate</h3><ul>
<li>安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install mlocate</span><br></pre></td></tr></table></figure>
<ul>
<li>更新索引数据库</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">updatedb</span><br></pre></td></tr></table></figure>
<ul>
<li>搜索</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">locate -i /etc/mysql</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;搜索etc目录下的包含mysql的目录或者文件，不区分大小写，去掉-i区分大小写。</p>
<ul>
<li>mac下更新索引数据库</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo /usr/libexec/locate.updatedb</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;mac下<code>locate</code>的updatedb指令在这个位置,所以直接运行这个指令就好了,使用<code>sudo</code>命令扩大权限,使得索引数据库更加完善.</p>
<p>&emsp;&emsp;为了方便起见,不至于每次更新数据库都执行这么一长串命令,可以设置别名.</p>
<ul>
<li>设置别名</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">'alias updatedb="sudo /usr/libexec/locate.updatedb"'</span> &gt;&gt; ~/.bash_profile</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;此时直接使用<code>updatedb</code>即可执行更新索引数据库的操作</p>
<h3 id="MAC本地使用ssh连接服务器"><a href="#MAC本地使用ssh连接服务器" class="headerlink" title="MAC本地使用ssh连接服务器"></a>MAC本地使用ssh连接服务器</h3><p>&emsp;&emsp;服务器端设置:修改配置文件<code>/etc/ssh/sshd_config</code>,将<code>PubkeyAuthentication</code>设置为yes，允许使用公密钥登陆，将<code>PasswordAuthentication</code>设置为yes，允许使用密码登陆</p>
<ul>
<li>一般指令</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh user@ip</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;端口号默认是22，如果服务器改了端口号，那么需要显式指定端口<code>ssh -p port user@ip</code>，会车之后会要求输入密码，ssh认证完成之后如果是第一次连接，那么会弹出提示，大意是第一次连接这个服务器，问你要不要继续连接，当输入<code>yes</code>后会将这个服务器的信息保存到<code>~/.ssh/known_hosts</code>文件中，下次登陆该服务器就不会有提示了。</p>
<p>&emsp;&emsp;采用别名登陆：本地的<code>~/.ssh/config</code>文件(若无则新建文件)中添加以下内容。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Host ferry</span><br><span class="line">HostName ip</span><br><span class="line">Port 22</span><br><span class="line">User root</span><br><span class="line">IdentityFile    /usr/<span class="built_in">local</span>/aliyun/SSH-ferry-aliyun-private</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>Host</code>设置服务器的别名，配置好后可以通过别名来进行登陆;<code>HostName</code>为IP地址;<code>Port</code>为端口号，ssh的端口号默认为22;<code>User</code>为要登陆的用户;<code>IdentityFile</code>为密钥的绝对路径,如果没有给定密钥路径，连接时会要求输入密码。</p>
<ul>
<li>连接指令</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh ferry</span><br></pre></td></tr></table></figure>
<h3 id="用-Sublime-Text-编辑-SSH-远程文件-MAC"><a href="#用-Sublime-Text-编辑-SSH-远程文件-MAC" class="headerlink" title="用 Sublime Text 编辑 SSH 远程文件(MAC)"></a>用 Sublime Text 编辑 SSH 远程文件(MAC)</h3><p>&emsp;&emsp;本地的<code>~/.ssh/config</code>文件中添加以下内容</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Host ferry</span><br><span class="line">HostName ip</span><br><span class="line">Port 22</span><br><span class="line">User root</span><br><span class="line">IdentityFile    /usr/<span class="built_in">local</span>/aliyun/SSH-ferry-aliyun-private</span><br><span class="line">RemoteForward 52698 localhost:52698</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;登陆远程服务器，然后执行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo wget -O /usr/<span class="built_in">local</span>/bin/subl \</span><br><span class="line">https://raw.github.com/aurora/rmate/master/rmate</span><br><span class="line">sudo chmod a+x /usr/<span class="built_in">local</span>/bin/subl</span><br><span class="line">sudo mv /usr/<span class="built_in">local</span>/bin/subl ~bin/</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;安装<code>rmate</code>并将其重命名为subl.最后让subl这个指令全局可用。此时，就可以用ssh连接到服务器，然后用<code>subl [-w] fileName</code>在本地用sublime打开文件进行编辑,-w参数会让shell不可操作，必须关闭打开的文件后才可以继续执行命令。</p>
<h3 id="SSH自动断开连接问题"><a href="#SSH自动断开连接问题" class="headerlink" title="SSH自动断开连接问题"></a>SSH自动断开连接问题</h3><p>&emsp;&emsp;使用ssh连接服务器，如果超过一定时间无操作就会断开连接，就很烦。</p>
<ul>
<li>编辑<code>/etc/ssh/sshd_config</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ClientAliveInterval 60</span><br><span class="line">ClientAliveCountMax 3</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;以上两个参数表示每隔60秒服务端向客户端发送一次心跳，若客户端3次无响应则断开连接。</p>
<h3 id="用户的创建与删除"><a href="#用户的创建与删除" class="headerlink" title="用户的创建与删除"></a>用户的创建与删除</h3><ul>
<li>创建用户(adduser/useradd)</li>
</ul>
<p>&emsp;&emsp;在CentOs下创建用户使用两个指令没有区别，在home下自动创建目录，没有设置密码，需要使用passwd命令修改密码。而在Ubuntu下useradd与adduser有所不同,adduser在使用该命令创建用户是会在/home下自动创建与用户名同名的用户目录，系统shell版本，会在创建时会提示输入密码，更加友好。useradd在使用该命令创建用户是不会在/home下自动创建与用户名同名的用户目录，而且不会自动选择shell版本，也没有设置密码，那么这个用户是不能登录的，需要使用passwd命令修改密码。</p>
<ul>
<li>删除用户</li>
</ul>
<p>&emsp;&emsp;userdel只能删除用户，并不会删除相关的目录文件。userdel -r 可以删除用户及相关目录。</p>
<h3 id="文件、目录权限"><a href="#文件、目录权限" class="headerlink" title="文件、目录权限"></a>文件、目录权限</h3><p>&emsp;&emsp;Linux系统是多用户操作系统，因此其中的每个文件和目录都有访问许可权限，用它来确定谁能通过何种方式对文件和目录进行访问和操作。</p>
<ul>
<li>权限的分类</li>
</ul>
<blockquote>
<p><code>r</code>读权限：可以打开文件、目录读取查看；<br><code>w</code>写权限：对文件、目录可以编写更改；<br><code>x</code>执行权限：对文件可执行(可执行文件)、对目录可查找该目录下的内容；<br><code>-</code>没有权限。</p>
</blockquote>
<ul>
<li>权限所属对象</li>
</ul>
<blockquote>
<p><code>u</code>拥有者：生成文件或目录时登录的当前人，权限最高；<br><code>g</code>同组人：系统管理员分配的同组的一个或几个人；<br><code>o</code>其他人：除拥有者，同组人以外的人；<br><code>a</code>所有人：包括拥有者、同组人及其他人。</p>
</blockquote>
<h4 id="chmod"><a href="#chmod" class="headerlink" title="chmod"></a>chmod</h4><ul>
<li>使用字母表示权限</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod g+rwx,o+r dir</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux_file_permissions.png" alt="设置权限"></p>
<ul>
<li>使用数字表示权限</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod 555 1.txt</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/linux_permission_num1.png" alt="设置权限"></p>
<p><img src="/linux-order/linux_permission_num2.png" alt="设置权限"></p>
<h4 id="chown"><a href="#chown" class="headerlink" title="chown"></a>chown</h4><ul>
<li>更改某个文件或目录的属主和属组，可用于授权[- R 递归式地改动指定目录及其下的所有子目录和文件的拥有者。]。</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chown -R ferry:ferry 1.txt</span><br></pre></td></tr></table></figure>
<h3 id="系统服务管理器指令systemctl"><a href="#系统服务管理器指令systemctl" class="headerlink" title="系统服务管理器指令systemctl"></a>系统服务管理器指令<code>systemctl</code></h3><p>&emsp;&emsp;systemctl命令是系统服务管理器指令，它实际上将 service 和 chkconfig 这两个命令组合到一起。</p>
<h4 id="常用指令"><a href="#常用指令" class="headerlink" title="常用指令"></a>常用指令</h4><ul>
<li>启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>重启服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl restart tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>查看服务状态</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>关闭服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl stop tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>设置开机自启</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>禁用开机自启</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">disable</span> tomcat.service</span><br></pre></td></tr></table></figure>
<ul>
<li>查看所有已启动的服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl list -units --<span class="built_in">type</span>=service</span><br></pre></td></tr></table></figure>
<h4 id="将服务配置成系统服务"><a href="#将服务配置成系统服务" class="headerlink" title="将服务配置成系统服务"></a>将服务配置成系统服务</h4><ul>
<li>创建服务的配置文件，以redis为例</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /lib/systemd/system/redisd.service</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础信息</span></span><br><span class="line">[Unit]</span><br><span class="line"><span class="comment"># 描述</span></span><br><span class="line">Description=Redis</span><br><span class="line"><span class="comment"># 在某个服务启动后启动</span></span><br><span class="line">After=network.target</span><br><span class="line"></span><br><span class="line"><span class="comment"># 服务信息</span></span><br><span class="line">[Service]</span><br><span class="line">Type=forking</span><br><span class="line"><span class="comment"># 启动服务的命令</span></span><br><span class="line">ExecStart=/usr/<span class="built_in">local</span>/bin/redis-server /usr/<span class="built_in">local</span>/bin/usr/<span class="built_in">local</span>/redis-4.0.14/redis.conf</span><br><span class="line"><span class="comment"># 终止服务的命令</span></span><br><span class="line">ExecStop=/usr/<span class="built_in">local</span>/bin/redis-cli -h 127.0.0.1 -p 6379 shutdown</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装相关信息</span></span><br><span class="line">[Install]</span><br><span class="line"><span class="comment"># WantedBy 是以哪种方式启动：multi-user.target表明当系统以多用户方式（默认的运行级别）启动时，这个服务需要被自动运行。</span></span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;具体这个配置文件还可以指定哪些参数可以自行百度。</p>
<ul>
<li>可执行权限</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x /lib/systemd/system/redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>刷新配置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;注意，每次修改<code>/lib/systemd/system/*.service</code>文件都需要刷新配置才可以进行启动。</p>
<ul>
<li>启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start redisd.service</span><br></pre></td></tr></table></figure>
<h3 id="wget"><a href="#wget" class="headerlink" title="wget"></a>wget</h3><p>&emsp;&emsp;Linux系统中的wget是一个下载文件的工具，它用在命令行下。wget支持HTTP，HTTPS和FTP协议，可以使用HTTP代理。wget 非常稳定，它在带宽很窄的情况下和不稳定网络中有很强的适应性.如果是由于网络的原因下载失败，wget会不断的尝试，直到整个文件下载完毕。如果是服务器打断下载过程，它会再次联到服务器上从停止的地方继续下载。这对从那些限定了链接时间的服务器上下载大文件非常有用。</p>
<ul>
<li>命令参数</li>
</ul>
<p><strong>启动参数</strong></p>
<p>-V, –version 显示wget的版本后退出</p>
<p>-h, –help 打印语法帮助</p>
<p>-b, –background 启动后转入后台执行</p>
<p>-e, –execute=COMMAND 执行`.wgetrc’格式的命令，wgetrc格式参见/etc/wgetrc或~/.wgetrc</p>
<p><strong>记录和输入文件参数</strong></p>
<p>-o, –output-file=FILE 把记录写到FILE文件中</p>
<p>-a, –append-output=FILE 把记录追加到FILE文件中</p>
<p>-d, –debug 打印调试输出</p>
<p>-q, –quiet 安静模式(没有输出)</p>
<p>-v, –verbose 冗长模式(这是缺省设置)</p>
<p>-nv, –non-verbose 关掉冗长模式，但不是安静模式</p>
<p>-i, –input-file=FILE 下载在FILE文件中出现的URLs</p>
<p>-F, –force-html 把输入文件当作HTML格式文件对待</p>
<p>-B, –base=URL 将URL作为在-F -i参数指定的文件中出现的相对链接的前缀</p>
<p>–sslcertfile=FILE 可选客户端证书</p>
<p>–sslcertkey=KEYFILE 可选客户端证书的KEYFILE</p>
<p>–egd-file=FILE 指定EGD socket的文件名</p>
<p><strong>下载参数</strong></p>
<p>–bind-address=ADDRESS 指定本地使用地址(主机名或IP，当本地有多个IP或名字时使用)</p>
<p>-t, –tries=NUMBER 设定最大尝试链接次数(0 表示无限制).</p>
<p>-O –output-document=FILE 把文档写到FILE文件中</p>
<p>-nc, –no-clobber 不要覆盖存在的文件或使用.#前缀</p>
<p>-c, –continue 接着下载没下载完的文件</p>
<p>–progress=TYPE 设定进程条标记</p>
<p>-N, –timestamping 不要重新下载文件除非比本地文件新</p>
<p>-S, –server-response 打印服务器的回应</p>
<p>–spider 不下载任何东西</p>
<p>-T, –timeout=SECONDS 设定响应超时的秒数</p>
<p>-w, –wait=SECONDS 两次尝试之间间隔SECONDS秒</p>
<p>–waitretry=SECONDS 在重新链接之间等待1…SECONDS秒</p>
<p>–random-wait 在下载之间等待0…2*WAIT秒</p>
<p>-Y, –proxy=on/off 打开或关闭代理</p>
<p>-Q, –quota=NUMBER 设置下载的容量限制</p>
<p>–limit-rate=RATE 限定下载输率</p>
<p><strong>目录参数</strong></p>
<p>-nd –no-directories 不创建目录</p>
<p>-x, –force-directories 强制创建目录</p>
<p>-nH, –no-host-directories 不创建主机目录</p>
<p>-P, –directory-prefix=PREFIX 将文件保存到目录 PREFIX/…</p>
<p>–cut-dirs=NUMBER 忽略 NUMBER层远程目录</p>
<p><strong>HTTP 选项参数</strong></p>
<p>–http-user=USER 设定HTTP用户名为 USER.</p>
<p>–http-passwd=PASS 设定http密码为 PASS</p>
<p>-C, –cache=on/off 允许/不允许服务器端的数据缓存 (一般情况下允许)</p>
<p>-E, –html-extension 将所有text/html文档以.html扩展名保存</p>
<p>–ignore-length 忽略 `Content-Length’头域</p>
<p>–header=STRING 在headers中插入字符串 STRING</p>
<p>–proxy-user=USER 设定代理的用户名为 USER</p>
<p>–proxy-passwd=PASS 设定代理的密码为 PASS</p>
<p>–referer=URL 在HTTP请求中包含 `Referer: URL’头</p>
<p>-s, –save-headers 保存HTTP头到文件</p>
<p>-U, –user-agent=AGENT 设定代理的名称为 AGENT而不是 Wget/VERSION</p>
<p>–no-http-keep-alive 关闭 HTTP活动链接 (永远链接)</p>
<p>–cookies=off 不使用 cookies</p>
<p>–load-cookies=FILE 在开始会话前从文件 FILE中加载cookie</p>
<p>–save-cookies=FILE 在会话结束后将 cookies保存到 FILE文件中</p>
<p><strong>FTP 选项参数</strong></p>
<p>-nr, –dont-remove-listing 不移走 `.listing’文件</p>
<p>-g, –glob=on/off 打开或关闭文件名的 globbing机制</p>
<p>–passive-ftp 使用被动传输模式 (缺省值).</p>
<p>–active-ftp 使用主动传输模式</p>
<p>–retr-symlinks 在递归的时候，将链接指向文件(而不是目录)</p>
<p><strong>递归下载参数</strong></p>
<p>-r, –recursive 递归下载－－慎用!</p>
<p>-l, –level=NUMBER 最大递归深度 (inf 或 0 代表无穷)</p>
<p>–delete-after 在现在完毕后局部删除文件</p>
<p>-k, –convert-links 转换非相对链接为相对链接</p>
<p>-K, –backup-converted 在转换文件X之前，将之备份为 X.orig</p>
<p>-m, –mirror 等价于 -r -N -l inf -nr</p>
<p>-p, –page-requisites 下载显示HTML文件的所有图片</p>
<p><strong>递归下载中的包含和不包含(accept/reject)</strong></p>
<p>-A, –accept=LIST 分号分隔的被接受扩展名的列表</p>
<p>-R, –reject=LIST 分号分隔的不被接受的扩展名的列表</p>
<p>-D, –domains=LIST 分号分隔的被接受域的列表</p>
<p>–exclude-domains=LIST 分号分隔的不被接受的域的列表</p>
<p>–follow-ftp 跟踪HTML文档中的FTP链接</p>
<p>–follow-tags=LIST 分号分隔的被跟踪的HTML标签的列表</p>
<p>-G, –ignore-tags=LIST 分号分隔的被忽略的HTML标签的列表</p>
<p>-H, –span-hosts 当递归时转到外部主机</p>
<p>-L, –relative 仅仅跟踪相对链接</p>
<p>-I, –include-directories=LIST 允许目录的列表</p>
<p>-X, –exclude-directories=LIST 不被包含目录的列表</p>
<p>-np, –no-parent 不要追溯到父目录</p>
<p>wget -S –spider url 不下载只显示过程</p>
<h3 id="less命令"><a href="#less命令" class="headerlink" title="less命令"></a>less命令</h3><p>&emsp;&emsp;使用<code>less</code>打开文件查看并快速定位文件内容，常用于查看日志文件。</p>
<ul>
<li>打开文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">less catalina.out</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;可以在打开的时候添加参数进行文件内容定位，比如说定位到第100行，最后一行等，但是在我实际应用的过程中基本没有用到，感觉华而不实，故此处不做介绍。</p>
<ul>
<li>全局导航</li>
</ul>
<p>g: 定位到文件的第一行</p>
<p>G: 定位到文件的最后一行</p>
<p>v: 调用编辑器进入编辑模式</p>
<p>q: 退出</p>
<ul>
<li>行导航</li>
</ul>
<p>j: 向下移动一行</p>
<p>k: 向上移动一行</p>
<p>=: 显示当前行信息，如行号、字节位置等</p>
<ul>
<li>页导航</li>
</ul>
<p>b: 向上翻一页</p>
<p>空格: 向下翻一页</p>
<p>u: 向上翻半页</p>
<p>d: 向下翻半页</p>
<ul>
<li>标记导航</li>
</ul>
<p>ma: 在当前位置标记，标记名为<code>a</code><br>‘a: 跳转到标记名为<code>a</code>的标记处</p>
<ul>
<li>搜索</li>
</ul>
<p>/text: 搜索<code>text</code>并定义到下一个匹配的文本，匹配内容高亮显示，在次搜索模式下，n: 定位到下一个匹配内容，N: 定位到上一个匹配内容。</p>
<p>?text: 搜索<code>text</code>并定义到上一个匹配的文本，匹配内容高亮显示，在次搜索模式下，n: 定位到下一个匹配内容，N: 定位到上一个匹配内容。</p>
<ul>
<li>实时查看文件内容</li>
</ul>
<p>F: 实现类似<code>tail -f *.log</code>的效果，可以实时查看文件的最新内容。使用<code>Ctrl C</code>退出这种状态。</p>
<h3 id="查看磁盘空间使用情况"><a href="#查看磁盘空间使用情况" class="headerlink" title="查看磁盘空间使用情况"></a>查看磁盘空间使用情况</h3><h4 id="du命令"><a href="#du命令" class="headerlink" title="du命令"></a>du命令</h4><p>&emsp;&emsp;du命令是对文件和目录磁盘使用的空间的查看，显示每个文件和目录的磁盘使用空间。是通过搜索文件来计算每个文件的大小然后累加，du能看到的文件只是一些当前存在的，没有被删除的。他计算的大小就是当前他认为存在的所有文件大小的累加和。</p>
<ul>
<li>缺省参数</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;显示当前目录下的子目录和文件的大小及名称，并递归查询并显示子目录下的目录文件的大小，最后一行为当前目录的大小。</p>
<ul>
<li>du file1/directory1 file2/directory2 … </li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du catalina.out</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;显示指定的文件/目录的大小。如果指定的是目录，还会递归查询并显示指定目录的子目录的大小。</p>
<ul>
<li>du -s</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -s</span><br><span class="line">du -s apache-tomcat-8.5.47/</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/du-s.png" alt="查看"></p>
<p>&emsp;&emsp;显示指定的文件/目录占用磁盘空间大小，不显示子目录</p>
<ul>
<li>du -h</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -h</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/du-h.png" alt="查看"></p>
<p>&emsp;&emsp;优化显示占用磁盘空间大小，会过滤文件，只显示目录。</p>
<ul>
<li>du -ah</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -ah logs/</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;优化显示指定文件/目录占用磁盘空间大小，递归查询并优化显示指定目录的子目录的占用磁盘空间大小。</p>
<ul>
<li>du -c</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -c README.md  RUNNING.txt</span><br></pre></td></tr></table></figure>
<p><img src="/linux-order/du-c.png" alt="查看"></p>
<p>&emsp;&emsp;查看文件占用磁盘空间大小并显示统计。</p>
<ul>
<li>du | sort -nr</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du | sort -nr</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;对目录文件大小进行顺序排序。</p>
<p>&emsp;&emsp;还有一些其他的参数可以使用，这里不做介绍，以上的参数可以组合使用，完成对目录磁盘空间占用大小的统计查看。</p>
<ul>
<li>查看指定目录下的文件/目录大小并优化显示并排序</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -ah apache-tomcat-8.5.47 | sort -nr</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看当前目录的各个子目录/文件的大小</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du --max-depth=1 -h</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">du -hd1</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;两条命令的效果是一样的。</p>
<h4 id="df命令"><a href="#df命令" class="headerlink" title="df命令"></a>df命令</h4><p>&emsp;&emsp;df命令用于显示目前在Linux系统上的文件系统的磁盘使用情况统计。通过文件系统来快速获取空间大小的信息，当我们删除一个文件的时候，这个文件不是马上就在文件系统当中消失了，而是暂时消失了，当所有程序都不用时，才会根据OS的规则释放掉已经删除的文件， df记录的是通过文件系统获取到的文件的大小，他比du强的地方就是能够看到已经删除的文件，而且计算大小的时候，把这一部分的空间也加上了，更精确了。</p>
<p>&emsp;&emsp;当文件系统也确定删除了该文件后，这时候du与df就一致了。</p>
<p>&emsp;&emsp;这里只记录我最常用的一个命令。优化文件系统的输出显示。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">df -h</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用心得，一般情况下，如果磁盘空间满了，我会先用<code>df -h</code>查看文件系统的使用情况，然后使用<code>du</code>命令跟踪定位到具体的某个或某些大文件，并根据实际情况清理释放磁盘空间。</p>
<h3 id="CentOS7防火墙设置"><a href="#CentOS7防火墙设置" class="headerlink" title="CentOS7防火墙设置"></a>CentOS7防火墙设置</h3><p>&emsp;&emsp;在旧版本的CentOS中，是使用 iptables 命令来设置防火墙的。但是，从CentOS7开始，默认就没有安装iptables，而是改用firewall来配置防火墙。修改了防火墙的设置，必须要重启防火墙才能生效。</p>
<ul>
<li><strong>启动,停止和重启</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start firewalld</span><br><span class="line">systemctl stop firewalld</span><br><span class="line">systemctl restart firewalld</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>添加端口</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --add-port=80/tcp --permanent</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>更新防火墙规则</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --reload</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看端口是否开放</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --query-port=80/tcp</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>删除</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --remove-port=80/tcp --permanent</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看防火墙状态</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --state</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看防火墙所有设置</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --list-all</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看所有打开的端口</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --zone=public --list-ports</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看区域信息</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --get-active-zones</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看指定接口所属区域</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --get-zone-of-interface=eth0</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>拒绝所有包</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --panic-on</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>取消拒绝状态</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --panic-off</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>查看是否拒绝</strong></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">firewall-cmd --query-panic</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>yum</tag>
        <tag>locate</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建maven私服</title>
    <url>/maven-private-server.html</url>
    <content><![CDATA[<p>&emsp;&emsp;私服是一种特殊的远程仓库，它是架设在局域网内的仓库服务，私服代理广域网上的远程仓库，供局域网内的Maven用户使用。当Maven需要下载构件的时候，它从私服请求，如果私服上不存在该构件，则从外部的远程仓库下载，缓存在私服上之后，再为Maven的下载请求提供服务。</p>
<a id="more"></a>
<h3 id="Nexus3"><a href="#Nexus3" class="headerlink" title="Nexus3"></a>Nexus3</h3><p>&emsp;&emsp;目前市面上有三种maven仓库管理器可以搭建私服，分别是<a href="http://archiva.apache.org" target="_blank" rel="noopener">apache的archiva</a>、<a href="https://www.jfrogchina.com" target="_blank" rel="noopener">artifactory</a>、<a href="https://www.sonatype.com" target="_blank" rel="noopener">Sonatype的Nexus</a>。目前使用较多的是Nexus，它简化了本地内部仓库的维护和外部仓库的访问；Nexus在代理远程仓库的同时维护本地仓库，以降低中央仓库的负荷,节省外网带宽和时间；Nexus是一套“开箱即用”的系统不需要数据库，它使用文件系统加Lucene来组织数据；Nexus使用ExtJS来开发界面，利用Restlet来提供完整的REST APIs；Nexus支持WebDAV与LDAP安全身份认证;Nexus还提供了强大的仓库管理功能，构件搜索功能，它基于REST，友好的UI是一个extjs的REST客户端，它占用较少的内存，基于简单文件系统而非数据库。</p>
<p>&emsp;&emsp;这里选择下载Nexus的docker镜像安装，我们可以在<code>docker hub</code>上选择<a href="https://hub.docker.com/r/sonatype/nexus3" target="_blank" rel="noopener">Nexus的官方镜像</a> 。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull sonatype/nexus3</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;运行容器</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d -p 8081:8081 --name nexus sonatype/nexus3</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;测试</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://localhost:8081</span><br></pre></td></tr></table></figure>
<h3 id="配置nexus"><a href="#配置nexus" class="headerlink" title="配置nexus"></a>配置nexus</h3><h4 id="初始化设置"><a href="#初始化设置" class="headerlink" title="初始化设置"></a>初始化设置</h4><p>&emsp;&emsp;登录</p>
<p><img src="/maven-private-server/nexus-home.png" alt="登录"></p>
<p>&emsp;&emsp;用户名是<code>admin</code>，可以通过<code>docker exec -it nexus /bin/bash</code>进入容器，然后查看<code>/nexus-data/admin.password</code>文件获取admin用户的初始密码。</p>
<p>&emsp;&emsp;登录后会要求修改初始密码,然后需要设置匿名用户的访问权限，这里禁止匿名用户访问。</p>
<p><img src="/maven-private-server/nexus-anonymous-permission.png" alt="匿名访问控制"></p>
<h4 id="仓库配置"><a href="#仓库配置" class="headerlink" title="仓库配置"></a>仓库配置</h4><p>&emsp;&emsp;nexus上的maven仓库分三种类型：</p>
<blockquote>
<p>proxy：代理仓库，代理远程的中央仓库，当用户向该类型仓库请求时，先在本地寻找，未找到的情况下去代理的中央仓库下载并存入本地然后返回给用户<br>hosted：宿主仓库，部署自己的构建到该仓库中<br>group：仓库组，用来组合多个宿主仓库和代理仓库的，当项目引用多个仓库时，可以使用仓库组将多个仓库组合起来，项目直接使用仓库组即可。</p>
</blockquote>
<p>&emsp;&emsp;替换代理仓库地址，删除nexus自带的仓库。这里我们选择<a href="https://maven.aliyun.com/repository/public" target="_blank" rel="noopener">阿里云的中央仓库</a>,这里给出我使用的仓库地址 <code>https://maven.aliyun.com/repository/public</code> 。</p>
<p>&emsp;&emsp;删除所有nexus自带的仓库，创建以下几个仓库：一个代理仓库，用来代理中央仓库；至少一个宿主仓库（每个项目创建一个对应的宿主仓库）；一个仓库组（可选，若A项目依赖于B项目，那么可以创建一个仓库组将代理仓库和B项目的仓库组合在一起，这样可以避免A项目使用多个仓库）。</p>
<p>&emsp;&emsp;创建代理仓库</p>
<p><img src="/maven-private-server/nexus-create-repository.png" alt="创建代理仓库"></p>
<p><img src="/maven-private-server/nexus-create-proxy.png" alt="创建代理仓库"></p>
<p><img src="/maven-private-server/nexus-create-proxy2.png" alt="创建代理仓库"></p>
<p>&emsp;&emsp;创建宿主仓库</p>
<p><img src="/maven-private-server/nexus-create-repository.png" alt="创建宿主仓库"></p>
<p><img src="/maven-private-server/nexus-create-hosted.png" alt="创建宿主仓库"></p>
<p><img src="/maven-private-server/nexus-create-hosted2.png" alt="创建宿主仓库"></p>
<p>&emsp;&emsp;创建仓库组</p>
<p><img src="/maven-private-server/nexus-create-repository.png" alt="创建仓库组"></p>
<p><img src="/maven-private-server/nexus-create-group.png" alt="创建仓库组"></p>
<p><img src="/maven-private-server/nexus-create-group2.png" alt="创建仓库组"></p>
<h4 id="用户角色配置"><a href="#用户角色配置" class="headerlink" title="用户角色配置"></a>用户角色配置</h4><p>&emsp;&emsp;创建角色</p>
<p><img src="/maven-private-server/nexus-create-role.png" alt="创建角色"></p>
<p><img src="/maven-private-server/nexus-create-role2.png" alt="创建角色"></p>
<p>&emsp;&emsp;创建用户</p>
<p><img src="/maven-private-server/nexus-create-user.png" alt="创建用户"></p>
<p><img src="/maven-private-server/nexus-create-user2.png" alt="创建用户"></p>
<p>&emsp;&emsp;到这里私服就算是配置好了，下面就是说明一下如何使用私服。</p>
<h3 id="使用私服"><a href="#使用私服" class="headerlink" title="使用私服"></a>使用私服</h3><h4 id="maven从私服获取依赖"><a href="#maven从私服获取依赖" class="headerlink" title="maven从私服获取依赖"></a>maven从私服获取依赖</h4><p>&emsp;&emsp;我们在上一步创建了角色和用户，并为角色分配了权限，为用户分配了角色，这里我们使用创建的用户来完成私服的使用。</p>
<p>&emsp;&emsp;首先我们需要直到maven获取依赖的搜索顺序：</p>
<blockquote>
<p>本地仓库：本地仓库的地址在maven的配置文件中指定，默认是<code>~/.m2/repository/</code>;<br>远程仓库：项目的配置文件(pom.xml)中的 repositories 配置的优先级高于配置文件(settings.xml)中的 profiles 中配置的 repositories；<br>中央仓库：默认的一个远程仓库，其id为<code>central</code></p>
</blockquote>
<p>&emsp;&emsp;在配置中需要注意<strong>若远程仓库需要用户名密码，则需要配置settings.xml配置文件中的server，其id标签和repository的id标签需要保持一致</strong>。</p>
<p>&emsp;&emsp;镜像仓库的理解：若A仓库的所有内容都可以在B仓库寻找到，那么B仓库就是A仓库的镜像。基于此，我们使用nexus创建一个代理仓库，可以使用这个代理仓库作为中央仓库的镜像，每个项目可以配置其自己的仓库。总结来说，每个项目可以从自己的仓库获取依赖文件，而镜像就是所有项目的最后一道防线，如果所需的依赖从镜像仓库中都不能获取，那么maven就会报错找不到依赖文件。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;mirrors&gt;</span><br><span class="line">    &lt;mirror&gt;</span><br><span class="line">        &lt;id&gt;nexus-proxy-aliyun&lt;/id&gt;</span><br><span class="line">        &lt;mirrorOf&gt;central&lt;/mirrorOf&gt;</span><br><span class="line">        &lt;name&gt;ferry私服代理仓库&lt;/name&gt;</span><br><span class="line">        &lt;url&gt;http://ferry.org.cn:8081/repository/proxy-aliyun/&lt;/url&gt;</span><br><span class="line">    &lt;/mirror&gt;</span><br><span class="line">&lt;/mirrors&gt;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上面的就是镜像的配置，该配置是写在settings.xml配置文件中的，我们可以配置多个仓库的镜像，这里举例配置了中央仓库的镜像，如上配置，当访问central仓库的时候url会被替换成该镜像配置中的url，如果访问需要用户名密码校验，那么他会去servers标签下寻找与此镜像id相同的server来完成用户认证。<strong>注意：<code>mirrorOf</code>中不能写<code>*</code>,<code>*</code>表示对所有仓库镜像</strong>。</p>
<p>&emsp;&emsp;基于上面的理解，我们可以为项目配置一个项目级的仓库，然后配置一个中央仓库的镜像仓库。</p>
<h5 id="配置项目级仓库"><a href="#配置项目级仓库" class="headerlink" title="配置项目级仓库"></a>配置项目级仓库</h5><p>&emsp;&emsp;settings配置文件修改,在servers标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>group-ferry<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">username</span>&gt;</span>developer<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">password</span>&gt;</span>***********<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;项目下的配置文件pom.xml修改,在repositories标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">repository</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>group-ferry<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://ferry.org.cn:8081/repository/group-ferry/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;注意，两个id要一致，这是用户认证的关键。</p>
<h5 id="配置镜像"><a href="#配置镜像" class="headerlink" title="配置镜像"></a>配置镜像</h5><p>&emsp;&emsp;settings配置文件修改,在servers标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-proxy-aliyun<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">username</span>&gt;</span>developer<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">password</span>&gt;</span>***********<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;settings配置文件修改,在mirrors标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mirror</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>nexus-proxy-aliyun<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mirrorOf</span>&gt;</span>central<span class="tag">&lt;/<span class="name">mirrorOf</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>ferry私服代理仓库<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://ferry.org.cn:8081/repository/proxy-aliyun/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mirror</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;同样的，两个id要一致，这是用户认证的关键。这里<code>mirrorOf</code>值为<code>central</code>，表示是<code>central</code>仓库的镜像。</p>
<p>&emsp;&emsp;配置全局仓库的话就是先添加用户密码信息（settings.xml中添加一个server标签），然后在settings.xml的<code>profiles</code>中定义，这里不举例了。</p>
<h4 id="构建服务到私服"><a href="#构建服务到私服" class="headerlink" title="构建服务到私服"></a>构建服务到私服</h4><p>&emsp;&emsp;实际开发中，我们需要将开发好的公共服务发布到私服供其他服务使用，这是我们需要进行一定的配置。</p>
<p>&emsp;&emsp;这里回顾以下我们之前在nexus上创建的宿主仓库（如下图），我们可以选择版本为稳定发布板（releases）还是开发版（Snapshot）亦或者修复版本（Mixed），还有一个重要的选择，就是<code>Deployment policy</code>需要选择<code>Allow redeploy</code>,否则无法上传我们的构建。</p>
<p><img src="/maven-private-server/nexus-create-hosted2.png" alt="创建宿主仓库"></p>
<p>&emsp;&emsp;settings配置文件修改,在servers标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">server</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>ferry-release<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">username</span>&gt;</span>developer<span class="tag">&lt;/<span class="name">username</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">password</span>&gt;</span>***********<span class="tag">&lt;/<span class="name">password</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">server</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;项目下的配置文件pom.xml修改,在distributionManagement标签下添加</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">repository</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span>&gt;</span>ferry-release<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url</span>&gt;</span>http://ferry.org.cn:8081/repository/ferry-releases/<span class="tag">&lt;/<span class="name">url</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">repository</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;两个id要一致，这是用户认证的关键。</p>
<p>&emsp;&emsp;发布构建的时候在项目根目录下执行<code>mvn clean deploy</code>即可。</p>
<h3 id="浏览nexus仓库内容"><a href="#浏览nexus仓库内容" class="headerlink" title="浏览nexus仓库内容"></a>浏览nexus仓库内容</h3><p>&emsp;&emsp;nexus提供了友好的UI界面供我们访问仓库内容</p>
<p><img src="/maven-private-server/nexus-browse.png" alt="browse"></p>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>maven</tag>
      </tags>
  </entry>
  <entry>
    <title>mysql</title>
    <url>/mysql.html</url>
    <content><![CDATA[<p>&emsp;&emsp;记录一下mysql中常用的语法吧，也算是通用的sql语法。</p>
<a id="more"></a>
<h3 id="建库建表"><a href="#建库建表" class="headerlink" title="建库建表"></a>建库建表</h3><ul>
<li>创建一个新的数据库</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">DATABASE</span> DEMO <span class="keyword">DEFAULT</span> <span class="built_in">CHARACTER</span> <span class="keyword">SET</span> utf8 <span class="keyword">COLLATE</span> utf8_general_ci;</span><br></pre></td></tr></table></figure>
<ul>
<li>建表语句</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">user</span> (</span><br><span class="line">  <span class="keyword">id</span> <span class="built_in">bigint</span>(<span class="number">20</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> AUTO_INCREMENT <span class="keyword">COMMENT</span> <span class="string">"自增长，不为空"</span>,</span><br><span class="line">  <span class="built_in">number</span> <span class="built_in">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">COMMENT</span> <span class="string">"编号"</span>,</span><br><span class="line">  <span class="keyword">name</span> <span class="built_in">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT</span> <span class="literal">NULL</span> <span class="keyword">COMMENT</span> <span class="string">"可以超过255个字符，存储变长，节省存储空间"</span>,</span><br><span class="line">  age <span class="built_in">tinyint</span> <span class="keyword">COMMENT</span> <span class="string">"-128~127"</span>,</span><br><span class="line">  money <span class="built_in">float</span>(<span class="number">10</span>,<span class="number">1</span>) <span class="keyword">COMMENT</span> <span class="string">"单精度浮点型，m总个数，d小数位，容易造成精度丢失"</span>,</span><br><span class="line">  height <span class="keyword">double</span>(<span class="number">10</span>,<span class="number">1</span>) <span class="keyword">COMMENT</span> <span class="string">"双精度浮点型，m总个数，d小数位"</span>,</span><br><span class="line">  weight <span class="keyword">double</span> <span class="keyword">COMMENT</span> <span class="string">"可以不设置m,d"</span>,</span><br><span class="line">  phone <span class="built_in">decimal</span>(<span class="number">10</span>,<span class="number">1</span>) <span class="keyword">COMMENT</span> <span class="string">"高精度的数据类型，常用来存储交易相关的数据，DECIMAL(M,N).M代表总精度，N代表小数点右侧的位数。1 &lt; M &lt; 254, 0 &lt; N &lt; 60;存储空间变长"</span>,</span><br><span class="line">  born_date <span class="built_in">date</span> <span class="keyword">COMMENT</span> <span class="string">"精确到年月日"</span>,</span><br><span class="line">  register_date datetime <span class="keyword">COMMENT</span> <span class="string">"精确到年月日时分秒"</span>,</span><br><span class="line">  description <span class="built_in">text</span> <span class="keyword">COMMENT</span> <span class="string">"总大小为65535字节，约为64KB"</span>,</span><br><span class="line">  <span class="keyword">constraint</span> pk_user_id_and_number primary <span class="keyword">key</span> (<span class="keyword">id</span>,<span class="built_in">number</span>),</span><br><span class="line">  <span class="keyword">constraint</span> fk_user_height_and_weight <span class="keyword">foreign</span> <span class="keyword">key</span>(height,weight) <span class="keyword">references</span> other_table_name(major_key_one,major_key_two),</span><br><span class="line">  <span class="keyword">constraint</span> uk_user_name <span class="keyword">unique</span> <span class="keyword">key</span>(<span class="keyword">name</span>)</span><br><span class="line">) <span class="keyword">ENGINE</span>=<span class="keyword">InnoDB</span> <span class="keyword">DEFAULT</span> <span class="keyword">CHARSET</span>=utf8 <span class="keyword">COMMENT</span> <span class="string">"表注释"</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>显示建表语句</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>查询表结构</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">desc user;</span><br></pre></td></tr></table></figure>
<ul>
<li>修改表名</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">table</span> <span class="keyword">user</span> <span class="keyword">rename</span> new_user;</span><br></pre></td></tr></table></figure>
<ul>
<li>如果存在则删除表</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">drop</span> <span class="keyword">table</span> <span class="keyword">if</span> <span class="keyword">exists</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<h3 id="常用关键字的用法"><a href="#常用关键字的用法" class="headerlink" title="常用关键字的用法"></a>常用关键字的用法</h3><h4 id="limit的用法"><a href="#limit的用法" class="headerlink" title="limit的用法"></a>limit的用法</h4><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">limit</span> <span class="number">5</span>;</span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> <span class="keyword">user</span> <span class="keyword">limit</span> <span class="number">2</span>,<span class="number">3</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;当<code>limit</code>后面只有一个参数的时候，表示返回最大的记录行数。当<code>limit</code>后面有两个参数的时候，第一个参数表示偏移量，第二个参数表示返回记录行的最大的数目</p>
<h4 id="distinct的用法"><a href="#distinct的用法" class="headerlink" title="distinct的用法"></a>distinct的用法</h4><ul>
<li>查询id不同的记录有多少条</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(<span class="keyword">distinct</span> <span class="keyword">id</span>) <span class="keyword">from</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>查询id和name同时不相同的记录</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">distinct</span> <span class="keyword">id</span>,<span class="keyword">name</span> <span class="keyword">from</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<h4 id="case的用法"><a href="#case的用法" class="headerlink" title="case的用法"></a>case的用法</h4><ul>
<li>第一种用法</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">id</span>, <span class="keyword">name</span>,</span><br><span class="line">	<span class="keyword">case</span> </span><br><span class="line">		<span class="keyword">when</span> sex = <span class="string">'male'</span> <span class="keyword">then</span> <span class="string">'男'</span></span><br><span class="line">		<span class="keyword">when</span> sex = <span class="string">'female'</span> <span class="keyword">then</span> <span class="string">'女'</span></span><br><span class="line">		<span class="keyword">else</span> <span class="string">'人妖'</span> </span><br><span class="line">	<span class="keyword">end</span> <span class="keyword">as</span> sex</span><br><span class="line"><span class="keyword">from</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这种用法还可以在<code>when</code>中使用<code>and</code>、<code>or</code>等关键字拼接多个条件对进行筛选</p>
<ul>
<li>第二种用法</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">id</span>, <span class="keyword">name</span>,</span><br><span class="line">	<span class="keyword">case</span> sex</span><br><span class="line">		<span class="keyword">when</span> <span class="string">'male'</span> <span class="keyword">then</span> <span class="string">'男'</span></span><br><span class="line">		<span class="keyword">when</span> <span class="string">'female'</span> <span class="keyword">then</span> <span class="string">'女'</span></span><br><span class="line">		<span class="keyword">else</span> <span class="string">'人妖'</span> </span><br><span class="line">	<span class="keyword">end</span> <span class="keyword">as</span> sex</span><br><span class="line"><span class="keyword">from</span> <span class="keyword">user</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;值得一提的是，<code>CASE</code>函数只返回第一个符合条件的结果，所以在使用的时候需要注意<code>WHEN</code>条件的返回。</p>
<h3 id="用户操作"><a href="#用户操作" class="headerlink" title="用户操作"></a>用户操作</h3><h4 id="创建用户"><a href="#创建用户" class="headerlink" title="创建用户"></a>创建用户</h4><ul>
<li>创建用户</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">'username'</span>@<span class="string">'host'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">BY</span> <span class="string">'password'</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>修改密码</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">user</span> <span class="string">'root'</span>@<span class="string">'localhost'</span> <span class="keyword">identified</span> <span class="keyword">by</span> <span class="string">'ferry'</span>;</span><br><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>host</code>是IP地址，可以指向某一个ip地址，也可以使用<code>%</code>来表示所有的ip地址都可以通过该用户名密码连接该数据库。密码可以为空，如果密码给空字符串，则登录该用户不需要密码（不推荐）。</p>
<h4 id="授权"><a href="#授权" class="headerlink" title="授权"></a>授权</h4><p>&emsp;&emsp;为用户授权的语法：grant 权限 on 数据库.表名 to ‘用户名‘@’登录主机’ IDENTIFIED BY ‘密码’;</p>
<ul>
<li>赋予用户某个数据库全部权限并刷新系统权限</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">grant</span> <span class="keyword">all</span> <span class="keyword">privileges</span> <span class="keyword">on</span> demo.* <span class="keyword">to</span> <span class="string">'ferry'</span>@<span class="string">'%'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">BY</span> <span class="string">'ferry'</span>;</span><br><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>all privileges</code>是所有权限的意思，这条语句的意思是将<code>demo</code>数据库的所有表的所有权限赋予密码为<code>ferry</code>，用户名为<code>ferry</code>的用户，可以通过任意IP地址通过该用户连接数据库。</p>
<ul>
<li>赋予用户部分权限并刷新系统权限</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">grant</span> <span class="keyword">select</span>,<span class="keyword">delete</span>,<span class="keyword">update</span>,<span class="keyword">create</span>,<span class="keyword">drop</span> <span class="keyword">on</span> demo.user <span class="keyword">to</span> <span class="string">'ferry'</span>@<span class="string">'localhost'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">BY</span> <span class="string">'ferry'</span>;</span><br><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这条语句的意思是将<code>demo</code>数据库的<code>user</code>的crud权限赋予密码为<code>ferry</code>，用户名为<code>ferry</code>的用户，该用户只能在本地连接数据库。</p>
<ul>
<li>超级权限</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">grant</span> <span class="keyword">all</span> <span class="keyword">privileges</span> <span class="keyword">on</span> demo.* <span class="keyword">to</span> <span class="string">'ferry'</span>@<span class="string">'%'</span> <span class="keyword">IDENTIFIED</span> <span class="keyword">BY</span> <span class="string">'ferry'</span> <span class="keyword">WITH</span> <span class="keyword">GRANT</span> <span class="keyword">OPTION</span>;</span><br><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>ferry</code>用户可以赋权给其他用户。</p>
<h4 id="删除用户"><a href="#删除用户" class="headerlink" title="删除用户"></a>删除用户</h4><ul>
<li>删除用户及其权限</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">drop</span> <span class="keyword">user</span> <span class="string">'ferry'</span>@<span class="string">'%'</span>;</span><br></pre></td></tr></table></figure>
<h3 id="基于centOS安装mysql5-7"><a href="#基于centOS安装mysql5-7" class="headerlink" title="基于centOS安装mysql5.7"></a>基于centOS安装mysql5.7</h3><h4 id="yum安装"><a href="#yum安装" class="headerlink" title="yum安装"></a>yum安装</h4><ul>
<li>更新yum</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum update</span><br></pre></td></tr></table></figure>
<ul>
<li>安装wget（已安装忽略）</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install wget</span><br></pre></td></tr></table></figure>
<ul>
<li>查看Linux发行版本（其实没必要，看一下自己的系统版本而已。。）</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /etc/redhat-release</span><br></pre></td></tr></table></figure>
<ul>
<li>获取MySQL官方的Yum Repository</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://dev.mysql.com/get/mysql57-community-release-el7-7.noarch.rpm</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里要bb一句，下载链接是从mysql官网找到粘贴过来的，用点心，注意官网的yum repository分类下面找下载链接</p>
<ul>
<li>查看文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql-ls-result.png" alt="查看结果"></p>
<ul>
<li>安装mysql的Yum Repository</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install mysql*</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;因为该目录下只有一个以mysql开口的文件名，所以在这里我直接使用<code>*</code>模糊查询。</p>
<ul>
<li>安装MySQL的服务器版本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install mysql-community-server</span><br></pre></td></tr></table></figure>
<ul>
<li>启动mysql服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start mysqld.service</span><br></pre></td></tr></table></figure>
<ul>
<li>查看服务状态</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status mysqld.service</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql-install-service-status.png" alt="查看状态"></p>
<p>&emsp;&emsp;很显然，服务已经正常的启动了，接下来就是登陆mysql了，使用yum安装mysql并启动服务后，MySQL进程会自动在进程日志中打印root用户的初始密码。</p>
<ul>
<li>获取mysql初始密码</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep <span class="string">"password"</span> /var/<span class="built_in">log</span>/mysqld.log</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql-init-password.png" alt="获取初始密码"></p>
<ul>
<li>登陆并修改密码</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">user</span> <span class="string">'root'</span>@<span class="string">'localhost'</span> <span class="keyword">identified</span> <span class="keyword">by</span> <span class="string">'ferry'</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;直接这样运行，那么肯定报错了，当你使用日志中的密码登陆时会要求你修改密码，在你修改密码之前，你所能做的操作真的少得可怜。同时这个密码的强度默认是中等的，具体这个中等强度有那些要求我不太清楚，反正密码我没设置成功过。想设置简单一点的密码就是降低密码强度。还需要注意的是因为root用户的ip地址是<code>localhost</code>，所以只能指向localhost。</p>
<ul>
<li>降低密码强度并设置密码最低位数</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> validate_password_policy=<span class="number">0</span>;</span><br><span class="line"><span class="keyword">set</span> <span class="keyword">global</span> validate_password_length=<span class="number">4</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>修改密码并刷新权限</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">user</span> <span class="string">'root'</span>@<span class="string">'localhost'</span> <span class="keyword">identified</span> <span class="keyword">by</span> <span class="string">'ferry'</span>;</span><br><span class="line"><span class="keyword">flush</span> <span class="keyword">privileges</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果报错，请使用：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">set</span> <span class="keyword">password</span> <span class="keyword">for</span> ferry@localhost=<span class="keyword">password</span>(<span class="string">'ferry'</span>);</span><br></pre></td></tr></table></figure>
<h4 id="二进制文件安装"><a href="#二进制文件安装" class="headerlink" title="二进制文件安装"></a>二进制文件安装</h4><h5 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h5><p>&emsp;&emsp;在<a href="www.mysql.com">mysql官网</a>点击<code>MySQL Community Server</code>-<code>Looking for previous GA versions?</code>选择指定的版本的数据库。如下选择的是GA版5.7.28的linux通用版64位的进行下载。</p>
<p><img src="/mysql/mysql-install-select.png" alt="选择二进制文件"></p>
<p>&emsp;&emsp;下文安装选择mysql8.0进行安装。</p>
<ul>
<li>MD5校验，确保软件包可用</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">md5sum mysql-8.0.18-linux-glibc2.12-x86_64.tar.xz</span><br></pre></td></tr></table></figure>
<ul>
<li>SELinux和系统防火墙关闭</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl stop firewalld.service</span><br><span class="line">vi /etc/sysconfig/selinux</span><br><span class="line">SELINUX=disabled</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;修改SELinux需要重启系统生效。安全增强型 Linux（Security-Enhanced Linux）简称 SELinux，它是一个 Linux 内核模块，也是 Linux 的一个安全子系统。SELinux 主要作用就是最大限度地减小系统中服务进程可访问的资源（最小权限原则）。</p>
<ul>
<li>I/O调度系统使用deadline模式</li>
</ul>
<ol>
<li><p>Linus Elevator: 在2.4 内核中它是第一种I/O调度器。它的主要作用是为每个设备维护一个查询请求，当内核收到一个新请求时，如果能合并就合并。如果不能合并，就会尝试排序。如果既不能合并，也没有合适的位置插入，就放到请求队列的最后。</p>
</li>
<li><p>Anticipatory: Anticipatory的中文含义是”预料的，预想的”，顾名思义有个I/O发生的时候，如果又有进程请求I/O操作，则将产生一个默认的6毫秒猜测时间，猜测下一个进程请求I/O是要干什么的。这个I/O调度器对读操作优化服务时间，在提供一个I/O的时候进行短时间等待，使进程能够提交到另外的I/O。Anticipatory算法从Linux 2.6.33版本后被删除了，因为使用CFQ通过配置也能达到Anticipatory的效果。</p>
</li>
<li><p>DeadLine: Deadline翻译成中文是截止时间调度器，是对Linus Elevator的一种改进，它避免有些请求太长时间不能被处理。另外可以区分对待读操作和写操作。DEADLINE额外分别为读I/O和写I/O提供了FIFO队列。</p>
</li>
<li><p>CFQ: CFQ全称Completely Fair Scheduler ，中文名称完全公平调度器，它是现在许多 Linux 发行版的默认调度器，CFQ是内核默认选择的I/O调度器。它将由进程提交的同步请求放到多个进程队列中，然后为每个队列分配时间片以访问磁盘。对于通用的服务器是最好的选择,CFQ均匀地分布对I/O带宽的访问。CFQ为每个进程和线程,单独创建一个队列来管理该进程所产生的请求,以此来保证每个进程都能被很好的分配到I/O带宽，I/O调度器每次执行一个进程的4次请求。该算法的特点是按照I/O请求的地址进行排序，而不是按照先来后到的顺序来进行响应。简单来说就是给所有同步进程分配时间片，然后才排队访问磁盘</p>
</li>
<li><p>NOOP: NOOP全称No Operation,中文名称电梯式调度器，该算法实现了最简单的FIFO队列，所有I/O请求大致按照先来后到的顺序进行操作。NOOP实现了一个简单的FIFO队列,它像电梯的工作主法一样对I/O请求进行组织。它是基于先入先出（FIFO）队列概念的 Linux 内核里最简单的I/O 调度器。此调度程序最适合于固态硬盘。</p>
</li>
</ol>
<p>&emsp;&emsp;I/O调度器的选择: 目前主流Linux发行版本使用三种I/O调度器：DeadLine、CFQ、NOOP，通常来说Deadline适用于大多数环境,特别是写入较多的文件服务器，从原理上看，DeadLine是一种以提高机械硬盘吞吐量为思考出发点的调度算法，尽量保证在有I/O请求达到最终期限的时候进行调度，非常适合业务比较单一并且I/O压力比较重的业务，比如Web服务器，数据库应用等。CFQ 为所有进程分配等量的带宽,适用于有大量进程的多用户系统，CFQ是一种比较通用的调度算法，它是一种以进程为出发点考虑的调度算法，保证大家尽量公平,为所有进程分配等量的带宽,适合于桌面多任务及多媒体应用。NOOP 对于闪存设备和嵌入式系统是最好的选择。对于固态硬盘来说使用NOOP是最好的，DeadLine次之，而CFQ效率最低。</p>
<ul>
<li>查看当前系统支持的I/O调度器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dmesg | grep -i scheduler</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql_install_io_show.png" alt="显示"></p>
<p>&emsp;&emsp;当前I/O调度器是<code>deadline</code>。</p>
<ul>
<li>查看某块硬盘的IO调度算法I/O调度器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /sys/block/sda/queue/scheduler</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果sda目录不存在，就找vda目录。</p>
<ul>
<li>swap分区的设置</li>
</ul>
<p>&emsp;&emsp;不建议设置，或者设置4G。</p>
<ul>
<li>文件系统的选择</li>
</ul>
<p>&emsp;&emsp;建议使用xfs文件系统，相比ext4，它更加方便管理，支持动态扩容，删除文件也很方便。</p>
<ul>
<li>操作系统的限制</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ulimit</span> -a</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql_os_ulimit.png" alt="查看参数"></p>
<p>&emsp;&emsp;open files如果设置不合理，那么当服务器连接过多或者表过多时就可能访问不了或者打不开表；max user processes参数的用途是，有时候我们可能会跑多实例，但是创建不了新的连接，报出”resource temporarily unavailable”的错误，表示没有足够的资源。</p>
<ul>
<li>修改系统限制，编辑<code>/etc/security/limits.conf</code>(重启系统)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">*   soft    nproc   65535</span><br><span class="line">*   hard    nproc   65535</span><br><span class="line">*   soft    nofile  65535</span><br><span class="line">*   hard    nofile  65535</span><br></pre></td></tr></table></figure>
<ul>
<li>关闭numa</li>
</ul>
<p>&emsp;&emsp;关闭numa功能，能够更好的分配内存，不需要采用swap的方式获取内存。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">numa --interleave=all /usr/<span class="built_in">local</span>/mysql/bin/mysqld_safe -defaults-file=/etc/my.cnf &amp;</span><br></pre></td></tr></table></figure>
<h5 id="用户，目录设置"><a href="#用户，目录设置" class="headerlink" title="用户，目录设置"></a>用户，目录设置</h5><ul>
<li>创建用户，用户组</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">groupadd mysql</span><br><span class="line">useradd -g mysql mysql -s /sbin/nologin</span><br></pre></td></tr></table></figure>
<ul>
<li>解压软件包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -zxvf mysql-8.0.18-linux-glibc2.12-x86_64.tar.gz</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果解压出错，但是之前md5校验没问题，那么大概率是我们的系统缺少什么依赖没有安装，按照shell提示的错误信息解决即可。</p>
<ul>
<li>软连接，方便日后升级</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -s mysql-8.0.18-linux-glibc2.12-x86_64 mysql</span><br></pre></td></tr></table></figure>
<ul>
<li>授权</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chown mysql:mysql -R mysql</span><br></pre></td></tr></table></figure>
<ul>
<li>数据目录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir -p /data/mysql</span><br><span class="line">chown mysql:mysql -R /data/mysql</span><br></pre></td></tr></table></figure>
<ul>
<li>配置mysql环境变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /etc/profile.d</span><br><span class="line">vi mysql.sh</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/usr/<span class="built_in">local</span>/mysql/bin</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chown mysql:mysql mysql.sh</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">. /etc/profile.d/mysql.sh</span><br></pre></td></tr></table></figure>
<ul>
<li>配置文件<code>vi /etc/my.cnf</code></li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h5 id="启动数据库"><a href="#启动数据库" class="headerlink" title="启动数据库"></a>启动数据库</h5><ul>
<li>初始化数据库</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/mysql/bin</span><br><span class="line">./mysqld --defaults-file=/etc/my.cnf --basedir=/usr/<span class="built_in">local</span>/mysql --datadir=/data/mysql/ --user=mysql --initialize</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>defaults-file</code>指定mysql的配置文件,<code>basedir</code>指定mysql的安装目录,<code>datadir</code>指定mysql的数据目录,<code>user</code>指定用户,<code>initialize</code>会在数据目录下生成一个文件log-error,记录零时的数据库初始化密码，如果是<code>initialize-insecure</code>代表无密码进入。</p>
<ul>
<li>启动数据库</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/mysql/bin/</span><br><span class="line">./mysqld_safe --defaults-file=/etc/my.cnf &amp;</span><br></pre></td></tr></table></figure>
<ul>
<li>登陆</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql -u root -p -P 3306 -h localhost</span><br></pre></td></tr></table></figure>
<h3 id="修改mysql配置文件"><a href="#修改mysql配置文件" class="headerlink" title="修改mysql配置文件"></a>修改mysql配置文件</h3><ul>
<li>查看mysql服务默认读取的的my.cnf文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mysql --<span class="built_in">help</span>|grep <span class="string">'my.cnf'</span></span><br></pre></td></tr></table></figure>
<h4 id="查询缓存"><a href="#查询缓存" class="headerlink" title="查询缓存"></a>查询缓存</h4><p>&emsp;&emsp;查询缓存只能缓存静态的数据，数据仓库之类可能考虑使用查询缓存，生产建议关闭。Mysql5.6之前查询缓存默认开启，之后默认关闭，8.0版本之后删除了查询缓存功能。</p>
<ul>
<li>查看查询缓存参数</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">show variables like &quot;%query_cache%&quot;;</span><br></pre></td></tr></table></figure>
<p><img src="/mysql/mysql_query_cache_variables.png" alt="查询结果"></p>
<ul>
<li>关闭查询缓存(vi /etc/my.cnf)重启服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">query_cache_type=0</span><br><span class="line">query_cache_size=0</span><br></pre></td></tr></table></figure>
<h4 id="设置时区"><a href="#设置时区" class="headerlink" title="设置时区"></a>设置时区</h4><p>&emsp;&emsp;mysql中默认使用的SYSTEM时区，即EST时区，EST时区要比北京时间（东八区）慢13个小时。我们一般使用北京时间也就是东八区。</p>
<ul>
<li>查询时区</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> <span class="keyword">variables</span> <span class="keyword">like</span> <span class="string">'%time_zone%'</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>设置时区(vi /etc/my.cnf)重启服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">default-time_zone = <span class="string">'+8:00'</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;未完待续。。。。。</p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>mysql</tag>
        <tag>limit</tag>
        <tag>case</tag>
        <tag>创建用户</tag>
        <tag>分配权限</tag>
        <tag>安装mysql5.7</tag>
      </tags>
  </entry>
  <entry>
    <title>oracle sql 调优</title>
    <url>/oracle-sql-optimize.html</url>
    <content><![CDATA[<p>&emsp;&emsp;oracle数据库sql调优学习记录笔记。</p>
<a id="more"></a>
<h1 id="核心思想"><a href="#核心思想" class="headerlink" title="核心思想"></a>核心思想</h1><h2 id="只有大表才会产生性能"><a href="#只有大表才会产生性能" class="headerlink" title="只有大表才会产生性能"></a>只有大表才会产生性能</h2><h1 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h1><h2 id="基数"><a href="#基数" class="headerlink" title="基数"></a>基数</h2><p>&emsp;&emsp;某个列唯一键的数量叫做基数。主键列的基数等于行数。性别列的基数等于2。</p>
<p>&emsp;&emsp;基数的高低影响列的数据分布。</p>
<p>&emsp;&emsp;当查询结果返回表中5%以内的数据时，应该走索引；当查询结果返回表中超过5%的数据时，应该走全表扫描。</p>
<ul>
<li><strong>查询性别列的基数</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(<span class="keyword">distinct</span> t.sex) <span class="keyword">from</span> <span class="keyword">user</span> t</span><br></pre></td></tr></table></figure>
<h2 id="选择性"><a href="#选择性" class="headerlink" title="选择性"></a>选择性</h2><p>&emsp;&emsp;基数/表的总行数*100%。</p>
<p>&emsp;&emsp;什么样的列必须创建索引？当一个列出现在 where 条件中，该列没有创建索引并且选择性大于20%，那么该列就必须创建索引。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="keyword">count</span>(<span class="keyword">distinct</span> t.sex) / <span class="keyword">count</span>(*) <span class="keyword">from</span> <span class="keyword">user</span> t</span><br></pre></td></tr></table></figure>
<h2 id="直方图"><a href="#直方图" class="headerlink" title="直方图"></a>直方图</h2><p>&emsp;&emsp;当某个列的基数很低，该列的数据分布就会不均衡。数据分布不均衡，就会导致在查询该列时要么走索引，要么走全表扫描，这时候很容易走错执行计划。</p>
<p>&emsp;&emsp;<strong>如果没有对基数低的列收集直方图统计信息，基于成本的优化器（CBO）会认为该列的数据分布是均衡的。</strong></p>
<p>&emsp;&emsp;<strong>直方图是用来帮助CBO在对基数很低、数据分布不均衡的列进行Rows估算的时候，可以得到更精确的Rows。</strong></p>
<p>&emsp;&emsp;什么样的列应该收集直方图？该列出现在where条件中，列的选择性小于1%并且该列没有收集过直方图。</p>
<h2 id="回表"><a href="#回表" class="headerlink" title="回表"></a>回表</h2><p>&emsp;&emsp;当对一个列创建索引之后，索引会包含该列的键值以及键值对应行所在的rowid。</p>
<p>&emsp;&emsp;<strong>通过索引中记录的rowid访问表中的数据就叫回表。回表一般是单块读（一个rowid对应一个数据块），回表次数太多会严重影响SQL性能。如果回表次数太多，就不应该走索引，应该直接走全表扫描。</strong></p>
<p>&emsp;&emsp;<code>arraysize</code>参数：表示Oracle服务器每次传输多少行数据到客户端。假设<code>arraysize=15</code>，如果一个数据块有150行数据，那么每次传输15行，需要读取这个数据块10次才能读完，此时逻辑读被放大了。为了消除arraysize对逻辑读的影响，可以将参数调大。</p>
<h1 id="全自动优化脚本"><a href="#全自动优化脚本" class="headerlink" title="全自动优化脚本"></a>全自动优化脚本</h1><h2 id="抓住必须创建索引的列"><a href="#抓住必须创建索引的列" class="headerlink" title="抓住必须创建索引的列"></a>抓住必须创建索引的列</h2><ul>
<li>刷行数据库监控信息</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">begin</span></span><br><span class="line">  dbms_stats.flush_database_monitoring_info;</span><br><span class="line"><span class="keyword">end</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>查询指定用户指定表出现在where条件中的列信息</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> r.name <span class="keyword">as</span> owner,</span><br><span class="line">       o.name <span class="keyword">as</span> table_name,</span><br><span class="line">       c.name <span class="keyword">as</span> column_name,</span><br><span class="line">       u.equality_preds,     <span class="comment">-- 等值过滤</span></span><br><span class="line">       u.equijoin_preds,     <span class="comment">-- 等值join，ex: where a.id=b.id</span></span><br><span class="line">       u.nonequijoin_preds,  <span class="comment">-- 不等于join</span></span><br><span class="line">       u.range_preds,        <span class="comment">-- 返回过滤次数</span></span><br><span class="line">       u.like_preds,         <span class="comment">-- like过滤</span></span><br><span class="line">       u.null_preds,         <span class="comment">-- null过滤</span></span><br><span class="line">       u.timestamp</span><br><span class="line"><span class="keyword">from</span> sys.col_usage$ u,</span><br><span class="line">     sys.obj$ o,</span><br><span class="line">     sys.col$ c,</span><br><span class="line">     sys.user$ r</span><br><span class="line"><span class="keyword">where</span> u.obj<span class="comment"># = o.obj#</span></span><br><span class="line">  <span class="keyword">and</span> u.obj<span class="comment"># = c.obj#</span></span><br><span class="line">  <span class="keyword">and</span> u.intcol<span class="comment"># = c.col#</span></span><br><span class="line">  <span class="keyword">and</span> r.name = <span class="string">'RETAIL'</span></span><br><span class="line">  <span class="keyword">and</span> o.name = <span class="string">'CON_CONTRACT'</span></span><br></pre></td></tr></table></figure>
<ul>
<li>查询指定用户指定表的索引信息</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> c.table_owner, c.table_name, c.column_name, c.index_name</span><br><span class="line"><span class="keyword">from</span> dba_ind_columns c</span><br><span class="line"><span class="keyword">where</span> c.table_owner = <span class="string">'RETAIL'</span></span><br><span class="line">  <span class="keyword">and</span> c.table_name = <span class="string">'CON_CONTRACT'</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>指定用户指定表的选择性大于20%的列</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> a.owner, a.table_name, a.column_name, <span class="keyword">round</span>(a.num_distinct / b.num_rows * <span class="number">100</span>, <span class="number">2</span>) selectivity</span><br><span class="line"><span class="keyword">from</span> dba_tab_col_statistics a,</span><br><span class="line">     dba_tables b</span><br><span class="line"><span class="keyword">where</span> a.owner = b.owner</span><br><span class="line">  <span class="keyword">and</span> a.table_name = b.table_name</span><br><span class="line">  <span class="keyword">and</span> a.owner = <span class="string">'RETAIL'</span></span><br><span class="line">  <span class="keyword">and</span> a.table_name = <span class="string">'CON_CONTRACT'</span></span><br><span class="line">  <span class="keyword">and</span> a.num_distinct / b.num_rows &gt;= <span class="number">0.2</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>最终的脚本</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> t1.owner, t1.table_name, t1.column_name, t1.num_rows, t1.Cardinality, t1.selectivity, <span class="string">'Need index'</span> <span class="keyword">as</span> <span class="keyword">notice</span></span><br><span class="line"><span class="keyword">from</span> (<span class="keyword">select</span> b.owner,</span><br><span class="line">             a.table_name,</span><br><span class="line">             a.column_name,</span><br><span class="line">             b.num_rows,</span><br><span class="line">             a.num_distinct                              Cardinality,</span><br><span class="line">             <span class="keyword">round</span>(a.num_distinct / b.num_rows * <span class="number">100</span>, <span class="number">2</span>) selectivity</span><br><span class="line">      <span class="keyword">from</span> dba_tab_col_statistics a,</span><br><span class="line">           dba_tables b</span><br><span class="line">      <span class="keyword">where</span> a.owner = b.owner</span><br><span class="line">        <span class="keyword">and</span> a.table_name = b.table_name</span><br><span class="line">        <span class="keyword">and</span> a.owner = <span class="string">'RETAIL'</span></span><br><span class="line">        <span class="keyword">and</span> a.table_name = <span class="string">'CON_CONTRACT'</span>) t1</span><br><span class="line"><span class="keyword">where</span> t1.selectivity &gt;= <span class="number">20</span></span><br><span class="line">  <span class="keyword">and</span> <span class="keyword">not</span> <span class="keyword">exists</span>(<span class="keyword">select</span> <span class="number">1</span></span><br><span class="line">                 <span class="keyword">from</span> dba_ind_columns t2</span><br><span class="line">                 <span class="keyword">where</span> t2.table_owner = t1.owner</span><br><span class="line">                   <span class="keyword">and</span> t2.table_name = t1.table_name</span><br><span class="line">                   <span class="keyword">and</span> t2.column_name = t1.column_name)</span><br><span class="line">  <span class="keyword">and</span> <span class="keyword">exists</span>(<span class="keyword">select</span> <span class="number">1</span></span><br><span class="line">             <span class="keyword">from</span> sys.col_usage$ u,</span><br><span class="line">                  sys.obj$ o,</span><br><span class="line">                  sys.col$ c,</span><br><span class="line">                  sys.user$ r</span><br><span class="line">             <span class="keyword">where</span> o.obj<span class="comment"># = u.obj#</span></span><br><span class="line">               <span class="keyword">and</span> c.obj<span class="comment"># = u.obj#</span></span><br><span class="line">               <span class="keyword">and</span> c.col<span class="comment"># = u.intcol#</span></span><br><span class="line">               <span class="keyword">and</span> r.name = t1.owner</span><br><span class="line">               <span class="keyword">and</span> o.name = t1.table_name</span><br><span class="line">               <span class="keyword">and</span> c.name = t1.column_name);</span><br></pre></td></tr></table></figure>
<h2 id="抓住必须创建直方图的列"><a href="#抓住必须创建直方图的列" class="headerlink" title="抓住必须创建直方图的列"></a>抓住必须创建直方图的列</h2><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> a.owner,</span><br><span class="line">       a.table_name,</span><br><span class="line">       a.column_name,</span><br><span class="line">       b.num_rows,</span><br><span class="line">       a.num_distinct,</span><br><span class="line">       trunc(a.num_distinct / b.num_rows * <span class="number">100</span>, <span class="number">2</span>) selectivity,</span><br><span class="line">       <span class="string">'Need Gather Histogram'</span>                     <span class="keyword">notice</span></span><br><span class="line"><span class="keyword">from</span> dba_tab_col_statistics a,</span><br><span class="line">     dba_tables b</span><br><span class="line"><span class="keyword">where</span> a.owner = <span class="string">'RETAIL'</span></span><br><span class="line">  <span class="keyword">and</span> a.table_name = <span class="string">'CON_CONTRACT'</span></span><br><span class="line">  <span class="keyword">and</span> a.owner = b.owner</span><br><span class="line">  <span class="keyword">and</span> a.table_name = b.table_name</span><br><span class="line">  <span class="keyword">and</span> a.num_distinct / b.num_rows &lt; <span class="number">0.01</span></span><br><span class="line">  <span class="keyword">and</span> (a.owner, a.table_name, a.column_name) <span class="keyword">in</span></span><br><span class="line">      (<span class="keyword">select</span> r.name owner, o.name table_name, c.name column_name</span><br><span class="line">       <span class="keyword">from</span> sys.col_usage$ u,</span><br><span class="line">            sys.obj$ o,</span><br><span class="line">            sys.col$ c,</span><br><span class="line">            sys.user$ r</span><br><span class="line">       <span class="keyword">where</span> o.obj<span class="comment"># = u.obj#</span></span><br><span class="line">         <span class="keyword">and</span> c.obj<span class="comment"># = u.obj#</span></span><br><span class="line">         <span class="keyword">and</span> c.col<span class="comment"># = u.intcol#</span></span><br><span class="line">         <span class="keyword">and</span> r.name = a.owner</span><br><span class="line">         <span class="keyword">and</span> o.name = a.table_name)</span><br><span class="line">  <span class="keyword">and</span> a.histogram = <span class="string">'NONE'</span>;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>oracle</category>
      </categories>
      <tags>
        <tag>oracle</tag>
        <tag>sql</tag>
      </tags>
  </entry>
  <entry>
    <title>oracle</title>
    <url>/oracle.html</url>
    <content><![CDATA[<p>&emsp;&emsp;Oracle数据库是目前世界上使用最为广泛的数据库管理系统，作为一个通用的数据库系统，它具有完整的数据管理功能；作为一个关系数据库，它是一个完备关系的产品；作为分布式数据库它实现了分布式处理功能。</p>
<a id="more"></a>
<h3 id="安装oracle"><a href="#安装oracle" class="headerlink" title="安装oracle"></a>安装oracle</h3><h4 id="基于centOS-7-安装oracle12c"><a href="#基于centOS-7-安装oracle12c" class="headerlink" title="基于centOS 7 安装oracle12c"></a>基于centOS 7 安装oracle12c</h4><h5 id="依赖安装，环境准备"><a href="#依赖安装，环境准备" class="headerlink" title="依赖安装，环境准备"></a>依赖安装，环境准备</h5><p>&emsp;&emsp;<strong>以下操作请登录 root 用户执行</strong>。</p>
<ul>
<li>安装依赖</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install binutils compat-libcap1 compat-libstdc++-33 compat-libstdc++-33.i686 gcc gcc-c++ glibc glibc.i686 glibc-devel glibc-devel.i686 ksh libgcc libgcc.i686 libstdc++ libstdc++.i686 libstdc++-devel libstdc++-devel.i686 libaio libaio.i686 libaio-devel libaio-devel.i686 ibXext ibXext.i686 libX11 libX11.i686 libxcb libxcb.i686 libXi libXi.i686 make sysstat</span><br></pre></td></tr></table></figure>
<ul>
<li>修改系统内核参数</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/sysctl.conf</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在文件末尾加以下几行</span></span><br><span class="line">kernel.shmmni=4096</span><br><span class="line">kernel.sem=250 32000 100 128</span><br><span class="line">fs.file-max=6815744</span><br><span class="line">fs.aio-max-nr=1048576</span><br><span class="line">net.ipv4.ip_local_port_range=9000 65500</span><br><span class="line">net.core.rmem_default=262144</span><br><span class="line">net.core.rmem_max=4194304</span><br><span class="line">net.core.wmem_default=262144</span><br><span class="line">net.core.wmem_max=1048576</span><br></pre></td></tr></table></figure>
<ul>
<li>使修改的内核参数生效</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl -p</span><br></pre></td></tr></table></figure>
<ul>
<li>创建用户和组</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">groupadd oinstall  </span><br><span class="line">groupadd dba  </span><br><span class="line">groupadd oper  </span><br><span class="line">useradd -g oinstall -G dba,oper oracle</span><br></pre></td></tr></table></figure>
<ul>
<li>设置 oracle 用户密码</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">passwd oracle</span><br></pre></td></tr></table></figure>
<ul>
<li>限制资源参数</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/pam.d/login</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在文件末尾加以下几行</span></span><br><span class="line">session    required     pam_selinux.so open</span><br><span class="line">session    required     pam_namespace.so</span><br><span class="line"></span><br><span class="line">session    required     pam_limits.so</span><br><span class="line"></span><br><span class="line">session    optional     pam_keyinit.so force revoke</span><br><span class="line">session    include      system-auth</span><br><span class="line">session   optional     pam_ck_connector.so</span><br></pre></td></tr></table></figure>
<ul>
<li>修改用户限制</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/security/limits.conf</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 在文件末尾加以下几行</span></span><br><span class="line">oracle  soft  nproc   2047</span><br><span class="line">oracle  hard  nproc   16384</span><br><span class="line">oracle  soft  nofile  1024</span><br><span class="line">oracle  hard  nofile  65536</span><br><span class="line">oracle  soft  stack   10240</span><br><span class="line">oracle  hard  stack   32768</span><br></pre></td></tr></table></figure>
<ul>
<li>创建交换空间</li>
</ul>
<p>&emsp;&emsp;若系统已有交换空间且大于8G，则这一步不用执行，oracle 大小大概在7.*G,若交换空间小于所需空间，安装时会安装失败。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dd <span class="keyword">if</span>=/dev/zero of=/var/swap bs=1024 count=8388608</span><br><span class="line">mkswap /var/swap</span><br><span class="line">chmod 0600 /var/swap</span><br><span class="line">swapon /var/swap</span><br></pre></td></tr></table></figure>
<ul>
<li>系统启动时自动启用交换分区</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/fstab</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/var/swap swap swap default 0 0</span><br></pre></td></tr></table></figure>
<ul>
<li>创建目录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mkdir -p /u01/app</span><br></pre></td></tr></table></figure>
<ul>
<li>修改目录属主和属组</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chown -R oracle:oinstall /u01/app</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<strong>以下操作请登录 oracle 用户执行</strong>。</p>
<ul>
<li>配置环境</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi ~/.bash_profile</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">umask</span> 022</span><br><span class="line"><span class="built_in">export</span> ORACLE_BASE=/u01/app/oracle</span><br></pre></td></tr></table></figure>
<ul>
<li>配置立即生效</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bash_profile</span><br></pre></td></tr></table></figure>
<ul>
<li>下载安装文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -b -c -O linuxx64_12201_database.zip https://download.oracle.com/otn/linux/oracle12c/122010/linuxx64_12201_database.zip?AuthParam=1581938083_d20f5a2be1ce563b28554044efc3c94d</span><br></pre></td></tr></table></figure>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unzip linuxx64_12201_database.zip</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里选择 oracle12c 的linux 64位的版本。注意，官网下载需要登录，先登录，然后点击下载，在浏览器中复制下载链接使用wget进行下载，<code>-b</code>参数表明wget进行后台下载，<code>-c</code>参数表明断点续传，<code>-O</code>参数为下载的文件命名。<code>AuthParam</code>参数有时效性，是oracle官网登录后点击下载然后生成的，所以这个下载链接需要自己去生成。</p>
<p><img src="/oracle/oracle-install-linux.png" alt="查找"></p>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unzip linuxx64_12201_database.zip</span><br></pre></td></tr></table></figure>
<h5 id="通过X11实现linux图形化界面本地显示-MAC"><a href="#通过X11实现linux图形化界面本地显示-MAC" class="headerlink" title="通过X11实现linux图形化界面本地显示-MAC"></a>通过X11实现linux图形化界面本地显示-MAC</h5><p>&emsp;&emsp;Windows环境使用xshell连接centos显示图形化界面可自行百度。mac下通过 Xquartz 协助完成图形化界面在本地显示的需求。</p>
<ul>
<li>mac下载Xquartz</li>
</ul>
<p>&emsp;&emsp;去<a href="https://www.xquartz.org/" target="_blank" rel="noopener">官网</a>下载安装包并按照提示安装。</p>
<ul>
<li>服务端设置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/ssh/sshd_config</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">X11Forwarding yes</span><br><span class="line">X11DisplayOffset 10</span><br><span class="line">X11UseLocalhost no</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl restart sshd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>mac设置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /private/etc/ssh/sshd_config</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">X11Forwarding yes</span><br><span class="line">X11UseLocalhost no</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;重启mac。</p>
<h5 id="安装数据库软件"><a href="#安装数据库软件" class="headerlink" title="安装数据库软件"></a>安装数据库软件</h5><ul>
<li>登录oracle用户</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -X oracle@ip</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用 <code>-X</code> 即使用 XQuartz 。</p>
<ul>
<li>尝试安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> tmp/</span><br><span class="line">./database/runInstaller</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若出现下面错误，则登录root用户检查<code>/usr/bin/xdpyinfo</code>是否存在。</p>
<p><img src="/oracle/oracle-install-test.png" alt="查找"></p>
<ul>
<li>下载xdpyinfo并设置xhost</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install xdpyinfo</span><br><span class="line"><span class="built_in">export</span> DISPLAY=:1.0</span><br><span class="line">xhost +</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若提示xhost找不到，则安装所以依赖。查找缺失依赖<code>yum whatprovides &quot;*/xhost&quot;</code>,安装依赖<code>yum -y install ***</code>,安装完成再执行<code>xhost +</code>。若报错<code>unable to open display &quot;&quot;</code>，则先安装vnc服务，再设置xhost。</p>
<ul>
<li>vnc服务安装</li>
</ul>
<p>&emsp;&emsp;终端输入<code>vncserver</code>测试是否安装了vnc服务，若未安装则安装vnc。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install tigervnc-server</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vncserver</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;按提示键入用户名密码。</p>
<ul>
<li>设置 xhost</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> DISPLAY=localhost:1</span><br><span class="line">xhost +</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;设置好xhost后退出root用户登录oracle用户<code>ssh -X oracle@ip</code>。</p>
<ul>
<li>设置 DISPLAY</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> DISPLAY=localhost:1.0</span><br></pre></td></tr></table></figure>
<ul>
<li>尝试安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> tmp/</span><br><span class="line">./database/runInstaller</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;出现下图，那基本上就是可以，稍等一会，安装界面就会在本地显示了。若弹出的界面中文乱码了，最简单的方式就是设置零时环境变量，将语言设置成英文<code>export LANG=en_US.UTF-8</code>。也有可能会出现一切正常但是安装画面并未显示的情况，此时我们退出重新登录oracle用户，查看DISPLAY变量是否有值，若有值可以先不设置临时值，直接运行脚本看看能否显示安装界面。</p>
<p><img src="/oracle/oracle-install-run.png" alt="查找"></p>
<p>&emsp;&emsp;下面就是正常的安装了。</p>
<p><img src="/oracle/oracle-install-1.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-2.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-3.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-4.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-5.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-6.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-7.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-8.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-9.png" alt="查找"></p>
<p>&emsp;&emsp;这一步报错了，那么我们根据他的错误提示解决就好了。这里是说系统缺少一个工具，那么我们登录root用户安装一下<code>yum -y install smartmontools</code>,然后点击<code>Check Again</code>。</p>
<p><img src="/oracle/oracle-install-10.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-11.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-12.png" alt="查找"></p>
<p>&emsp;&emsp;这一步按照他的提示去root用户下执行这两个文件。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh /u01/app/oraInventory/orainstRoot.sh</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh /u01/app/oracle/product/12.2.0/dbhome_1/root.sh</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里会让你输入服务器的bin目录，默认是<code>/usr/local/bin</code>，然后会让你确认是否安装Oracle跟踪文件分析器，这里输入yes。两个脚本执行完成后，在安装界面点击OK，继续下一步。</p>
<p><img src="/oracle/oracle-install-13.png" alt="查找"></p>
<p>&emsp;&emsp;到这里，数据库软件就安装完成了。</p>
<ul>
<li>配置环境变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi ~/.bash_profile</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> ORACLE_HOME=/u01/app/oracle/product/12.2.0/dbhome_1</span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:<span class="variable">$ORACLE_HOME</span>/bin</span><br><span class="line"></span><br><span class="line"><span class="built_in">export</span> ORACLE_SID=orcl</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> ~/.bash_profile</span><br></pre></td></tr></table></figure>
<ul>
<li>创建监听</li>
</ul>
<p>&emsp;&emsp;退出当前登录并重新登录oracle用户。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> LANG=en_US.UTF-8</span><br><span class="line">netca</span><br></pre></td></tr></table></figure>
<p><img src="/oracle/oracle-install-14.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-15.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-16.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-17.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-18.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-19.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-20.png" alt="查找"></p>
<p>&emsp;&emsp;此时，我们的监听就创建完成了，在弹出的页面点击finish就可以退出了。</p>
<ul>
<li>创建数据库</li>
</ul>
<p>&emsp;&emsp;退出当前登录并重新登录oracle用户。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> LANG=en_US.UTF-8</span><br><span class="line">dbca</span><br></pre></td></tr></table></figure>
<p><img src="/oracle/oracle-install-21.png" alt="查找"></p>
<p><img src="/oracle/oracle-install-22.png" alt="查找"></p>
<p>&emsp;&emsp;这里填入管理员，例如sys/system用户的密码，勾选创建容器服务器，并设置容器名称。</p>
<p>&emsp;&emsp;接下来一路默认就好了。</p>
<p>&emsp;&emsp;登录oracle用户，启动监听和容器。</p>
<ul>
<li>启动监听</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsnrctl start</span><br></pre></td></tr></table></figure>
<ul>
<li>启动容器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sqlplus sys as sysdba</span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">startup</span><br></pre></td></tr></table></figure>
<h4 id="docker中安装oracle"><a href="#docker中安装oracle" class="headerlink" title="docker中安装oracle"></a>docker中安装oracle</h4><h5 id="查找并拉取镜像"><a href="#查找并拉取镜像" class="headerlink" title="查找并拉取镜像"></a>查找并拉取镜像</h5><p>&emsp;&emsp;我们去 <a href="hub.docker.com">hub.docker.com</a> 上搜索 <a href="https://hub.docker.com/_/oracle-database-enterprise-edition" target="_blank" rel="noopener">oracle</a> ，选择类型为database，如下图，这个镜像是oracle官方提供的，我们就选择这个好了。</p>
<p><img src="/oracle/oracle-docker-hub.png" alt="查找"></p>
<p>&emsp;&emsp;不要说话，照着做就完事了，官方镜像，逼格高点，需填写一些东西。要先登录docker hub，没账号的自己建一个。</p>
<p><img src="/oracle/oracle-show-proceed.png" alt="查找"></p>
<p><img src="/oracle/oracle-docker-info.png" alt="查找"></p>
<p>&emsp;&emsp;复制去下载镜像，terminal中可能还需要docker login一下，得用你docker hub中login的账号。镜像下载速度看几个方面，一个就是你docker的源用的哪里的，一个就是你的网速，网的稳定性。</p>
<p>&emsp;&emsp;左边的是官网友情提供的一些说明，比如连接的一些基本参数，如何指定一个外部配置文件，怎么连接这个容器。一个很重要的信息是sys管理员的密码是<code>Oradoc_db1</code></p>
<p><img src="/oracle/oracle-docker-show-detail.png" alt="查找"></p>
<h5 id="运行镜像"><a href="#运行镜像" class="headerlink" title="运行镜像"></a>运行镜像</h5><ul>
<li>查看我们刚才拉取的镜像</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker images</span><br></pre></td></tr></table></figure>
<p><img src="/oracle/docker-image.png" alt="运行结果"></p>
<ul>
<li>运行</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -d -it --name oracle12.2.0.1 -p 1521:1521 12a359cd0528</span><br></pre></td></tr></table></figure>
<blockquote>
<p>参数解释<br>run:run命令就是用来运行镜像文件的呀…通常使用docker run 镜像名称或者镜像的IMAGE ID 组成来运行镜像<br>-d:后台运行<br>–name:重命名运行镜像生成的容器<br>-p:端口映射,如上面的,将容器的1521端口映射到宿主机的1521端口,将容器的8080端口映射到宿主机的8081端口<br>-v:可以将容器的指定目录映射到宿主机的指定目录,这个操作很骚气,很多人喜欢,但是我觉得没有必要啊,防止误删容器数据丢失??你别瞎几把删除容器不就好了…<br>-it:-i以交互模式运行容器,-t为容器重新分配一个伪输入终端.这两个指令通常是搭配在一起使用.使用这个命令就是在生成容器之后模拟出来一个终端让你可以对这个容器进行操作,当然,像oracle,使用-it一般也需要在后面加上启动参数,选择以什么用户进入容器</p>
</blockquote>
<p>&emsp;&emsp;如下图所示，容器启动需要时间,等到状态变成healthy时就启动好了。</p>
<p><img src="/oracle/docker-start-image.png" alt="运行结果"></p>
<ul>
<li>连接</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -it oracle12.2.0.1 bash -c <span class="string">"source /home/oracle/.bashrc; sqlplus /nolog"</span></span><br></pre></td></tr></table></figure>
<p><img src="/oracle/oracle-docker-conn.png" alt="运行结果"></p>
<p>&emsp;&emsp;用户名<code>sys</code>，密码<code>Oradoc_db1</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -it oracle12.2.0.1 bash</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这是直接访问容器的目录，导入导出，查看日志等操作可能用到。</p>
<h3 id="数据库操作"><a href="#数据库操作" class="headerlink" title="数据库操作"></a>数据库操作</h3><h4 id="启动和停止监听与容器"><a href="#启动和停止监听与容器" class="headerlink" title="启动和停止监听与容器"></a>启动和停止监听与容器</h4><ul>
<li>启动监听</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsnrctl start</span><br></pre></td></tr></table></figure>
<ul>
<li>关闭监听</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsnrctl stop</span><br></pre></td></tr></table></figure>
<ul>
<li>查看状态</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsnrctl status</span><br></pre></td></tr></table></figure>
<ul>
<li>启动容器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sqlplus sys as sysdba</span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">startup</span><br></pre></td></tr></table></figure>
<ul>
<li>关闭容器</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sqlplus sys as sysdba</span><br></pre></td></tr></table></figure>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">shutdown</span><br></pre></td></tr></table></figure>
<h4 id="连接数据库"><a href="#连接数据库" class="headerlink" title="连接数据库"></a>连接数据库</h4><ul>
<li>在oracle用户下执行：</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sqlplus sys/oracle as sysdba</span><br></pre></td></tr></table></figure>
<h4 id="创建表空间，用户"><a href="#创建表空间，用户" class="headerlink" title="创建表空间，用户"></a>创建表空间，用户</h4><p>&emsp;&emsp;以下操作登录sys/system用户执行。</p>
<ul>
<li>创建表空间</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLESPACE</span> ferry <span class="keyword">DATAFILE</span> <span class="string">'/u01/app/oracle/admin/ORCL/dpdump/ferry.dbf'</span> <span class="keyword">SIZE</span> <span class="number">50</span>M <span class="keyword">AUTOEXTEND</span> <span class="keyword">ON</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;on后面可以指定每次自增长空间大小,路径为绝对路径，不存在则报错。</p>
<ul>
<li>创建用户并指定表空间</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">user</span> ferry <span class="keyword">identified</span> <span class="keyword">by</span> ferry <span class="keyword">default</span> <span class="keyword">tablespace</span> ferry;</span><br></pre></td></tr></table></figure>
<ul>
<li>用户授权</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">grant</span> dba <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">connect</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">alter</span> <span class="keyword">session</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">any</span> <span class="keyword">context</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">procedure</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">sequence</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">session</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">synonym</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">type</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">user</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">view</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">any</span> <span class="keyword">table</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> DEBUG <span class="keyword">CONNECT</span> <span class="keyword">SESSION</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">query</span> rewrite <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">select</span> <span class="keyword">any</span> dictionary <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">unlimited</span> <span class="keyword">tablespace</span> <span class="keyword">to</span> ferry;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">read</span>,write <span class="keyword">on</span> <span class="keyword">directory</span> DATA_PUMP_DIR <span class="keyword">to</span> ferry;</span><br></pre></td></tr></table></figure>
<h4 id="删除用户，表空间"><a href="#删除用户，表空间" class="headerlink" title="删除用户，表空间"></a>删除用户，表空间</h4><p>&emsp;&emsp;以下操作登录sys/system用户执行。</p>
<ul>
<li>删除用户</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">drop</span> <span class="keyword">user</span> ferry <span class="keyword">cascade</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;加上cascade是将用户的数据库数据一并删除，并没有删除相应的表空间。</p>
<ul>
<li>删除表空间</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLESPACE</span> ferry <span class="keyword">INCLUDING</span> <span class="keyword">CONTENTS</span> <span class="keyword">AND</span> <span class="keyword">DATAFILES</span>;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若删除的表空间是默认的表空间，则会提示不能删除默认表空间，需要先修改默认表空间然后才能删除。</p>
<ul>
<li>查看并修改默认表空间</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看默认表空间</span></span><br><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> database_properties <span class="keyword">where</span> property_name=<span class="string">'DEFAULT_PERMANENT_TABLESPACE'</span>;</span><br><span class="line"><span class="comment">-- 修改默认表空间</span></span><br><span class="line"><span class="keyword">alter</span> <span class="keyword">database</span> <span class="keyword">default</span> <span class="keyword">tablespace</span> <span class="keyword">USERS</span>;</span><br></pre></td></tr></table></figure>
<h4 id="wm-concat"><a href="#wm-concat" class="headerlink" title="wm_concat"></a>wm_concat</h4><p>&emsp;&emsp;在oracle11g和12c上已经摒弃了wm_concat函数，但是我们的程序中可能依旧使用了这个函数，为了数据库的迁移省事，可以选择自己创建一个wm_concat函数。但是注意，即使创建了该函数，在使用的过程中，也需要用to_char(wm_concat())方式，才能完全替代之前的应用。</p>
<h5 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h5><p>&emsp;&emsp;以下操作登录sys/system用户执行。</p>
<ul>
<li>解锁WMSYS用户</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">user</span> wmsys <span class="keyword">account</span> <span class="keyword">unlock</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li>授权WMSYS用户</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">grant</span> dba <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">connect</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">alter</span> <span class="keyword">session</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">any</span> <span class="keyword">context</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">procedure</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">sequence</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">session</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">synonym</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">table</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">type</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">user</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">view</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">create</span> <span class="keyword">any</span> <span class="keyword">table</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> DEBUG <span class="keyword">CONNECT</span> <span class="keyword">SESSION</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">query</span> rewrite <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">select</span> <span class="keyword">any</span> dictionary <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">unlimited</span> <span class="keyword">tablespace</span> <span class="keyword">to</span> wmsys;</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">read</span>,write <span class="keyword">on</span> <span class="keyword">directory</span> DATA_PUMP_DIR <span class="keyword">to</span> wmsys;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;以WMSYS用户连接数据库并执行下面的操作.</p>
<ul>
<li>创建包，包体和函数</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">TYPE</span> WM_CONCAT_IMPL <span class="keyword">AS</span> <span class="keyword">OBJECT</span></span><br><span class="line"><span class="comment">-- AUTHID CURRENT_USER AS OBJECT</span></span><br><span class="line">(</span><br><span class="line">CURR_STR <span class="built_in">VARCHAR2</span>(<span class="number">32767</span>), </span><br><span class="line"><span class="keyword">STATIC</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEINITIALIZE(SCTX <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEITERATE(<span class="keyword">SELF</span> <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL,</span><br><span class="line">P1 <span class="keyword">IN</span> <span class="built_in">VARCHAR2</span>) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATETERMINATE(<span class="keyword">SELF</span> <span class="keyword">IN</span> WM_CONCAT_IMPL,</span><br><span class="line">RETURNVALUE <span class="keyword">OUT</span> <span class="built_in">VARCHAR2</span>,</span><br><span class="line">FLAGS <span class="keyword">IN</span> <span class="built_in">NUMBER</span>)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEMERGE(<span class="keyword">SELF</span> <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL,</span><br><span class="line">SCTX2 <span class="keyword">IN</span> WM_CONCAT_IMPL) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span></span><br><span class="line">);</span><br><span class="line">/</span><br><span class="line"> </span><br><span class="line"><span class="comment">-- 定义类型body:</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">TYPE</span> <span class="keyword">BODY</span> WM_CONCAT_IMPL</span><br><span class="line"><span class="keyword">IS</span></span><br><span class="line"><span class="keyword">STATIC</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEINITIALIZE(SCTX <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">NUMBER</span></span><br><span class="line"><span class="keyword">IS</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">SCTX := WM_CONCAT_IMPL(<span class="literal">NULL</span>) ;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATEITERATE(SELF IN OUT WM_CONCAT_IMPL,</span><br><span class="line">P1 IN VARCHAR2)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">IF</span>(CURR_STR <span class="keyword">IS</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>) <span class="keyword">THEN</span></span><br><span class="line">CURR_STR := CURR_STR || <span class="string">','</span> || P1;</span><br><span class="line">ELSE</span><br><span class="line">CURR_STR := P1;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">IF</span>;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATETERMINATE(SELF IN WM_CONCAT_IMPL,</span><br><span class="line">RETURNVALUE OUT VARCHAR2,</span><br><span class="line">FLAGS IN NUMBER)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">RETURNVALUE := CURR_STR ;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATEMERGE(SELF IN OUT WM_CONCAT_IMPL,</span><br><span class="line">SCTX2 IN WM_CONCAT_IMPL)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">IF</span>(SCTX2.CURR_STR <span class="keyword">IS</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>) <span class="keyword">THEN</span></span><br><span class="line">SELF.CURR_STR := SELF.CURR_STR || <span class="string">','</span> || SCTX2.CURR_STR ;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">IF</span>;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">/</span><br><span class="line"><span class="comment">--自定义行变列函数:</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">FUNCTION</span> wm_concat(P1 <span class="built_in">VARCHAR2</span>)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">VARCHAR2</span> <span class="keyword">AGGREGATE</span> <span class="keyword">USING</span> WM_CONCAT_IMPL ;</span><br><span class="line">/</span><br></pre></td></tr></table></figure>
<ul>
<li>创建同义词并授权</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">public</span> <span class="keyword">synonym</span> WM_CONCAT_IMPL <span class="keyword">for</span> wmsys.WM_CONCAT_IMPL</span><br><span class="line">/</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">public</span> <span class="keyword">synonym</span> wm_concat <span class="keyword">for</span> wmsys.wm_concat</span><br><span class="line">/</span><br><span class="line"> </span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">execute</span> <span class="keyword">on</span> WM_CONCAT_IMPL <span class="keyword">to</span> <span class="keyword">public</span></span><br><span class="line">/</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">execute</span> <span class="keyword">on</span> wm_concat <span class="keyword">to</span> <span class="keyword">public</span></span><br><span class="line">/</span><br></pre></td></tr></table></figure>
<h5 id="方法二"><a href="#方法二" class="headerlink" title="方法二"></a>方法二</h5><p>&emsp;&emsp;若容器中没有用户 wmsys，可以随便选择一个用户创建此函数，例如，这里使用ferry用户创建该函数。</p>
<p>&emsp;&emsp;以 ferry 用户连接数据库并执行下面的操作.</p>
<ul>
<li>创建包，包体和函数</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">TYPE</span> WM_CONCAT_IMPL <span class="keyword">AS</span> <span class="keyword">OBJECT</span></span><br><span class="line"><span class="comment">-- AUTHID CURRENT_USER AS OBJECT</span></span><br><span class="line">(</span><br><span class="line">CURR_STR <span class="built_in">VARCHAR2</span>(<span class="number">32767</span>), </span><br><span class="line"><span class="keyword">STATIC</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEINITIALIZE(SCTX <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEITERATE(<span class="keyword">SELF</span> <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL,</span><br><span class="line">P1 <span class="keyword">IN</span> <span class="built_in">VARCHAR2</span>) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATETERMINATE(<span class="keyword">SELF</span> <span class="keyword">IN</span> WM_CONCAT_IMPL,</span><br><span class="line">RETURNVALUE <span class="keyword">OUT</span> <span class="built_in">VARCHAR2</span>,</span><br><span class="line">FLAGS <span class="keyword">IN</span> <span class="built_in">NUMBER</span>)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">NUMBER</span>,</span><br><span class="line"><span class="keyword">MEMBER</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEMERGE(<span class="keyword">SELF</span> <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL,</span><br><span class="line">SCTX2 <span class="keyword">IN</span> WM_CONCAT_IMPL) <span class="keyword">RETURN</span> <span class="built_in">NUMBER</span></span><br><span class="line">);</span><br><span class="line">/</span><br><span class="line"> </span><br><span class="line"><span class="comment">-- 定义类型body:</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">TYPE</span> <span class="keyword">BODY</span> WM_CONCAT_IMPL</span><br><span class="line"><span class="keyword">IS</span></span><br><span class="line"><span class="keyword">STATIC</span> <span class="keyword">FUNCTION</span> ODCIAGGREGATEINITIALIZE(SCTX <span class="keyword">IN</span> <span class="keyword">OUT</span> WM_CONCAT_IMPL)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">NUMBER</span></span><br><span class="line"><span class="keyword">IS</span></span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">SCTX := WM_CONCAT_IMPL(<span class="literal">NULL</span>) ;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATEITERATE(SELF IN OUT WM_CONCAT_IMPL,</span><br><span class="line">P1 IN VARCHAR2)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">IF</span>(CURR_STR <span class="keyword">IS</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>) <span class="keyword">THEN</span></span><br><span class="line">CURR_STR := CURR_STR || <span class="string">','</span> || P1;</span><br><span class="line">ELSE</span><br><span class="line">CURR_STR := P1;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">IF</span>;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATETERMINATE(SELF IN WM_CONCAT_IMPL,</span><br><span class="line">RETURNVALUE OUT VARCHAR2,</span><br><span class="line">FLAGS IN NUMBER)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line">RETURNVALUE := CURR_STR ;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">MEMBER FUNCTION ODCIAGGREGATEMERGE(SELF IN OUT WM_CONCAT_IMPL,</span><br><span class="line">SCTX2 IN WM_CONCAT_IMPL)</span><br><span class="line">RETURN NUMBER</span><br><span class="line">IS</span><br><span class="line"><span class="keyword">BEGIN</span></span><br><span class="line"><span class="keyword">IF</span>(SCTX2.CURR_STR <span class="keyword">IS</span> <span class="keyword">NOT</span> <span class="literal">NULL</span>) <span class="keyword">THEN</span></span><br><span class="line">SELF.CURR_STR := SELF.CURR_STR || <span class="string">','</span> || SCTX2.CURR_STR ;</span><br><span class="line"><span class="keyword">END</span> <span class="keyword">IF</span>;</span><br><span class="line">RETURN ODCICONST.SUCCESS;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line"><span class="keyword">END</span>;</span><br><span class="line">/</span><br><span class="line"><span class="comment">--自定义行变列函数:</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">OR</span> <span class="keyword">REPLACE</span> <span class="keyword">FUNCTION</span> wm_concat(P1 <span class="built_in">VARCHAR2</span>)</span><br><span class="line"><span class="keyword">RETURN</span> <span class="built_in">VARCHAR2</span> <span class="keyword">AGGREGATE</span> <span class="keyword">USING</span> WM_CONCAT_IMPL ;</span><br><span class="line">/</span><br></pre></td></tr></table></figure>
<ul>
<li>创建同义词并授权</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">create</span> <span class="keyword">public</span> <span class="keyword">synonym</span> WM_CONCAT_IMPL <span class="keyword">for</span> ferry.WM_CONCAT_IMPL</span><br><span class="line">/</span><br><span class="line"><span class="keyword">create</span> <span class="keyword">public</span> <span class="keyword">synonym</span> wm_concat <span class="keyword">for</span> ferry.wm_concat</span><br><span class="line">/</span><br><span class="line"> </span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">execute</span> <span class="keyword">on</span> WM_CONCAT_IMPL <span class="keyword">to</span> <span class="keyword">public</span></span><br><span class="line">/</span><br><span class="line"><span class="keyword">grant</span> <span class="keyword">execute</span> <span class="keyword">on</span> wm_concat <span class="keyword">to</span> <span class="keyword">public</span></span><br><span class="line">/</span><br></pre></td></tr></table></figure>
<h4 id="数据库导入导出"><a href="#数据库导入导出" class="headerlink" title="数据库导入导出"></a>数据库导入导出</h4><p>&emsp;&emsp;这里使用的是 impdp/expdp 命令进行导入导出的,需要在oracle用户下执行命令。</p>
<ul>
<li>数据导出</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">expdp user_name/password@127.0.0.1:1521/ORCL directory=DATA_PUMP_DIR dumpfile=ferry.dmp [version=11.2.0.2.0]</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;用户名和密码就是你要导出的数据库所属的用户,IP地址和端口号是服务器的,服务名是你这个用户登录所使用的服务名,当然,我觉得这里如果你是用SID的,那么替换成SID我觉得也没问题,目录指定你要到处的dump的存放位置,这个我们一般使用oracle自带的那么目录,如下图,指定文件名就没什么好说的了,后缀别搞错就行了,最后面的版本号是可选的,当你将高版本的数据迁移到低版本时,你需要指定导出dump的版本,这样你在低版本的数据库中执行导入才不会有问题,但是,高版本导出低版本数据也是有可能发生错误的,比如你高版本oracle服务器端使用了高版本的特性,但是你指定的导出版本没有这个特性,那么就可能会报错啊(事实是,我指定低版本号执行导出确实有过报错,大概是数据类型过长还是什么的我忘了,以上导出版本冲突是我的猜想…).</p>
<p><img src="/oracle/oracle-directories-1.png" alt="目录"></p>
<p><img src="/oracle/oracle-directories-2.png" alt="目录"></p>
<p>&emsp;&emsp;上图1是通过客户端<code>oracle sql developer</code>登录你要导出的用户查看当前用户的目录访问权限并查看指定目录的系统路径,在导出成功后可以在此系统路径下找到导出的日志文件和dump.</p>
<p>&emsp;&emsp;上图2是通过客户端<code>PL/SQL</code>登录你要导出的用户查看当前用户的目录访问权限,在你想查看的目录上鼠标右键-edit查看该目录的系统路径.</p>
<p>&emsp;&emsp;你以为导出就到此结束了么,或者你以为导出成功拿到dump就结束了么??不!!!你得拿到你导出的这个dump的schema和tablespace信息,这个信息没拿到,你怎么执行导入操作呢?</p>
<ul>
<li>获取导出数据的schema和表空间</li>
</ul>
<p>&emsp;&emsp;schema一般情况下都是和用户一一对应的,所以我们这直接取导出数据的用户名就好了,表空间就需要在服务器上使用sqlplus连接该用户,执行<code>select default_tablespace from user_users;</code>查看当前用户的表空间,或者可以使用客户端连接登录该用户执行sql,多样化嘛,怎么方便怎么来.</p>
<ul>
<li>数据导入</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">impdp user_name/password@127.0.0.1:1521/ORCL remap_schema=from_schema:to_schema remap_tablespace=from_tablespace:to_tablespace directory=DATA_PUMP_DIR dumpfile=ferry.dmp</span><br></pre></td></tr></table></figure>
<h4 id="事务锁查询与杀死"><a href="#事务锁查询与杀死" class="headerlink" title="事务锁查询与杀死"></a>事务锁查询与杀死</h4><p>&emsp;&emsp;查询与杀死操作在同一个session中处理。</p>
<ul>
<li>查询</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> sess.sid,</span><br><span class="line">sess.serial<span class="comment">#,</span></span><br><span class="line">lo.oracle_username,</span><br><span class="line">lo.os_user_name,</span><br><span class="line">ao.object_name,</span><br><span class="line">lo.locked_mode</span><br><span class="line"><span class="keyword">from</span> v$locked_object lo,</span><br><span class="line">dba_objects ao,</span><br><span class="line">v$<span class="keyword">session</span> sess</span><br><span class="line"><span class="keyword">where</span> ao.object_id = lo.object_id <span class="keyword">and</span> lo.session_id = sess.sid;</span><br></pre></td></tr></table></figure>
<ul>
<li>杀死</li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">ALTER</span> <span class="keyword">SYSTEM</span> <span class="keyword">KILL</span> <span class="keyword">SESSION</span> <span class="string">'SID,SERIR#'</span>;</span><br></pre></td></tr></table></figure>
<h4 id="PDB容器的操作"><a href="#PDB容器的操作" class="headerlink" title="PDB容器的操作"></a>PDB容器的操作</h4><ul>
<li><strong>查看PDB容器</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">show</span> pdbs</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>启动PDB容器</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">pluggable</span> <span class="keyword">database</span> pdborcl1 <span class="keyword">open</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>关闭PDB容器</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">pluggable</span> <span class="keyword">database</span> pdborcl1 <span class="keyword">close</span>;</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>切换会话</strong></li>
</ul>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">alter</span> <span class="keyword">session</span> <span class="keyword">set</span> <span class="keyword">container</span>=odborcl1;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>docker中安装oracle</tag>
        <tag>oracle数据迁移</tag>
      </tags>
  </entry>
  <entry>
    <title>redis</title>
    <url>/redis.html</url>
    <content><![CDATA[<p>&emsp;&emsp;redis学习之路。</p>
<a id="more"></a>
<h3 id="简单配置"><a href="#简单配置" class="headerlink" title="简单配置"></a>简单配置</h3><p>&emsp;&emsp;使用yum安装的redis，他的配置文件的路径为<code>/etc/redis.conf</code>，修改此配置文件以满足我们的要求。</p>
<ul>
<li>绑定IP</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">bind</span> 10.15.159.19 127.0.0.1</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;bind可以绑定多个IP地址，中间用空格分隔，也可以将bind注释掉。注意：<font color="#dd0000">你只能通过bind的IP来访问redis服务</font>。将bind注释掉则可以通过任意IP访问redis服务。</p>
<ul>
<li>端口号</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">port 6379</span><br></pre></td></tr></table></figure>
<ul>
<li>数据库数量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">databases 16</span><br></pre></td></tr></table></figure>
<ul>
<li>连接密码(默认不需要)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">requirepass foobared</span><br></pre></td></tr></table></figure>
<ul>
<li>查看 redis 当前使用的配置文件目录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli info | grep config</span><br></pre></td></tr></table></figure>
<h3 id="redis-数据结构"><a href="#redis-数据结构" class="headerlink" title="redis 数据结构"></a>redis 数据结构</h3><p>&emsp;&emsp;redis 一共有五种数据结构，分别为String、List、hash、Set、Zset。</p>
<p>&emsp;&emsp;原子操作：在多进程（线程）访问共享资源时，能够确保所有其他的进程（线程）都不在同一时间内访问相同的资源。原子操作（atomic operation）是不需要synchronized，这是Java多线程编程的老生常谈了。所谓原子操作是指不会被线程调度机制打断的操作；这种操作一旦开始，就一直运行到结束，中间不会有任何 context switch （切换到另一个线程）。</p>
<h4 id="字符串类型-String"><a href="#字符串类型-String" class="headerlink" title="字符串类型(String)"></a>字符串类型(String)</h4><p>&emsp;&emsp;空间分配原则：当len小于IMB（1024*1024）时增加字符串分配空间大小为原来的2倍，当len大于等于1M时每次分配 额外多分配1M的空间。取字符串的长度的时间复杂度为O(1)</p>
<ul>
<li>赋值(set key value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set</span> hello world</span><br></pre></td></tr></table></figure>
<ul>
<li>取值(get key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">get hello</span><br></pre></td></tr></table></figure>
<ul>
<li>自增(incr key)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">incr num</span><br></pre></td></tr></table></figure>
<ul>
<li>自减(decr key)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">decr num</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自增/自减该key对应的value值都会相应的加/减1，若该key不存在，则先<code>set key 0</code>然后执行加/减1操作，最后将执行结果返回。如果该key的值不是数字则报错。</p>
<ul>
<li>自增N(incrby key increment)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">incrby count 5</span><br></pre></td></tr></table></figure>
<ul>
<li>自减N(decrby key increment)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">decrby count 5</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;自增N/自减N同自增/自减。</p>
<ul>
<li>增加浮点数(incrbyfloat key increment)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">incrbyfloat float1 3</span><br></pre></td></tr></table></figure>
<ul>
<li>向尾部追加(append key value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">append hello !</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;向字符串尾部追加内容，返回追加后的字符串长度，若key不存在，则新增。</p>
<ul>
<li>字符串长度(strlen key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">strlen hello</span><br></pre></td></tr></table></figure>
<ul>
<li>同时设置多个key(mset key value [key value …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mset str1 hehe str2 xixi str3 haha</span><br></pre></td></tr></table></figure>
<ul>
<li>同时获取多个key的值(mget key [key …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mget str1 str2 str3</span><br></pre></td></tr></table></figure>
<h4 id="列表-list"><a href="#列表-list" class="headerlink" title="列表(list)"></a>列表(list)</h4><p>&emsp;&emsp;列表类型存储了一个有序的字符串列表。常用的操作是向两端插入新的元素。时间复杂度为 O（1）。结构为一个链表。记录头和尾的地址。看到这里，Redis 数据类型的列表类型一个重大的作用呼之欲出，那就是队列。新来的请求插入到尾部，新处理过的从头部删除。列表类型就是一个下标从 0 开始的数组。由于是链表存储，那么越靠近头和尾的元素操作越快，越靠近中间则越慢.</p>
<ul>
<li>向头部插入(lpush key value [value …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lpush list apple banana pear</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回列表的长度。向头部插入，先向头部插入apple，再向头部插入banana，最后向头部插入pear，所以操作后列表的第一个元素应该是pear。</p>
<ul>
<li>向尾部插入(rpush key value [value])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpush list orange grape</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回列表的长度。向尾部插入，orange，grape，所以操作后列表的最后一个元素应该是grape。</p>
<ul>
<li>列表元素个数(llen key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">llen list</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回列表的长度。若key不存在，返回0。</p>
<ul>
<li>获取列表的子列表(lrange key start end)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lrange list 0 -1</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回第 start 个到第 end 个元素的列表。包含 start 和 end。支持负数索引。-1 表示最后一个元素，-2 表示倒数第二个元素。</p>
<ul>
<li>获取指定索引值(lindex key index)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lindex list 0</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若index为负数则从尾部向前索引，-1为最后一个元素的位置。</p>
<ul>
<li>从头部弹出(lpop key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lpop list</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回被弹出的元素。</p>
<ul>
<li>从尾部弹出(rpop key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpop list</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回被弹出的元素。</p>
<ul>
<li>删除列表中指定值(lrem key count value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lrem list 1 apple</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;返回删除的数量。count为0，删除所有列表中指定的值；count为正数，从前往后删除count个指定的元素(count大于实际的元素数量时删除所有)；count为负数时，从后往前删除count个指定的元素(count大于实际的元素数量时删除所有)。</p>
<ul>
<li>设置索引和值(lset key index value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpush fruit apple pear banana orange</span><br><span class="line">lset fruit 0 <span class="string">"greet apple"</span></span><br><span class="line">lrange fruit 0 -1</span><br></pre></td></tr></table></figure>
<ul>
<li>保留片段，删除其它(ltrim key start end)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ltrim fruit 0 -2</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;保留start到end之间的所有元素，含start和end。其他全部删除。舒服从末尾向前，-1表示最后一个元素的下标值。</p>
<ul>
<li>向列表插入元素(linsert key before|after pivot value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">linsert fruit before pear apple</span><br><span class="line">linsert fruit after pear apple</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;从头部开始遍历，直到发现第一个pivot，根据before|after将value插入pivot之前还是之后。</p>
<ul>
<li>把一个列表的一个元素转到另一个列表(rpoplpush source destination)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpoplpush fruit list</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;将source列表的最后一个元素弹出并使用lpush操作插入到destination列表中。</p>
<h4 id="散列类型-hash"><a href="#散列类型-hash" class="headerlink" title="散列类型(hash)"></a>散列类型(hash)</h4><p>&emsp;&emsp;Redis 是以字典（关联数组）的形式存储的，一个 key 对应一个 value。在字符串类型中，value 只能是一个字符串。那么在散列类型中，value 对应的也是一个字典（关联数组）。那么就可以理解，Redis 的散列类型中，key 对应的 value 是一个二维数组。但是字段的值只可以是字符串。也就是说只能是二维数组，不能有更多的维度。</p>
<ul>
<li>赋值(hset key field value)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hset map name ferry</span><br></pre></td></tr></table></figure>
<ul>
<li>取值(hget key field)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hget map name</span><br></pre></td></tr></table></figure>
<ul>
<li>同一个key多个字段赋值(hmset key field value [field value …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hmset map birthday 1995-04-09 sex male</span><br></pre></td></tr></table></figure>
<ul>
<li>同一个KEY多个字段取值(hmget key field [field …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hmget map name birthday sex</span><br></pre></td></tr></table></figure>
<ul>
<li>获取KEY的所有字段和所有值(hgetall key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hgetall map</span><br></pre></td></tr></table></figure>
<ul>
<li>字段是否存在(hexists key field)(存在返回1，否则返回0)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexists map name</span><br><span class="line">hexists map age</span><br></pre></td></tr></table></figure>
<ul>
<li>当字段不存在时赋值(hsetnx key field value)(原子操作)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hsetnx map height 175</span><br><span class="line">hsetnx map name jack</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若字段存在返回0，否则设置字段的值并返回1。等效于hexists+hset，但该操作是原子操作，线程安全的。</p>
<ul>
<li>自增N(hincrby key field increment)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hincrby map height 2</span><br></pre></td></tr></table></figure>
<ul>
<li>删除字段(hdel key field [field …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hdel map height</span><br></pre></td></tr></table></figure>
<ul>
<li>只获取字段名(hkeys key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hkeys map</span><br></pre></td></tr></table></figure>
<ul>
<li>只获取字段值(hvals key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hvals map</span><br></pre></td></tr></table></figure>
<ul>
<li>获取字段数量(hlen key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hlen map</span><br></pre></td></tr></table></figure>
<h4 id="集合类型-set"><a href="#集合类型-set" class="headerlink" title="集合类型(set)"></a>集合类型(set)</h4><p>&emsp;&emsp;redis的集合和列表都可以存储多个字符串，它们之间的不同在于，列表可以存储多个相同的字符串，而集合则通过使用散列表（hashtable）来保证自已存储的每个字符串都是各不相同的(这些散列表只有键，但没有与键相关联的值)，redis中的集合是无序的。</p>
<ul>
<li>增加(sadd key member [member …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sadd s 1 a 2 b 3 c</span><br></pre></td></tr></table></figure>
<ul>
<li>删除(srem key member [member …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">srem s 1 2 3</span><br></pre></td></tr></table></figure>
<ul>
<li>获取集合所有元素(smembers key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">smembers s</span><br></pre></td></tr></table></figure>
<ul>
<li>判断指定集合中是否存在某个元素(sismember key member)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sismember s a</span><br></pre></td></tr></table></figure>
<ul>
<li>差集运算(sdiff key [key …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sadd b 1 2 3 a b c</span><br><span class="line">sdiff a b</span><br><span class="line">sdiff b a</span><br></pre></td></tr></table></figure>
<ul>
<li>交集运算(sinter key [key …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sinter s b</span><br></pre></td></tr></table></figure>
<ul>
<li>并集运算(sunion key [key …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sunion s b</span><br></pre></td></tr></table></figure>
<ul>
<li>差集/交集/并集运算结果存储在一个集合中(sdiffstore/sinterstore/sunionstore destination key [key …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sdiffstore bs b s</span><br><span class="line">sinterstore sbs b s</span><br><span class="line">sunionstore sb s b</span><br></pre></td></tr></table></figure>
<ul>
<li>随机获取元素(srandmember key [count])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">srandmember b -10</span><br></pre></td></tr></table></figure>
<p>&amp;emsp&emsp;若count不存在，随机从集合中获取一个元素输出；若count等于0，输出空；若count大于0，输出不重复的count个元素（count大于实际数量时输出集合中所有元素）；若count小于0，输出可重复的count个元素。</p>
<ul>
<li>随机弹出一个元素(spop key [count])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">spop b 10</span><br></pre></td></tr></table></figure>
<p>&amp;emsp&emsp;若count不存在，随机从集合中弹出一个元素并输出该元素；若count等于0，弹出0个元素并输出空；若count大于0，弹出不重复的count个元素（count大于实际数量时弹出集合中所有元素）并输出这些被弹出的元素；若count小于0，报错<code>(error) ERR index out of range</code>。</p>
<h4 id="有序集合-zset"><a href="#有序集合-zset" class="headerlink" title="有序集合(zset)"></a>有序集合(zset)</h4><p>&emsp;&emsp;有序集合和散列一样，都用于存储键值对：有序集合的键被称为成员（member),每个成员都是各不相同的。有序集合的值则被称为分值（score），分值必须为浮点数。有序集合是redis里面唯一一个既可以根据成员访问元素(这一点和散列一样),又可以根据分值以及分值的排列顺序访问元素的结构。</p>
<ul>
<li>增加(zadd key [NX|XX] [CH] [INCR] score member [score member …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zadd z 1 a 2 b 3 c 4 d 5 e 6 f 7 g</span><br></pre></td></tr></table></figure>
<ul>
<li>获取分数(zscore key member)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zscore z e</span><br></pre></td></tr></table></figure>
<ul>
<li>获取排名在某个范围内的元素列表，闭区间(zrange key start stop [withscores])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zrange z 0 10 withscores</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;若withscores参数不存在，则按顺序输出范围内的元素，若withscores参数存在，则一行元素，一行分值按顺序输出范围内的元素-分值。若start/stop为-1，则取顺序是最后的元素-分值；为-2，则取顺序是倒数第二的元素-分值；以此类推。</p>
<ul>
<li>获取指定分数范围的元素列表，闭区间,limit参数分页(zrangebyscore key min max [withscores] [limit offset count])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zrangebyscore z 5 10 withscores <span class="built_in">limit</span> 1 2</span><br></pre></td></tr></table></figure>
<ul>
<li>增加元素分值(zincrby key increment member)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zincrby z 8 v</span><br></pre></td></tr></table></figure>
<ul>
<li>获取元素数量(zcard key)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zcard z</span><br></pre></td></tr></table></figure>
<ul>
<li>获取分值范围内的元素个数(zcount key min max)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zcount z 0 10</span><br></pre></td></tr></table></figure>
<ul>
<li>删除元素(zrem key member [member …])</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zrem z v z</span><br></pre></td></tr></table></figure>
<ul>
<li>删除指定排名范围内的元素(zremrangebyrank key start stop)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zremrangebyrank z 0 10</span><br></pre></td></tr></table></figure>
<ul>
<li>删除指定分值范围内的元素(zremrangebyscore key min max)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zremrangebyscore z 2 10</span><br></pre></td></tr></table></figure>
<ul>
<li>获取元素正序排名(zrank key member)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zrank z f</span><br></pre></td></tr></table></figure>
<ul>
<li>获取元素倒序排名(zrevrank key member)</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zrevrank z a</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>redis</tag>
      </tags>
  </entry>
  <entry>
    <title>正则表达式</title>
    <url>/regex-md.html</url>
    <content><![CDATA[<p>&emsp;&emsp;字符串是编程中用到最多，最常见的一种数据结构，对字符串的处理需求无处不在。正则表达式是一种用来匹配字符串的强力武器，它的设计思想是用一种描述性语言给字符串定义一个规则，凡事符合规则的字符串，就认为”匹配”了。</p>
<a id="more"></a>
<h1 id="正则表达式介绍"><a href="#正则表达式介绍" class="headerlink" title="正则表达式介绍"></a>正则表达式介绍</h1><p>&emsp;&emsp;一个正则表达式通常被称为一个模式（pattern），为用来描述或者匹配一系列符合某个句法规则的字符串。例如：Handel、Händel和Haendel这三个字符串，都可以由H(a|ä|ae)ndel这个模式来描述。大部分正则表达式的形式都有如下的结构：</p>
<ul>
<li><p>选择：竖线|代表选择（即或集），具有最低优先级。例如gray|grey可以匹配grey或gray。</p>
</li>
<li><p>数量限定：某个字符后的数量限定符用来限定前面这个字符允许出现的个数。最常见的数量限定符包括+、?和*（不加数量限定则代表出现一次且仅出现一次）：</p>
</li>
</ul>
<blockquote>
<p><code>+</code>代表前面的字符必须至少出现一次。（1次或多次）。例如，goo+gle可以匹配google、gooogle、goooogle等;<br><code>?</code>代表前面的字符最多只可以出现一次。（0次或1次）。例如，colou?r可以匹配color或者colour;<br><code>*</code>代表前面的字符可以不出现，也可以出现一次或者多次。（0次、1次或多次）。例如，0*42可以匹配42、042、0042、00042等。</p>
</blockquote>
<ul>
<li>匹配：圆括号()可以用来定义操作符的范围和优先度。例如，gr(a|e)y等价于gray|grey，(grand)?father匹配father和grandfather。上述这些构造子都可以自由组合，因此H(ae?|ä)ndel和H(a|ae|ä)ndel是相同的，表示{“Handel”, “Haendel”, “Händel”}。</li>
</ul>
<h2 id="表达式全集"><a href="#表达式全集" class="headerlink" title="表达式全集"></a>表达式全集</h2><p><img src="/regex-md/regex.png" alt="查看"></p>
<h2 id="元字符"><a href="#元字符" class="headerlink" title="元字符"></a>元字符</h2><p><img src="/regex-md/regex-echar.png" alt="查看"></p>
<h2 id="重复"><a href="#重复" class="headerlink" title="重复"></a>重复</h2><p><img src="/regex-md/regex-repetition.png" alt="查看"></p>
<h2 id="优先权"><a href="#优先权" class="headerlink" title="优先权"></a>优先权</h2><p><img src="/regex-md/regex_order.png" alt="查看"></p>
<h1 id="匹配原理"><a href="#匹配原理" class="headerlink" title="匹配原理"></a>匹配原理</h1><p>&emsp;&emsp;实现正则表达式引擎的有两种方式：DFA 自动机（确定型有穷自动机）和 NFA 自动机（不确定型有穷自动机）。DFA 自动机的时间复杂度是线性的，更加稳定，但是功能有限。而 NFA 的时间复杂度比较不稳定，有时候很好，有时候不怎么好，好不好取决于你写的正则表达式。但是胜在 NFA 的功能更加强大，所以包括 Java 、.NET、Perl、Python、Ruby、PHP 等语言都使用了 NFA 去实现其正则表达式。</p>
<h2 id="DFA"><a href="#DFA" class="headerlink" title="DFA"></a>DFA</h2><p>&emsp;&emsp;DFA从匹配文本入手，从左到右，每个字符不会匹配两次，它的时间复杂度是多项式的，所以通常情况下，它的速度更快，但支持的特性很少，不支持捕获组、各种引用等等。举例说明：字符串：<code>after tonight</code>,表达式：<code>to(nite|nighta|night)</code>;从a开始匹配t，直到第一个t跟正则的t匹配，但e跟o匹配失败，继续，直到文本里面的第二个 t 匹配正则的t，接着o与o匹配，n的时候发现正则里面有三个可选匹配，开始并行匹配，直到文本中的g使得第一个可选条件不匹配，继续，直到最后匹配。</p>
<h2 id="NFA"><a href="#NFA" class="headerlink" title="NFA"></a>NFA</h2><p>&emsp;&emsp;NFA 是以正则表达式为基准去匹配的。也就是说，NFA 自动机会读取正则表达式的一个一个字符，然后拿去和目标字符串匹配，匹配成功就换正则表达式的下一个字符，否则继续和目标字符串的下一个字符比较。</p>
<p>&emsp;&emsp;以正则表达式<code>ab*c</code>匹配字符串 <font color="#00dddd">a</font><font color="#dd00dd">a</font>bc为例：</p>
<blockquote>
<p>第一步：用a匹配字符<font color="#00dddd">a</font>，匹配成功；<br>第二步：用b*匹配字符<font color="#dd00dd">a</font>，匹配失败；<br>第三步：用c匹配字符<font color="#dd00dd">a</font>，匹配失败；<br>第四步：回溯，表达式回退到a,用a匹配字符<font color="#dd00dd">a</font>，匹配成功；<br>第五步：用b*匹配字符b，匹配成功；<br>第六步：用b*匹配字符c，匹配失败；<br>第七步：用c匹配字符c，匹配成功；</p>
</blockquote>
<p>&emsp;&emsp;可以看到，在第三步匹配失败的时候发生了<strong>回溯</strong>，表达式回到了开头从字符串的下一个位置重新匹配。</p>
<h1 id="java正则表达式"><a href="#java正则表达式" class="headerlink" title="java正则表达式"></a>java正则表达式</h1><p>&emsp;&emsp;java.util.regex是一个用正则表达式所订制的模式来对字符串进行匹配工作的类库包。它包括两个类：Pattern和Matcher，Pattern：一个Pattern是一个正则表达式经编译后的表现模式。 Matcher：一个Matcher对象是一个状态机器，它依据Pattern对象做为匹配模式对字符串展开匹配检查。 首先一个Pattern实例订制了一个所用语法与PERL的类似的正则表达式经编译后的模式，然后一个Matcher实例在这个给定的Pattern实例的模式控制下进行字符串的匹配工作。</p>
<h2 id="匹配模式"><a href="#匹配模式" class="headerlink" title="匹配模式"></a>匹配模式</h2><ul>
<li>构建匹配模式</li>
</ul>
<p>&emsp;&emsp;Pattern类用于创建一个正则表达式,也可以说创建一个匹配模式,它的构造方法是私有的,不可以直接创建,但可以通过Pattern.complie(String regex)简单工厂方法创建一个正则表达式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"r+"</span>);</span><br><span class="line">logger.info(pattern.pattern());</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过调用静态方法compile创建一个正则表达式的对象，在这个对象上调用pattern/toString方法返回这个正则表达式的字符串表现形式。</p>
<ul>
<li>拆分给定序列</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\.+?"</span>);</span><br><span class="line"></span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, <span class="number">4</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, <span class="number">3</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, <span class="number">2</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, <span class="number">1</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, <span class="number">0</span>)));</span><br><span class="line">logger.info(Arrays.toString(pattern.split(<span class="string">"https://ferry.org.cn"</span>, -<span class="number">1</span>)));</span><br></pre></td></tr></table></figure>
<p><img src="/regex-md/regex-split.png" alt="查看"></p>
<p>&emsp;&emsp;字符串的split方法，当给定的正则表达式不是简单字符时，调用pattern.split得到结果。</p>
<ul>
<li>匹配给定输入</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// '.'匹配任意字符(除了\n和\r),'+'匹配前面的表达式一次或者多次</span></span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, Pattern.matches(<span class="string">".+"</span>, <span class="string">"https://ferry.org.cn"</span>));</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;本质上是创建匹配器，然后调用匹配器的matches方法。String的matches方法就是调用的Pattern.matches。</p>
<h2 id="匹配器"><a href="#匹配器" class="headerlink" title="匹配器"></a>匹配器</h2><ul>
<li>创建匹配器</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\.+?"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"https://ferry.org.cn"</span>);</span><br></pre></td></tr></table></figure>
<ul>
<li>匹配操作</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 匹配一个或者多个'.'</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\.+?"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"https://ferry.org.cn"</span>);</span><br><span class="line"><span class="comment">// 整个输入序列是否完全匹配该正则表达式</span></span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.matches());</span><br><span class="line"><span class="comment">// 从输入序列的开头开始匹配该正则表达式</span></span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.lookingAt());</span><br><span class="line"><span class="comment">// 输入序列中是否有某一个自序列能够匹配正则表达式</span></span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.find());</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;matches和lookingAt方法每次调用都会从输入序列的开头开始匹配，而find方法第一次从开头开始匹配，后面每次调用都是从上次匹配到的子字符串在输入序列的结束位置开始继续向后匹配。</p>
<ul>
<li>获取匹配的子字符串在字符串中的索引</li>
</ul>
<p>&emsp;&emsp;当使用matches,lookingAt或者find方法返回true时，此时就可以使用start和end查找匹配到的子字符串在字符串的索引位置。返回的索引数值总是等于下一个字符在字符串中的下标。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">".+"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"https://ferry.org.cn"</span>);</span><br><span class="line"><span class="keyword">if</span>(matcher.matches())&#123;</span><br><span class="line">    logger.info(<span class="string">"matches start: &#123;&#125;"</span>, matcher.start());</span><br><span class="line">    logger.info(<span class="string">"matches end: &#123;&#125;"</span>, matcher.end());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"^http(s)?"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"https://ferry.org.cn"</span>);</span><br><span class="line"><span class="keyword">if</span>(matcher.lookingAt())&#123;</span><br><span class="line">    logger.info(<span class="string">"lookingAt start: &#123;&#125;"</span>, matcher.start());</span><br><span class="line">    logger.info(<span class="string">"lookingAt end: &#123;&#125;"</span>, matcher.end());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\.+?"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"https://ferry.org.cn"</span>);</span><br><span class="line"><span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span> (matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"find &#123;&#125; start: &#123;&#125;"</span>, ++count, matcher.start());</span><br><span class="line">    logger.info(<span class="string">"find &#123;&#125; end: &#123;&#125;"</span>, count, matcher.end());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>替换</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"/+?"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferry/org/cn"</span>);</span><br><span class="line">logger.info(<span class="string">"replaceFirst: &#123;&#125;"</span>, matcher.replaceFirst(<span class="string">"."</span>));</span><br><span class="line">logger.info(<span class="string">"replaceAll: &#123;&#125;"</span>, matcher.replaceAll(<span class="string">"."</span>));</span><br></pre></td></tr></table></figure>
<ul>
<li>分组</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(\\w+)\\.+?(\\w+)"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferrysy.com"</span>);</span><br><span class="line"><span class="keyword">if</span>(matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"group count: &#123;&#125;"</span>, matcher.groupCount());</span><br><span class="line">    logger.info(<span class="string">"group 0: &#123;&#125;"</span>, matcher.group(<span class="number">0</span>));</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">do</span>&#123;</span><br><span class="line">        logger.info(<span class="string">"group &#123;&#125;: &#123;&#125;"</span>, ++i, matcher.group(i));</span><br><span class="line">    &#125;<span class="keyword">while</span> (i &lt; matcher.groupCount());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/regex-md/regex-group.png" alt="查看"></p>
<p>&emsp;&emsp;正则表达式中，匹配<code>(pattern)</code>但并非是零宽预言的子字符串就是一个分组，可以使用groupCount方法查看分组数量，matcher.group(0)表示整个匹配的字符串，matcher.group(i)表示获取匹配的第i个分组。</p>
<h1 id="进阶用法"><a href="#进阶用法" class="headerlink" title="进阶用法"></a>进阶用法</h1><h2 id="分组"><a href="#分组" class="headerlink" title="分组"></a>分组</h2><p>&emsp;&emsp;分组，用小括号来指定子表达式(也叫做分组)，可以指定这个子表达式的重复次数，你也可以对子表达式进行其它一些操作。</p>
<p>1, 默认情况下，每个分组都有一个组号，规则是：从左向右，以分组的左括号为标志，第一个出现的分组的组号为1，第二个为2，以此类推；<br>2, 后向引用用于重复搜索前面某个分组匹配的文本。例如，\1 代表分组1匹配的文本。注意，\1 代表的是分组1匹配到的文本，而不是分组1的表达式，其次，\1并不是分组；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(\\w+)\\.+?(\\w+)"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferrysy.com"</span>);</span><br><span class="line"><span class="keyword">if</span>(matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"group count: &#123;&#125;"</span>, matcher.groupCount());</span><br><span class="line">    logger.info(<span class="string">"group 0: &#123;&#125;"</span>, matcher.group(<span class="number">0</span>));</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">do</span>&#123;</span><br><span class="line">        logger.info(<span class="string">"group &#123;&#125;: &#123;&#125;"</span>, ++i, matcher.group(i));</span><br><span class="line">    &#125;<span class="keyword">while</span> (i &lt; matcher.groupCount());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\b(\\w+)\\b\\s\\1"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferry ferry"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.groupCount());</span><br><span class="line"><span class="keyword">if</span>(matcher.find())&#123;</span><br><span class="line">    logger.info(matcher.group(<span class="number">1</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="零宽断言"><a href="#零宽断言" class="headerlink" title="零宽断言"></a>零宽断言</h2><p>&emsp;&emsp;查找在某些内容(但并不包括这些内容)之前或之后的东西，用于指定一个位置，这个位置应该满足一定的条件(即断言)，因此它们也被称为零宽断言。</p>
<ul>
<li><strong>零宽度正预测先行断言(?=exp)</strong></li>
</ul>
<p>&emsp;&emsp;它断言自身出现的位置的后面能匹配表达式。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 可以匹配does不可以匹配do</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"do(?=es)"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"do"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.lookingAt());</span><br><span class="line">matcher.reset(<span class="string">"does"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.lookingAt());</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 限制字符串长度</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(?=(^.&#123;5&#125;$))\\w+"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferry"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.matches());</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>零宽度正回顾后发断言(?&lt;=exp)</strong></li>
</ul>
<p>&emsp;&emsp;断言自身出现的位置的前面能匹配表达式 exp。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 可以匹配where不可以匹配here</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(?&lt;=wh)ere"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"here"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.find());</span><br><span class="line">matcher.reset(<span class="string">"where"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.find());</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>零宽度负预测先行断言(?!exp)</strong></li>
</ul>
<p>&emsp;&emsp;断言此位置的后面不能匹配表达式 exp。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 每五个字符后的字符不能是数字</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(.&#123;5&#125;(?!\\d))+"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferrylove2"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.matches());</span><br></pre></td></tr></table></figure>
<ul>
<li><strong>零宽度负回顾后发断言(?&lt;!exp)</strong></li>
</ul>
<p>&emsp;&emsp;断言此位置的前面不能匹配表达式exp。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 匹配五个下写字母且前面一个字符不是数字的</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"(?&lt;!\\d)[a-z]&#123;5&#125;"</span>);</span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ferry"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.matches());</span><br><span class="line">matcher.reset(<span class="string">"1ferry"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.find());</span><br></pre></td></tr></table></figure>
<h2 id="贪婪-懒惰与独占"><a href="#贪婪-懒惰与独占" class="headerlink" title="贪婪,懒惰与独占"></a>贪婪,懒惰与独占</h2><ul>
<li><strong>贪婪</strong></li>
</ul>
<p>&emsp;&emsp;当正则表达式中包含能接受重复的限定符时，通常的行为是（在使整个表达式能得到匹配的前提下）匹配尽可能多的字符。这被称为贪婪匹配。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 贪婪匹配</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\d.*\\d"</span>); <span class="comment">// 匹配两个数字，数字中间有零或多个字符</span></span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"1fer22ry9"</span>);</span><br><span class="line"><span class="keyword">while</span> (matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"&#123;&#125;"</span>, matcher.group());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 最终匹配的是整个字符串</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong>懒惰</strong></li>
</ul>
<p>&emsp;&emsp;有时，我们更需要懒惰匹配，也就是匹配尽可能少的字符。前面给出的限定符都可以被转化为懒惰匹配模式，只要在它后面加上一个问号?。这样.*?就意味着匹配任意数量的重复，但是在能使整个匹配成功的前提下使用最少的重复。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 懒惰匹配</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\d.*?\\d"</span>); <span class="comment">// 匹配两个数字，数字中间有零或多个字符</span></span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"11fer22ry99"</span>);</span><br><span class="line"><span class="keyword">while</span> (matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"&#123;&#125;"</span>, matcher.group());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 匹配的结果有三个，11，22，99</span></span><br></pre></td></tr></table></figure>
<ul>
<li><strong>最先开始的匹配拥有最高优先权</strong></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Pattern pattern = Pattern.compile(<span class="string">"\\d.*?\\d"</span>); <span class="comment">// 匹配两个数字，数字中间有零或多个字符</span></span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"1fer22ry99"</span>);</span><br><span class="line"><span class="keyword">while</span> (matcher.find())&#123;</span><br><span class="line">    logger.info(<span class="string">"&#123;&#125;"</span>, matcher.group());</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 匹配结果有两个，1fer2,2ry9</span></span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;上述正则表达式虽然是懒惰模式，理论上应该匹配22，99，但是第一个出现的数字是1，所以1具有优先匹配权。</p>
<ul>
<li><strong>懒惰限定符</strong></li>
</ul>
<p><img src="/regex-md/regex-lazy.png" alt="查看"></p>
<ul>
<li><strong>独占</strong></li>
</ul>
<p>&emsp;&emsp;在独占模式下，表达式尽可能长的匹配字符串，若匹配失败则结束匹配而不会回溯。独占表达式就是在重复表达式后面添加一个<code>+</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 独占模式</span></span><br><span class="line">Pattern pattern = Pattern.compile(<span class="string">"s*+\\w"</span>);  <span class="comment">// 此表达式相当于匹配零或多个s,最后一个字符不能是s结尾</span></span><br><span class="line">Matcher matcher = pattern.matcher(<span class="string">"ss"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>,matcher.matches());  <span class="comment">// false</span></span><br><span class="line">matcher.reset(<span class="string">"s4"</span>);</span><br><span class="line">logger.info(<span class="string">"&#123;&#125;"</span>, matcher.matches());  <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<h2 id="平衡组"><a href="#平衡组" class="headerlink" title="平衡组"></a>平衡组</h2><p>&emsp;&emsp;平衡组，故名思义，平衡即对称，主要是结合几种正则语法规则，提供对配对出现的嵌套结构的匹配。平衡组有狭义与广义两种定义，狭义平衡组指(?Expression) 语法，而广义平衡组并不是固定的语法规则，而是几种语法规则的综合运用，我们平时所说的平衡组通常指的是广义平衡组。</p>
<p>&emsp;&emsp;下面是可能会用到的语法结构：</p>
<blockquote>
<p>(?&lt;group>) 把捕获的内容命名为group,并压入堆栈<br>(?<-group>) 从堆栈中弹出最后压入堆栈的名为group的捕获内容，如果堆栈本来为空，则本分组的匹配失败<br>(?(group)yes|no) 如果堆栈上存在以名为group的捕获内容的话，继续匹配yes部分的表达式，否则继续匹配no部分<br>(?!) 顺序否定环视，由于没有后缀表达式，试图匹配总是失败</-group></p>
</blockquote>
<h1 id="练习"><a href="#练习" class="headerlink" title="练习"></a>练习</h1><p>&emsp;&emsp;正则表达式的学习最好是结合例子，单纯的记忆是很难玩的懂的，前面介绍了正则的java使用，那么在实践中会使用java的类去展示正则表达式的实际效果。</p>
<h2 id="英文域名"><a href="#英文域名" class="headerlink" title="英文域名"></a>英文域名</h2><p>&emsp;&emsp;域名的匹配规则如下：</p>
<blockquote>
<p>域名由多个标签(各级域名)组成，每个标签长度限制63，域名总长度253；<br>各个标签之间用<code>.</code>连接；<br>标签可以由英文字母，阿拉伯数字和中横线组成<br>中横线不能连续出现、不能单独注册，也不能放在开头和结尾。</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="邮箱校验"><a href="#邮箱校验" class="headerlink" title="邮箱校验"></a>邮箱校验</h3><p>&emsp;&emsp;先说一下邮箱的规则，邮箱分两部分有符号<code>@</code>连接，第一部分：组成元素可以是大小写英文字母，中横线，阿拉伯数字，下划线，英文句号，这里我们暂且规定邮箱第一部分长度不小于5，且只能用大小写英文字母开头；第二部分：域名。</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>正则表达式</tag>
        <tag>Pattern类</tag>
        <tag>Matcher类</tag>
      </tags>
  </entry>
  <entry>
    <title>常用软件安装</title>
    <url>/software-install.html</url>
    <content><![CDATA[<p>&emsp;&emsp;记录一下常用的软件的安装以及环境变量的配置，省的以后百度浪费时间。</p>
<a id="more"></a>
<h3 id="JDK的安装"><a href="#JDK的安装" class="headerlink" title="JDK的安装"></a>JDK的安装</h3><p>&emsp;&emsp;作为Java语言的SDK(软件开发工具包，Software development kit)，普通用户并不需要安装JDK来运行Java程序，而只需要安装JRE（Java Runtime Environment）。而程序开发者必须安装JDK(Java Development Kit)来编译、调试程序。</p>
<h4 id="linux系统安装JDK"><a href="#linux系统安装JDK" class="headerlink" title="linux系统安装JDK"></a>linux系统安装JDK</h4><p>&emsp;&emsp;在linux上安装JDK有多种方式，你可以使用<code>wget</code>从官网下载压缩文件然后解压，然后配置环境变量；也可以使用<code>yum</code>直接安装JDK。</p>
<h5 id="使用yum安装-openjdk"><a href="#使用yum安装-openjdk" class="headerlink" title="使用yum安装 openjdk"></a>使用<code>yum</code>安装 openjdk</h5><ul>
<li>查看JDK版本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list java-1.8*</span><br></pre></td></tr></table></figure>
<p><img src="/software-install/install-jdk-linux.png" alt="运行结果"></p>
<ul>
<li>安装JDK</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install java-1.8.0-openjdk-devel.x86_64</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里需要注意，我们安装JDK需要选择<code>-devel</code>的下载安装，没有这个的是JRE，<code>x86_64</code>是64位的，请根据机器的版本进行选择。<code>-y</code>参数是在安装过程中如有出现选择是与否时全部默认选择是。</p>
<ul>
<li>查看版本信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -version</span><br></pre></td></tr></table></figure>
<ul>
<li>查看JDK安装目录</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">which</span> java</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;发现是一个引用，再查看引用的目录，最终找到实际的安装目录</p>
<ul>
<li>配置java环境变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/profile</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-1.8.0.212.b04-0.el7_6.x86_64</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$&#123;PATH&#125;</span>:<span class="variable">$&#123;JAVA_HOME&#125;</span>/bin</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure>
<p><img src="/software-install/jdk_install_dir.png" alt="jdk安装目录"></p>
<h5 id="使用-rpm-安装-oracle-jdk"><a href="#使用-rpm-安装-oracle-jdk" class="headerlink" title="使用 rpm 安装 oracle jdk"></a>使用 rpm 安装 oracle jdk</h5><ul>
<li>移除系统自带的open jdk</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum remove -y *openjdk*</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;从<a href="https://www.oracle.com/technetwork/java/javase/downloads/index.html" target="_blank" rel="noopener">官网下载</a>对应版本的jdk，注意，是rpm后缀的，可以直接使用wget下载到服务器，但是我每次这样都会说验证失败，所以我的操作是先下载到本地，然后通过scp或者ftp上传到服务器。</p>
<ul>
<li>安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">rpm -ivh /usr/<span class="built_in">local</span>/jdk-8u231-linux-i586.rpm</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;安装完成后rpm包就可以删除了。使用<code>java -version</code>可以查看jdk是否安装成功。配置java环境变量可以参考【使用<code>yum</code>安装 openjdk】节的步奏。</p>
<h3 id="maven安装"><a href="#maven安装" class="headerlink" title="maven安装"></a>maven安装</h3><p><img src="/software-install/install-maven.png" alt="maven"></p>
<p>&emsp;&emsp;在官网上有不同的maven下载链接供大家选择，这里简单介绍一下。<code>-bin</code>结尾的是编译好的class文件，可以直接使用，我们一般也选择这种的进行下载，<code>-src是java原文件，可以查看源码。</code>.tar.gz<code>是linux系统用的压缩格式，</code>.zip`是windows系统用的压缩格式，所以根据自己的硬件配置选择对应的文件下载就好了。</p>
<p>&emsp;&emsp;友情提示，maven是用java语言实现的，所以安装maven必须得先安装jdk。</p>
<h4 id="linux安装maven"><a href="#linux安装maven" class="headerlink" title="linux安装maven"></a>linux安装maven</h4><ul>
<li>下载</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget http://mirrors.tuna.tsinghua.edu.cn/apache/maven/maven-3/3.6.0/binaries/apache-maven-3.6.0-bin.tar.gz</span><br></pre></td></tr></table></figure>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar zxvf apache-maven-3.6.0-bin.tar.gz</span><br></pre></td></tr></table></figure>
<ul>
<li>配置环境变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/profile</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;在文件最后面添加如下语句</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> MAVEN_HOME=/usr/<span class="built_in">local</span>/apache-maven-3.6.0</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$&#123;PATH&#125;</span>:<span class="variable">$&#123;MAVEN_HOME&#125;</span>/bin</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;按<code>ESC</code>,输入<code>:</code>然后输入<code>wq</code>然后回车，这一步是保存文件的操作。</p>
<ul>
<li>使配置立即生效</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure>
<ul>
<li>查看maven版本信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mvn --version</span><br></pre></td></tr></table></figure>
<ul>
<li>修改镜像为阿里云</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi <span class="variable">$MAVEN_HOME</span>/conf/settings.xml</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;将mirrors下替换为以下内容。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">&lt;mirror&gt;</span><br><span class="line">  &lt;id&gt;alimaven&lt;/id&gt;</span><br><span class="line">  &lt;name&gt;aliyun maven&lt;/name&gt;</span><br><span class="line">  &lt;url&gt;http://maven.aliyun.com/nexus/content/groups/public/&lt;/url&gt;</span><br><span class="line">  &lt;mirrorOf&gt;central&lt;/mirrorOf&gt;</span><br><span class="line">&lt;/mirror&gt;</span><br></pre></td></tr></table></figure>
<h3 id="tomcat安装"><a href="#tomcat安装" class="headerlink" title="tomcat安装"></a>tomcat安装</h3><p>&emsp;&emsp;tomcat的运行是需要JDK的支持的哦，所以，请先安装JDK。</p>
<p><img src="/software-install/install-tomcat-linux.png" alt="tomcat"></p>
<h4 id="linux安装tomcat"><a href="#linux安装tomcat" class="headerlink" title="linux安装tomcat"></a>linux安装tomcat</h4><p>&emsp;&emsp;在tomcat官网上，在左边选择我们需要的tomcat版本，在右边选择<code>core</code>下的<code>tar.gz</code>格式的文件进行下载。</p>
<p>&emsp;&emsp;有时候我们会在网上看到安装了tomcat后还需要配置一个环境变量叫做<code>CATALINA_HOME</code>,关于这个环境变量的配置可以参考这篇博客，个人觉得说的很详细了。<a href="https://www.cnblogs.com/heshan664754022/archive/2013/03/27/2984357.html" target="_blank" rel="noopener">tomcat启动分析</a>。</p>
<ul>
<li>下载</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget http://mirror.bit.edu.cn/apache/tomcat/tomcat-8/v8.5.35/bin/apache-tomcat-8.5.35.tar.gz</span><br></pre></td></tr></table></figure>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar zxvf apache-tomcat-8.5.35.tar.gz</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;到这里就已经安装好了。</p>
<h3 id="git安装"><a href="#git安装" class="headerlink" title="git安装"></a>git安装</h3><p>&emsp;&emsp;git，非常先进的分布式版本控制系统。</p>
<h4 id="linux安装git"><a href="#linux安装git" class="headerlink" title="linux安装git"></a>linux安装git</h4><p>&emsp;&emsp;在linux环境下可以使用<code>yum</code>指令安装git，但是版本未必符合我们的要求，为此，这里介绍从github下载源码进行编译安装的方式。</p>
<ul>
<li>下载</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://github.com/git/git/archive/master.zip</span><br></pre></td></tr></table></figure>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">unzip master.zip</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果这里提示<code>-bash: unzip: 未找到命令</code>,那么请使用<code>yum -y install unzip</code>安装，然后执行上面命令。</p>
<ul>
<li>重命名</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mv git-master git</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;将解压的文件夹重新命名，这一步可有可无，我有强迫症，喜欢用这个名字命名git文件夹。</p>
<ul>
<li>安装插件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum -y install curl-devel expat-devel gettext-devel openssl-devel zlib-devel gcc perl-ExtUtils-MakeMaker</span><br></pre></td></tr></table></figure>
<ul>
<li>进入git根目录，编译</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make prefix=/usr/<span class="built_in">local</span>/git all</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;其中的<code>/usr/local/git</code>是git的安装目录，这个就是配置git安装目录并且编译</p>
<ul>
<li>安装</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make install</span><br></pre></td></tr></table></figure>
<ul>
<li>查看版本信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git --version</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这一步如果抱git命令未找到错误，那么就需要你去配置一下环境变量了。</p>
<ul>
<li>配置环境变量</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/profile</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$PATH</span>:/usr/<span class="built_in">local</span>/git/bin</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;保存退出后执行<code>source /etc/profile</code>然后查看版本信息。</p>
<h3 id="redis安装"><a href="#redis安装" class="headerlink" title="redis安装"></a>redis安装</h3><p>&emsp;&emsp;介绍安装redis服务并且注册为系统服务开机自启。</p>
<h4 id="windows安装redis"><a href="#windows安装redis" class="headerlink" title="windows安装redis"></a>windows安装redis</h4><p>&emsp;&emsp;现在redis官网没有windows版本，只有linux版本可供下载，那么先从github上<a href="https://github.com/MicrosoftArchive/redis/releases" target="_blank" rel="noopener">下载 redis</a> 。</p>
<p>&emsp;&emsp;下载安装版的就按照步骤next就好了，解压版的就解压在你指定的地方就可以了。</p>
<ul>
<li>在redis安装目录下打开终端，按住shift点击鼠标右键打开命令窗口输入以下指令启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server redis.windows.conf</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;服务成功启动如图所示：</p>
<p><img src="/software-install/redis_start.png" alt="启动redis"></p>
<p>&emsp;&emsp;通过上述方式启动redis虽然可以正常使用，但是只要关闭该命令窗口服务就会停止，而不关闭该窗口任务栏占用一个位置就很烦，每次开机都要打开安装目录执行一遍启动命令也是很烦的。所以可以考虑将redis设置成系统服务。</p>
<ul>
<li>在没有启动redis的前提下，在redis的安装目录打开命令窗口，执行</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server --service-install redis.windows-service.conf --loglevel verbose</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;如果没有报错那么说明成功了，此时打开任务管理器，单击服务，可以看到redis服务已经存在了。</p>
<p><img src="/software-install/redis_server.png" alt="注册服务"></p>
<ul>
<li>刚刚设置的服务可能没有启动，此时执行下面的语句启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server --service-start</span><br></pre></td></tr></table></figure>
<ul>
<li>刷新任务管理器，查看服务是否已经启动，当有一天你可能不再使用redis了，可以使用以下命令停止redis服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server --service-stop</span><br></pre></td></tr></table></figure>
<ul>
<li>卸载redis服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server --service-uninstall</span><br></pre></td></tr></table></figure>
<h4 id="linux安装redis"><a href="#linux安装redis" class="headerlink" title="linux安装redis"></a>linux安装redis</h4><h5 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h5><ul>
<li>安装redis</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y redis</span><br></pre></td></tr></table></figure>
<ul>
<li>启动redis服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start redis.service</span><br></pre></td></tr></table></figure>
<ul>
<li>开机自启</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> redis.service</span><br></pre></td></tr></table></figure>
<h5 id="方法二"><a href="#方法二" class="headerlink" title="方法二"></a>方法二</h5><h6 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h6><ul>
<li>下载安装包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/</span><br><span class="line">wget http://download.redis.io/releases/redis-4.0.14.tar.gz</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;选择Stable版本下载。</p>
<ul>
<li>解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -zxvf redis-4.0.14.tar.gz</span><br></pre></td></tr></table></figure>
<ul>
<li>编译</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/redis-4.0.14/</span><br><span class="line">make</span><br></pre></td></tr></table></figure>
<ul>
<li>启动</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/redis-4.0.14/src/</span><br><span class="line">./redis-server ../redis.conf</span><br></pre></td></tr></table></figure>
<ul>
<li>连接</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/redis-4.0.14/src/</span><br><span class="line">./redis-cli</span><br></pre></td></tr></table></figure>
<h6 id="配置开机自启服务"><a href="#配置开机自启服务" class="headerlink" title="配置开机自启服务"></a>配置开机自启服务</h6><ul>
<li>复制启动脚本</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 复制redis的脚本文件</span></span><br><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/redis-4.0.14/src/</span><br><span class="line">cp redis-server redis-cli /usr/<span class="built_in">local</span>/bin/</span><br></pre></td></tr></table></figure>
<ul>
<li>修改redis的配置文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /usr/<span class="built_in">local</span>/redis-4.0.14/redis.conf</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">supervised systemd</span><br><span class="line">daemonize yes</span><br></pre></td></tr></table></figure>
<ul>
<li>配置服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /lib/systemd/system/redisd.service</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础信息</span></span><br><span class="line">[Unit]</span><br><span class="line"><span class="comment"># 描述</span></span><br><span class="line">Description=Redis</span><br><span class="line"><span class="comment"># 在某个服务启动后启动</span></span><br><span class="line">After=network.target</span><br><span class="line"></span><br><span class="line"><span class="comment"># 服务信息</span></span><br><span class="line">[Service]</span><br><span class="line">Type=forking</span><br><span class="line"><span class="comment"># 启动服务的命令</span></span><br><span class="line">ExecStart=/usr/<span class="built_in">local</span>/bin/redis-server /usr/<span class="built_in">local</span>/bin/usr/<span class="built_in">local</span>/redis-4.0.14/redis.conf</span><br><span class="line"><span class="comment"># 终止服务的命令</span></span><br><span class="line">ExecStop=/usr/<span class="built_in">local</span>/bin/redis-cli -h 127.0.0.1 -p 6379 shutdown</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装相关信息</span></span><br><span class="line">[Install]</span><br><span class="line"><span class="comment"># WantedBy 是以哪种方式启动：multi-user.target表明当系统以多用户方式（默认的运行级别）启动时，这个服务需要被自动运行。</span></span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure>
<ul>
<li>可执行权限</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x /lib/systemd/system/redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>刷新配置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br></pre></td></tr></table></figure>
<ul>
<li>启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>查看状态</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>关闭服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl stop redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>开机自启</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">enable</span> redisd.service</span><br></pre></td></tr></table></figure>
<ul>
<li>禁止开机自启</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl <span class="built_in">disable</span> redisd.service</span><br></pre></td></tr></table></figure>
<h3 id="linux安装jenkins"><a href="#linux安装jenkins" class="headerlink" title="linux安装jenkins"></a>linux安装jenkins</h3><h4 id="yum-安装-jenkins"><a href="#yum-安装-jenkins" class="headerlink" title="yum 安装 jenkins"></a>yum 安装 jenkins</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo</span><br><span class="line">sudo rpm --import https://pkg.jenkins.io/redhat-stable/jenkins.io.key</span><br><span class="line">sudo yum install -y jenkins</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;目录介绍：</p>
<blockquote>
<p>配置文件目录：<code>/etc/sysconfig/jenkins</code>;<br>默认的JENKINS_HOME目录：<code>/var/lib/jenkins/</code>；<br>jenkins安装目录：/usr/lib/jenkins/；<br>jenkins日志文件：<code>/var/log/jenkins/jenkins.log</code>。</p>
</blockquote>
<p>&emsp;&emsp;具体的路径啊，端口啊都可以通过配置文件修改的，参数里面找一下，一眼就认出来，改好了保存重启jenkins就好了。</p>
<ul>
<li>启动</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start jenkins</span><br></pre></td></tr></table></figure>
<h4 id="下载war包运行"><a href="#下载war包运行" class="headerlink" title="下载war包运行"></a>下载war包运行</h4><p>&emsp;&emsp;下载<a href="https://updates.jenkins-ci.org/download/war/" target="_blank" rel="noopener">稳定版war包</a>，然后使用命令<code>nohup java -jar jenkins.war --httpPort=8080 &gt; jenkins.log &amp;</code>运行jenkins。</p>
<h4 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h4><p>&emsp;&emsp;jenkins默认的工作目录是<code>~/.jenkins/</code>目录，我们可以在运行jenkins之前配置环境变量指定jenkins的工作目录。</p>
<ul>
<li>vi /etc/profile</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> JENKINS_HOME=/usr/<span class="built_in">local</span>/jenkins</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;保存退出后执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure>
<ul>
<li>查看jenkins进程信息</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ps -aux |grep jenkins|grep -v grep</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;jenkins默认监听的是8080端口，需要防火墙开放8080端口或者自定义的端口。</p>
<ul>
<li>第一次登陆</li>
</ul>
<p><img src="/software-install/jenkins_login.png" alt="login"></p>
<p>&emsp;&emsp;根据提示去对应的文件查看密码,然后将密码输入，点击继续。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cat /var/lib/jenkins/secrets/initialAdminPassword</span><br></pre></td></tr></table></figure>
<p><img src="/software-install/jenkins_plugin.png" alt="plugin"></p>
<p>&emsp;&emsp;怎么选都可以，按照推荐的安装插件省事，但是会安装一堆我不需要的插件，不喜欢，这里我选择的是”选择插件来安装”。在插件列表我只选择了一个<code>Localization: Chinese (Simplified)</code>插件，这个插件是简体中文语言包。</p>
<ul>
<li>创建管理员用户</li>
</ul>
<p>&emsp;&emsp;按照提示创建就好了。创建好了之后后面的一路默认就好了。管理员账号创建好了之后 jenkins 的初始化基本完成了，此时我们可以重启 jenkins 让中文插件生效（重启：在jenkins服务的访问地址后加restart，例如：http:localhost:8080/restart）。</p>
<ul>
<li>全局工具配置</li>
</ul>
<blockquote>
<p>jdk:指定服务器的jdk路径，使用服务器的jdk<br>maven:指定服务器的maven路径，使用服务器的maven</p>
</blockquote>
<p><img src="/software-install/jenkins_global_settings.png" alt="plugin"></p>
<ul>
<li>插件安装</li>
</ul>
<p>&emsp;&emsp;系统管理-插件管理-可选插件：下面是我另外安装的插件列表:</p>
<blockquote>
<p>Role-based Authorization Strategy：jenkins 用户权限角色控制插件；<br>Publish over SSH：允许 Jenkins 所在机器通过SSH服务登录到远程机执行脚本；<br>Git Parameter：参数化构建git项目。根据branch,tag,commit等信息打包发布。在发布时，可以自己选择分支来进行发布；<br>Maven Integration：在构建项目时可以直接构建一个maven项目。</p>
</blockquote>
<ul>
<li>JDK和MAVEN的路径配置</li>
</ul>
<p>&emsp;&emsp;系统管理-全局工具配置:点击新增JDK，取消勾选”自动安装”，将JAVA_HOME的路径copy到此处；点击新增MAVEN，取消勾选”自动安装”，将M2_HOME的路径copy到此处。</p>
<ul>
<li>错误解决</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">HTTP ERROR 403 Problem accessing /view/all/createItem. </span><br><span class="line">Reason: No valid crumb was included <span class="keyword">in</span> the request </span><br><span class="line">Powered by Jetty:// 9.4.z-SNAPSHOT</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;系统管理-全局安全设置-跨站请求伪造保护:取消勾选”防止跨站点请求伪造”。</p>
<h4 id="创建spring-boot项目的任务"><a href="#创建spring-boot项目的任务" class="headerlink" title="创建spring boot项目的任务"></a>创建spring boot项目的任务</h4><ul>
<li>新建任务</li>
</ul>
<p><img src="/software-install/jenkins_task.png" alt="task"></p>
<ul>
<li>配置</li>
</ul>
<ol>
<li><p>描述：随便写一点。</p>
</li>
<li><p>参数化构建过程：勾选，选择<code>Git Parameter</code>，<code>Parameter Type</code>可以选择branch或者tag，看具体情况，然后给定一个默认值，</p>
</li>
</ol>
<p><img src="/software-install/jenkins_general.png" alt="General"></p>
<ol start="3">
<li><p>源码管理：选择git,添加项目的git地址，然后添加用户名密码或者公私钥。<code>Branch Specifier (blank for &#39;any&#39;)</code>使用$符号后面加上前面参数化构建过程中定义的名字，在这里就是<code>$branch</code>。</p>
</li>
<li><p>Pre Steps：add一个”执行shell”。</p>
</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">today=$(date +%Y%m%d) ;</span><br><span class="line"></span><br><span class="line">server_name=ferry ;</span><br><span class="line"></span><br><span class="line">target_dir=/u01/<span class="variable">$server_name</span> ;</span><br><span class="line">start_log_dir=<span class="variable">$target_dir</span>/start_log/<span class="variable">$today</span> ;</span><br><span class="line">start_log=<span class="variable">$start_log_dir</span>/start.log ;</span><br><span class="line">back_up_dir=<span class="variable">$target_dir</span>/back_up/<span class="variable">$today</span> ;</span><br><span class="line">log_dir=<span class="variable">$target_dir</span>/<span class="built_in">log</span>/<span class="variable">$today</span> ;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="built_in">test</span> ! -d <span class="variable">$target_dir</span> ; <span class="keyword">then</span> mkdir -p <span class="variable">$target_dir</span> ; <span class="keyword">fi</span>;</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">test</span> ! -d <span class="variable">$start_log_dir</span> ; <span class="keyword">then</span> mkdir -p <span class="variable">$start_log_dir</span> ; <span class="keyword">fi</span>;</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">test</span> ! -d <span class="variable">$back_up_dir</span> ; <span class="keyword">then</span> mkdir -p <span class="variable">$back_up_dir</span> ; <span class="keyword">fi</span>;</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">test</span> ! -d <span class="variable">$log_dir</span> ; <span class="keyword">then</span> mkdir -p <span class="variable">$log_dir</span> ; <span class="keyword">fi</span>;</span><br><span class="line"></span><br><span class="line">java_pid=`ps -aux|grep java|grep -v grep|grep -v <span class="built_in">kill</span>|grep -v jenkins|awk <span class="string">'&#123;print $2&#125;'</span>` ;</span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> -------------------BEGIN KILL SERVER----------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"><span class="keyword">if</span> [ <span class="variable">$java_pid</span> ] ; </span><br><span class="line"><span class="keyword">then</span> <span class="built_in">kill</span> -s 9 <span class="variable">$java_pid</span> ; </span><br><span class="line"><span class="built_in">echo</span> <span class="built_in">kill</span> java server success &gt;&gt; <span class="variable">$start_log</span> ; </span><br><span class="line"><span class="keyword">else</span> <span class="built_in">echo</span> not exists server is running &gt;&gt; <span class="variable">$start_log</span> ; </span><br><span class="line"><span class="keyword">fi</span> ;</span><br><span class="line"><span class="built_in">echo</span> -------------------END   KILL SERVER----------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> ----------------BEGIN BACKUP OLD SERVER-------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">test</span> -f <span class="variable">$WORKSPACE</span>/core/target/ferry.jar ;</span><br><span class="line"><span class="keyword">then</span> mv <span class="variable">$WORKSPACE</span>/core/target/ferry.jar <span class="variable">$back_up_dir</span>/ ; </span><br><span class="line"><span class="built_in">echo</span> move jar to <span class="variable">$back_up_dir</span> dir &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"><span class="keyword">fi</span> ;</span><br><span class="line"><span class="built_in">echo</span> -----------------END BACKUP OLD SERVER--------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br></pre></td></tr></table></figure>
<ol start="5">
<li><p>Build：Goals and options中填写<code>clean package -Dmaven.test.skip=true -P prod</code></p>
</li>
<li><p>Post Steps：add一个”执行shell”。</p>
</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">BUILD_ID=DONTKILLME</span><br><span class="line">today=$(date +%Y%m%d) ;</span><br><span class="line">server_name=ferry ;</span><br><span class="line"></span><br><span class="line">target_dir=/u01/<span class="variable">$server_name</span> ;</span><br><span class="line">start_log_dir=<span class="variable">$target_dir</span>/start_log/<span class="variable">$today</span> ;</span><br><span class="line">start_log=<span class="variable">$start_log_dir</span>/start.log ;</span><br><span class="line">back_up_dir=<span class="variable">$target_dir</span>/back_up/<span class="variable">$today</span> ;</span><br><span class="line">log_dir=<span class="variable">$target_dir</span>/<span class="built_in">log</span>/<span class="variable">$today</span> ;</span><br><span class="line"></span><br><span class="line"><span class="built_in">echo</span> ----------------BEGIN RUN SERVER-------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"><span class="built_in">cd</span> <span class="variable">$WORKSPACE</span>/core/target/ ;</span><br><span class="line">nohup java -jar ferry.jar &gt; <span class="variable">$log_dir</span>/log.out &amp; </span><br><span class="line"><span class="built_in">echo</span> SERVER RUNNING SUCCESS &gt;&gt; <span class="variable">$start_log</span> ;</span><br><span class="line"><span class="built_in">echo</span> ------------------END RUN SERVER-------------------- &gt;&gt; <span class="variable">$start_log</span> ;</span><br></pre></td></tr></table></figure>
<ul>
<li>执行任务</li>
</ul>
<p>&emsp;&emsp;点击”Build with Parameters”-&gt;选择分支-&gt;点击”开始构建”</p>
<p><img src="/software-install/jenkins_build.png" alt="build"></p>
<h3 id="sysbench安装"><a href="#sysbench安装" class="headerlink" title="sysbench安装"></a>sysbench安装</h3><p>&emsp;&emsp;<a href="https://dev.mysql.com/downloads/benchmarks.html" target="_blank" rel="noopener">sysbench</a>是一个开源的、模块化的、跨平台的多线程性能测试工具，可以用来进行CPU、内存、磁盘I/O、线程、数据库的性能测试。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/</span><br><span class="line">tar -zxvf sysbench-0.4.12.14.tar.gz</span><br><span class="line"><span class="built_in">cd</span> sysbench-0.4.12.14/</span><br><span class="line">./configure --with-mysql-includes=/usr/<span class="built_in">local</span>/mysql/include --with-mysql-libs=/usr/<span class="built_in">local</span>/mysql/lib</span><br><span class="line">make &amp;&amp; make install</span><br></pre></td></tr></table></figure>
<h3 id="nginx安装"><a href="#nginx安装" class="headerlink" title="nginx安装"></a>nginx安装</h3><h4 id="centos源代码编译安装nginx"><a href="#centos源代码编译安装nginx" class="headerlink" title="centos源代码编译安装nginx"></a>centos源代码编译安装nginx</h4><h5 id="基本安装"><a href="#基本安装" class="headerlink" title="基本安装"></a>基本安装</h5><ul>
<li>下载源文件并解压</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /usr/<span class="built_in">local</span>/</span><br><span class="line">wget http://nginx.org/download/nginx-1.16.1.tar.gz</span><br><span class="line">tar -zxvf nginx-1.16.1.tar.gz</span><br></pre></td></tr></table></figure>
<ul>
<li>编译选项的配置参数说明</li>
</ul>
<blockquote>
<p><code>--prefix=path</code>:它定义了存放服务器文件的路径。configure及nginx.conf配置文件中的所有路径都是以该目录作为一个相对路径（库源文件路径除外）。默认情况下，其会被设置为/usr/local/nginx目录。<br><code>--sbin-path=path</code>: 设置nginx可执行文件的名字。该名字只在安装过程中会被用到。默认情况下该文件会被命名为prefix/sbin/nginx<br><code>--conf-path=path</code>: 设置nginx.conf配置文件的名字。假如需要，nginx可以通过命令行参数-c file 来指定一个不同的配置文件。默认情况下，该文件会被设置为prefix/conf/nginx.conf。<br><code>--pid-path=path</code>: 设置nginx.pid文件的名字，该文件会用于存放主进程的进程ID。在安装之后，该文件的名字可以通过nginx.conf文件中的pid指令进行修改。默认情况下该文件会被命名为prefix/logs/nginx.pid<br><code>--error-log-path</code>=path: 设置主要的error,warnings及diagnostic文件的名字。在安装之后，该文件的名字也可以通过nginx.conf文件中的error_log指令进行修改。默认情况下，该文件会被命名为prefix/logs/error.log<br><code>--http-log-path</code>=path: 发送到HTTP Server的请求的日志文件名称。在安装之后，该文件的名称可以通过nginx.conf文件中的access_log指令进行修改。默认情况下，该文件会被命名为prefix/logs/access.log<br><code>--build=name</code>: 设置nginx构建出来后的名字（可选项）<br><code>--user=name</code>: 设置nginx工作进程的所属用户。在安装之后，该名字也可以通过nginx.conf文件中的user指令进行修改。默认的用户名为nobody<br><code>--group=name</code>: 设置nginx工作进程的所属组。在安装之后，该名字也可以通过nginx.conf文件中的user指令进行修改。默认情况下会被设置为非特权用户的用户名称<br><code>--with-select_module/without-select_module</code>: 使能/禁止一个模块使用select()方法。这个模块会被自动的编译构建假如该平台并不支持一些更合适的方法，例如kqueue，epoll，/dev/poll<br><code>--with-poll_module/without-poll_module</code>: 使能/禁止一个模块使用poll()方法。该模块会被自动的编译构建假如该平台并不支持一些更合适的方法，例如kqueue，epoll,/dev/poll。<br><code>--without-http_gzip_module</code>: 禁止构建压缩响应的模块。要想构建及运行此模块，必须依赖与zlib库。<br><code>--without-http_rewrite_module</code>: 禁止构建允许HTTP Server进行请求重定向及改变请求URI的模块。要构建此模块的话需要PCRE库的支持。<br><code>--without-http_proxy_module</code>: 禁止构建HTTP Server proxying模块<br><code>--without-http_ssl_module</code>: 构建HTTP Server对https协议的支持。默认情况下该模块并不会被构建。构建和运行本模块需要OpenSSL的支持<br><code>--with-pcre=path</code>: 设置PCRE库原文件的路径。该库的发布版本(version 4.4 – 8.40)需要从PCRE官方网站下载然后解压。剩余的操作则由nginx的./configure和make完成。在location指令中的表达式匹配及ngx_http_rewrite_module模块需要依赖该库。<br><code>--with-pcre-jit</code>: 构建支持“just-in-time compilation”特性的PCRE库。<br><code>--with-zlib=path</code>: 设置zlib库源文件的路径。该库的发布版本(version 1.1.3 - 1.2.11)需要从zlib官方网站下载然后解压。剩余的操作是由nginx的./configure及make来完成。ngx_http_gzip_module模块需要依赖于该库。<br><code>--with-cc-opt=parameters</code>: 设置一些额外的参数，这些参数会被添加到CFLAGS变量后。当在FreeBSD系统下使用系统PCRE库的时候，–with-cc-opt=”-I /usr/local/include” 应该被指定。假如需要指定select()函数支持的文件句柄数也可以通过这样指定：–with-cc-opt=”-D FD_SETSIZE=2048”。<br><code>--with-ld-opt=parameters</code>: 设置链接时候的一些额外的参数。当在FreeBSD系统下使用PCRE库时，应该指定–with-ld-opt=”-L /usr/local/lib”。<br><code>--with-debug</code>: 将Nginx需要打印debug调试级别日志的代码编译进Nginx。这样可以在Nginx运行时通过修改配置文件来使其打印调试日志，这对于研究、定位Nginx问题非常有帮助</p>
</blockquote>
<ul>
<li>配置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./configure --prefix=/usr/<span class="built_in">local</span>/nginx-1.16.1 --sbin-path=/usr/<span class="built_in">local</span>/nginx-1.16.1/sbin/nginx --conf-path=/usr/<span class="built_in">local</span>/nginx-1.16.1/conf/nginx.conf --pid-path=/usr/<span class="built_in">local</span>/nginx-1.16.1/nginx.pid --error-log-path=/usr/<span class="built_in">local</span>/nginx-1.16.1/logs/error.log --http-log-path=/usr/<span class="built_in">local</span>/nginx-1.16.1/logs/access.log --with-http_ssl_module --with-debug</span><br></pre></td></tr></table></figure>
<ul>
<li>编译</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<ul>
<li>启动</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/<span class="built_in">local</span>/nginx-1.16.1/sbin/nginx -c /usr/<span class="built_in">local</span>/nginx-1.16.1/conf/nginx.conf</span><br></pre></td></tr></table></figure>
<ul>
<li>停止</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/<span class="built_in">local</span>/nginx-1.16.1/sbin/nginx -s quit</span><br></pre></td></tr></table></figure>
<ul>
<li>设置环境变量，nginx全局可用</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /etc/profile</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> NGINX_HOME=/usr/<span class="built_in">local</span>/nginx-1.16.1</span><br><span class="line"><span class="built_in">export</span> PATH=<span class="variable">$&#123;PATH&#125;</span>:<span class="variable">$&#123;NGINX_HOME&#125;</span>/sbin</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure>
<h5 id="将nginx配置成服务-基于centos7"><a href="#将nginx配置成服务-基于centos7" class="headerlink" title="将nginx配置成服务(基于centos7)"></a>将nginx配置成服务(基于centos7)</h5><ul>
<li>创建service文件</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vi /lib/systemd/system/nginx.service</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 基础信息</span></span><br><span class="line">[Unit]</span><br><span class="line"><span class="comment"># 描述</span></span><br><span class="line">Description=Nginx</span><br><span class="line"><span class="comment"># 在某个服务启动后启动</span></span><br><span class="line">After=network.target</span><br><span class="line"></span><br><span class="line"><span class="comment"># 服务信息</span></span><br><span class="line">[Service]</span><br><span class="line">Type=forking</span><br><span class="line"><span class="comment"># 启动服务的命令</span></span><br><span class="line">ExecStart=/usr/<span class="built_in">local</span>/nginx-1.16.1/sbin/nginx -c /usr/<span class="built_in">local</span>/nginx-1.16.1/conf/nginx.conf</span><br><span class="line"><span class="comment"># 终止服务的命令</span></span><br><span class="line">ExecStop=/usr/<span class="built_in">local</span>/nginx-1.16.1/sbin/nginx -s quit</span><br><span class="line"></span><br><span class="line"><span class="comment"># 安装相关信息</span></span><br><span class="line">[Install]</span><br><span class="line"><span class="comment"># WantedBy 是以哪种方式启动：multi-user.target表明当系统以多用户方式（默认的运行级别）启动时，这个服务需要被自动运行。</span></span><br><span class="line">WantedBy=multi-user.target</span><br></pre></td></tr></table></figure>
<ul>
<li>设置可执行权限</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x /lib/systemd/system/nginx.service</span><br></pre></td></tr></table></figure>
<ul>
<li>刷新配置</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl daemon-reload</span><br></pre></td></tr></table></figure>
<ul>
<li>启动服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start nginx.service</span><br></pre></td></tr></table></figure>
<ul>
<li>查看服务</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl status nginx.service</span><br></pre></td></tr></table></figure>
<h3 id="docker安装"><a href="#docker安装" class="headerlink" title="docker安装"></a>docker安装</h3><p>&emsp;&emsp;这里提供传送门<a href="https://docs.docker.com/engine/install/#server" target="_blank" rel="noopener">docker官网的安装说明</a>,可以查看对应的操作系统安装docker的详细步骤。</p>
<h4 id="windows-mac"><a href="#windows-mac" class="headerlink" title="windows/mac"></a>windows/mac</h4><p>&emsp;&emsp;这两种的形式没什么好说的，从<a href="https://www.docker.com/get-started" target="_blank" rel="noopener">官网</a> 下载然后安装就完事了。</p>
<h4 id="centos"><a href="#centos" class="headerlink" title="centos"></a>centos</h4><p>&emsp;&emsp;通过设置仓库的方式安装docker，那么需要设置仓库源，设置仓库源需要用到<code>yum-config-manager</code>，而它来自<code>yum-utils</code>。</p>
<ul>
<li>移除旧的安装包</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum remove docker docker-client docker-client-latest docker-common docker-latest docker-latest-logrotate docker-logrotate docker-engine</span><br></pre></td></tr></table></figure>
<ul>
<li>安装工具</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y yum-utils</span><br></pre></td></tr></table></figure>
<ul>
<li>设置仓库源</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum-config-manager --add-repo http://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;这里使用的是阿里云的源，速度比官方的快，给出其他参考的源：<code>官方：https://download.docker.com/linux/centos/docker-ce.repo</code> , <code>清华大学：https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/centos/docker-ce.repo</code> 。</p>
<ul>
<li>安装社区版引擎</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y docker-ce docker-ce-cli containerd.io</span><br></pre></td></tr></table></figure>
<ul>
<li>安装指定版本引擎</li>
</ul>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum list docker-ce --showduplicates | sort -r</span><br></pre></td></tr></table></figure>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y docker-ce-19.03.13 docker-ce-cli-19.03.13 containerd.io</span><br></pre></td></tr></table></figure>
<p><img src="/software-install/docker-install-error.png" alt="error"></p>
<p>&emsp;&emsp;这一步如果如上图报错，说明<code>containerd.io</code>版本过低，我们手动下载安装高版本的就好了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget https://download.docker.com/linux/centos/7/x86_64/edge/Packages/containerd.io-1.3.7-3.1.el7.x86_64.rpm</span><br><span class="line">yum install -y containerd.io-1.3.7-3.1.el7.x86_64.rpm</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;安装好了之后继续上述的安装docker的操作。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">yum install -y docker-ce docker-ce-cli containerd.io</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;到这里我们docker就安装好了，但是还没有启动，同事docker group也创建了，但是没有用户添加到这个用户组，这个我们后续可以按需设置。</p>
<p>&emsp;&emsp;启动docker服务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">systemctl start docker</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;查看docker信息以验证安装是否成功</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker info</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>软件安装</category>
      </categories>
      <tags>
        <tag>jdk的安装</tag>
      </tags>
  </entry>
  <entry>
    <title>使用javaconfig配置spring项目</title>
    <url>/spring-javaconfig.html</url>
    <content><![CDATA[<p>&emsp;&emsp;使用javaconfig对spring项目进行配置，记录一下常用的注解及其含义，以方便后期回顾。此项目是使用maven搭建的非web项目，是我学习spring练手用的，是向web项目的过渡阶段产物。</p>
<a id="more"></a>
<h3 id="Maven依赖"><a href="#Maven依赖" class="headerlink" title="Maven依赖"></a>Maven依赖</h3><ul>
<li>先添加spring需要的依赖</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-context<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.0.6.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.0.6.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--测试依赖--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.12<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--日志依赖--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>slf4j-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.7.25<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--lombok--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.18.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>javax.persistence<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>persistence-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="自动化装配bean"><a href="#自动化装配bean" class="headerlink" title="自动化装配bean"></a>自动化装配bean</h3><p>&emsp;&emsp;新建一个javaconfig包，所有的配置类都放在这个包下面。</p>
<h4 id="Configuration-配置类声明"><a href="#Configuration-配置类声明" class="headerlink" title="@Configuration 配置类声明"></a>@Configuration 配置类声明</h4><ul>
<li>新建一个类，这里我的类名为<code>BaseBeanJavaConfig</code></li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BaseBeanJavaConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;<code>@Configuration</code>注解声明这是一个配置类,所有的配置类都必须使用这个注解，否则spring又怎么会知道他是一个配置类呢。</p>
<h4 id="ComponentScan-组件扫描"><a href="#ComponentScan-组件扫描" class="headerlink" title="@ComponentScan 组件扫描"></a>@ComponentScan 组件扫描</h4><p>&emsp;&emsp;组件扫描有多种方式可供选择：</p>
<p>1.没有使用<code>@ComponentScan</code>注解，那么将按照默认规则扫描该配置类所在包。</p>
<p>2.使用<code>@ComponentScan</code>注解，形如下面代码块所示，那么spring将扫描<code>service.impl</code>包及向下所有包中含有<code>@Component</code>注解的类,在{}中可以使用<code>,</code>分隔设置多个包，这样可以扫描多个包啦。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackages = &#123;<span class="string">"cn.org.ferry.sys.service.impl"</span>&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BaseBeanJavaConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>3.使用class类型，这种方式spring将扫描该类所在包及向下所有包，当然，这种方式也可以用<code>,</code>分隔设置多个类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackageClasses = &#123;SysUserServiceImpl<span class="class">.<span class="keyword">class</span>&#125;)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">BaseBeanJavaConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;一般情况下，我们都是将配置类单独放在一个包中的，毕竟如果业务代码包中多了一个配置类感觉上也怪怪的，所以第一种方式放弃，第二种和第三种方式相比较，如果你重构了代码，包路径改变了，那么使用第二种方式就会出现问题，所以一般情况下采取第三种方案来实现组件扫描。</p>
<p>&emsp;&emsp;值得一提的是，不管使用那种方式扫描包，你需要装配的bean都需要在类级别上加上注解，毕竟spring扫描的时候也只是扫描指定的包下面含有该注解的类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 说一下我对这四种注解的简单看法，目前的理解就是在不同的类中使用特定的注解让人更容易理解这个类的作用</span></span><br><span class="line"><span class="meta">@Component</span>  <span class="comment">// 不管什么情况，用它都不会报错</span></span><br><span class="line"><span class="comment">// @Controller 控制器中使用该注解</span></span><br><span class="line"><span class="comment">// @Service service.impl中使用该注解</span></span><br><span class="line"><span class="comment">// @Repository  与mybatis框架整合时 mapper 包中使用此注解</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Student</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="装配bean"><a href="#装配bean" class="headerlink" title="装配bean"></a>装配bean</h4><p>&emsp;&emsp;以上操作即可通过spring生成bean，那么接下来就是装配bean了，简单说就是如何使用这个bean。</p>
<ul>
<li>就拿上面那个<code>Student</code>类来举例，添加一个方法。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Student</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.getClass().getSimpleName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>在测试类中进行测试这个类有没有实现自动装配</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RunWith</span>(SpringJUnit4ClassRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ContextConfiguration</span>(<span class="title">classes</span> </span>= BaseBeanJavaConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">StudentTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> Student student;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(student.toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/spring-javaconfig/spring_auto_bean_test.png" alt="测试结果"></p>
<p>&emsp;&emsp;当然bean的使用不局限于设置属性这一种方法，也可以通过构造器或者是任意一个方法的形参列表传递。下面的代码结果与上述图片展示结果一样。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StudentUse</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Student student;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">StudentUse</span><span class="params">(Student student)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.student = student;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">info</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(student.toString()); <span class="comment">// 使用这个bean</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过给<code>@Autowired</code>注解加上属性<code>required = false</code>来避免当没有匹配的bean时spring抛出异常，但是这样设置就得在使用这个bean时进行判空了，因为它有可能没有匹配的bean而是个空值。</p>
<h3 id="显式装配bean"><a href="#显式装配bean" class="headerlink" title="显式装配bean"></a>显式装配bean</h3><p>&emsp;&emsp;有时候，自动化配置方案行不通，就只能够显式的配置，例如，你希望将第三方类库中的类配置为bean，但是你没有办法对该类进行修改添加<code>@Component</code>注解,那么可以采用显式装配方式进行配置。</p>
<h4 id="简单bean配置"><a href="#简单bean配置" class="headerlink" title="简单bean配置"></a>简单bean配置</h4><p>&emsp;&emsp;基于一些因素，现在有一个Apple类无法通过自动装配完成bean的装配，现在需要明确的配置该bean。</p>
<ul>
<li>Apple类样例</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.getClass().getSimpleName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>配置</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RootJavaConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Apple <span class="title">apple</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Apple();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过上面代码，在一个配置类中的方法上使用<code>@Bean</code>注解，返回值类型是你希望注册的Bean的类型，在该方法中，你可以使用一切java所能实现的技术来满足你的需求以便返回实例。值得一提的是，你可以在该方法的形参列表中传递其他的bean来配合你完成工作。</p>
<ul>
<li>测试</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RunWith</span>(SpringJUnit4ClassRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ContextConfiguration</span>(<span class="title">classes</span> </span>= RootJavaConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">AppleTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> Apple apple;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(apple.toString());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/spring-javaconfig/spring_bean_test2.png" alt="测试结果"></p>
<p>&emsp;&emsp;显而易见，这样装配是没有问题的，在实际应用中，我们可能会使用到第三方类库中的类，例如数据源，那么我们就可以使用这种显式装配的方式将他配置起来。</p>
<h4 id="给bean设置id"><a href="#给bean设置id" class="headerlink" title="给bean设置id"></a>给bean设置id</h4><p>&emsp;&emsp;在默认的情况下，bean的id是和带有<code>@Bean</code>注解的方法名一样的，如上例，bean的id为apple。在实际应用中，我们有可能需要自定义bean的id，那么可以通过下面方式完成操作。</p>
<ul>
<li>设置bean的id</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RootJavaConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"appleBean"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Apple <span class="title">apple</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Apple();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;通过在<code>@Bean</code>注解上添加<code>name</code>属性值为bean赋上id。</p>
<h3 id="混合配置"><a href="#混合配置" class="headerlink" title="混合配置"></a>混合配置</h3><p>&emsp;&emsp;项目的配置需要视情况而定，有时候使用xml配置项目是最佳方案，而在spring中，xml配置和javaconfig配置并不是互斥的，那么就可以同时使用这两种方案来实现项目的最优配置。</p>
<p>&emsp;&emsp;在自动装配过程中，spring并不会在意你使用的bean是怎么来的，如果你引用了一个外部的配置中的bean，那么你只需要引用一下那个配置类/配置文件即可。</p>
<h4 id="配置类组合"><a href="#配置类组合" class="headerlink" title="配置类组合"></a>配置类组合</h4><p>&emsp;&emsp;在实际应用中，我们可能根据功能的区分定义多个配置类，这样便于维护以及理解，使得配置类不会过于臃肿。那么问题来了，如何整合众多的配置类呢？</p>
<ul>
<li>配置类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackageClasses = &#123;Student<span class="class">.<span class="keyword">class</span>&#125;)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">BaseBeanConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span>(name = <span class="string">"sysUserServiceImpl"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ISysUserService <span class="title">sysUserServiceImpl</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SysUserServiceImpl();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DataDourceConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>整合配置类</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@Import</span>(&#123;BaseBeanConfig<span class="class">.<span class="keyword">class</span>,<span class="title">DataDourceConfig</span>.<span class="title">class</span>&#125;)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">RootJavaConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用<code>@Import</code>注解引入一个配置类，在括号中填入需要引入的配置类的class类型，更通用的方法是建立一个根配置类，在这个配置类中引入所有的配置类。</p>
<h4 id="配置类引入xml配置文件"><a href="#配置类引入xml配置文件" class="headerlink" title="配置类引入xml配置文件"></a>配置类引入xml配置文件</h4><ul>
<li>引入配置文件</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@Import</span>(&#123;BaseBeanConfig<span class="class">.<span class="keyword">class</span>&#125;)</span></span><br><span class="line"><span class="class">@<span class="title">ImportResource</span>(</span>&#123;<span class="string">"classpath:/spring/applicationContext-spring.xml"</span>&#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RootJavaConfig</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>&emsp;&emsp;使用<code>@ImportResource</code>注解，后面跟上字符串类型的配置文件的相对路径即可引入对应的配置文件，同样的，也可以使用<code>,</code>分隔然后引入多个配置文件。</p>
<h3 id="高级装配"><a href="#高级装配" class="headerlink" title="高级装配"></a>高级装配</h3><p>&emsp;&emsp;spring的bean装配技术还有许多技巧，借助它们可以实现更为高级的bean装配功能.</p>
<h4 id="profile"><a href="#profile" class="headerlink" title="profile"></a>profile</h4><p>&emsp;&emsp;在实际开发中，我们在项目开发到一定程度时需要迁移环境，例如开发环境，测试环境，生产环境等之间的切换。在不同的环境可能需要创建不同的bean，那么我们可以借助<code>@Profile</code>注解来实现这个功能。<code>@Profile</code>注解有两种使用方式，下面列举。</p>
<ul>
<li>例子</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Fruits</span> </span>&#123;</span><br><span class="line">    <span class="function">String <span class="title">info</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Banana</span> <span class="keyword">implements</span> <span class="title">Fruits</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.getClass().getSimpleName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span> <span class="keyword">implements</span> <span class="title">Fruits</span> </span>&#123;</span><br><span class="line">     <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">         <span class="keyword">return</span> <span class="keyword">this</span>.getClass().getSimpleName();</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>类级别使用<code>@Profile</code>注解</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackageClasses = &#123;Fruits<span class="class">.<span class="keyword">class</span>&#125;)</span></span><br><span class="line">@Profile("dev")</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BaseBeanConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span>(<span class="string">"apple"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Fruits <span class="title">apple</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Apple();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>测试，在测试类中使用<code>@ActiveProfiles</code>注解来选择激活的环境。</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RunWith</span>(SpringJUnit4ClassRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ContextConfiguration</span>(<span class="title">classes</span> </span>= RootJavaConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line">@ActiveProfiles("dev")</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FruitTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> Fruits fruits;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(fruits.info());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>方法级别使用<code>@Profile</code>注解</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(basePackageClasses = &#123;Fruits<span class="class">.<span class="keyword">class</span>&#125;)</span></span><br><span class="line"><span class="class"><span class="title">public</span> <span class="title">class</span> <span class="title">BaseBeanConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span>(<span class="string">"apple"</span>)</span><br><span class="line">    <span class="meta">@Profile</span>(<span class="string">"dev"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Fruits <span class="title">apple</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Apple();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Bean</span>(<span class="string">"banana"</span>)</span><br><span class="line">    <span class="meta">@Profile</span>(<span class="string">"test"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Fruits <span class="title">banana</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Banana();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>测试，此时激活dev环境，那么控制台会打印出Apple</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RunWith</span>(SpringJUnit4ClassRunner<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line"><span class="class">@<span class="title">ContextConfiguration</span>(<span class="title">classes</span> </span>= RootJavaConfig<span class="class">.<span class="keyword">class</span>)</span></span><br><span class="line">@ActiveProfiles("dev")</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FruitTest</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> Fruits fruits;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(fruits.info());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img src="/spring-javaconfig/spring_bean_test2.png" alt="测试结果"></p>
<h4 id="激活profile"><a href="#激活profile" class="headerlink" title="激活profile"></a>激活profile</h4><p>&emsp;&emsp;spring在确定哪个profile处于激活状态时，依赖于两个属性：<code>spring.profiles.active</code>和<code>spring.profiles.default</code>（<code>spring.profiles.active</code>的优先级更高一点）。设置这两个属性值有下列几种办法。</p>
<ul>
<li>作为<code>DispatcherServlet</code>的初始化参数；</li>
<li>作为Web应用的上下文参数；</li>
<li>作为JDNI条目；</li>
<li>作为环境变量；</li>
<li>作为JVM的系统属性；</li>
<li>集成测试类中，使用<code>@ActiveProfiles</code>注解设置。</li>
</ul>
<h4 id="处理自动装配的歧义性"><a href="#处理自动装配的歧义性" class="headerlink" title="处理自动装配的歧义性"></a>处理自动装配的歧义性</h4><p>&emsp;&emsp;在使用自动装配的时候，可能一个接口下面有很多实现类，我们为此都实现了<code>@Component</code>注解，那么在自动装配的时候，spring就会无法判断到底要选择哪个实现类进行装配。</p>
<ul>
<li><code>@Primary</code>注解，首选标记</li>
</ul>
<p>&emsp;&emsp;可以在你希望装配的实现类上除了<code>@Component</code>注解再加上<code>@Primary</code>注解，这表明当接口下有很多实现类都实现了<code>@Component</code>注解时将首选使用了<code>@Primary</code>注解的实现类进行装配。</p>
<ul>
<li><code>@Qualifier</code>注解，使用限定符来限定注入的bean的具体实现</li>
</ul>
<p>&emsp;&emsp;也可以不使用<code>@Primary</code>注解，那么在注入bean的地方可以使用<code>@Qualifier</code>注解表明本次注入的bean。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="meta">@Qualifier</span>(<span class="string">"apple"</span>)</span><br><span class="line"><span class="keyword">private</span> Fruits fruits;</span><br></pre></td></tr></table></figure>
<ul>
<li>限定符默认为这个bean的ID，也可以自定义限定符</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Qualifier</span>(<span class="string">"apple"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Apple</span> <span class="keyword">implements</span> <span class="title">Fruits</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">info</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.getClass().getSimpleName();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>spring</tag>
        <tag>javaconfig</tag>
        <tag>自动装配</tag>
        <tag>高级装配</tag>
      </tags>
  </entry>
</search>
